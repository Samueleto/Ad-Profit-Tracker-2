# Zeydoo Endpoint Scheduled Sync

POST /api/networks/zeydoo/scheduled-sync

## Description

REQUEST: POST with no body. Secured via an internal API key in the 'x-internal-secret' header. PROCESS: 1. Validate the 'x-internal-secret' header against the INTERNAL_SYNC_SECRET environment variable. 2. Perform a collection-group query on networkConfigs for all documents where networkId=='zeydoo', isActive==true, and syncSchedule matches the job frequency (e.g., 'daily'). 3. For each matching config, get the userId and fetch/decrypt the corresponding API key from the apiKeys collection. 4. Call the Zeydoo API for yesterday's stats, parse the response, and batch-upsert records into the adStats collection. 5. Update the user's networkConfigs document with the sync status and timestamp. 6. Write an audit log for each user's sync attempt. SECURITY: Internal-only endpoint, not callable by end-users. Uses a shared secret for auth instead of a user's Firebase ID token.

---
Generated by VisualPRD