# Feature User Settings Preferences Endpoint Patch Preferences

PATCH /api/settings/preferences

## Description

REQUEST: PATCH { timezone?: string, currency?: string, defaultDateRange?: string, notifications?: { dailySummaryEmail?: boolean, weeklyReportEmail?: boolean } }. All fields optional — only provided fields are updated (merge/upsert pattern).
PROCESS: 1. Extract Bearer token from Authorization header. 2. Verify Firebase ID token via Firebase Admin SDK — return 401 immediately if invalid. 3. Validate request body: if timezone provided, validate it is a known IANA timezone string (check against a curated list or use a validation library like 'luxon' or 'Intl.supportedValuesOf'); if currency provided, validate it is a known ISO 4217 code; if defaultDateRange provided, validate it is one of the allowed enum values ('last_7_days', 'last_14_days', 'last_30_days', 'this_month'); notification fields must be boolean if provided. Return 400 with field-specific error messages for any invalid values. 4. Build a Firestore update object using dot-notation field paths (e.g., 'preferences.timezone') so only specified fields are updated without overwriting others. 5. Add 'preferences.updatedAt': admin.firestore.FieldValue.serverTimestamp() to the update object. 6. Call users/{uid}.update(updateObject) — this is an upsert-safe merge since we use dot-notation paths. 7. Return the full updated preferences object.
SUCCESS (200): { timezone: string, currency: string, defaultDateRange: string, notifications: { dailySummaryEmail: boolean, weeklyReportEmail: boolean }, updatedAt: timestamp }
ERRORS: 400 (invalid timezone string, invalid currency code, invalid defaultDateRange enum, non-boolean notification value), 401 (invalid/expired token), 403 (attempting to update another user's preferences — should not occur if UID from token), 429 (rate limited), 500 (Firestore write failure)
SECURITY: Firebase Admin SDK token verification as step 1. UID always from verified token. Whitelist-only field updates — no arbitrary Firestore fields can be written. Server timestamp prevents client timestamp forgery.

---
Generated by VisualPRD