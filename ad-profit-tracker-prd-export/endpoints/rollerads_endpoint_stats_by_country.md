# Rollerads Endpoint Stats By Country

GET /api/networks/rollerads/stats/by-country

## Description

REQUEST: GET with query params: dateFrom (YYYY-MM-DD, required), dateTo (YYYY-MM-DD, required), limit? (integer 1–100, default 20 — top N countries by revenue).
PROCESS:
1. Verify Firebase ID token — return 401 if invalid.
2. Validate dateFrom, dateTo, and limit. Return 400 if invalid.
3. Query adStats Firestore collection: filter userId==uid AND networkId=='rollerads' AND date>=dateFrom AND date<=dateTo AND country != null. Use getDocs.
4. Group results by country in memory: sum revenue, impressions, clicks per country across the date range.
5. Sort by total revenue descending (RollerAds is revenue-only — sort by revenue, not cost). Slice to limit.
6. Compute revenueShare percentage for each country (country revenue / total revenue * 100).
7. Enrich country codes with full country names using a static ISO 3166-1 alpha-2 lookup map.
SUCCESS (200): { networkId: 'rollerads', dateFrom, dateTo, countries: [{ country: string, countryName: string, revenue: number, impressions: number, clicks: number, ctr: number, revenueShare: number }], totalRevenue: number }
ERRORS:
- 400: missing/invalid params
- 401: unauthenticated
- 500: Firestore read failure
SECURITY: userId from verified token. Query scoped to authenticated user only.

---
Generated by VisualPRD