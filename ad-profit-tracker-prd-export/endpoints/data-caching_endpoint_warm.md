# Data Caching Endpoint Warm

POST /api/cache/warm

## Description

REQUEST: POST { tasks: [{ endpoint: string, params: object }] }. Internal endpoint to pre-populate or 'warm' the server-side cache. PROCESS: 1. Validate the 'x-internal-secret' header. 2. Iterate through the list of tasks. 3. For each task, invoke the corresponding internal data-fetching logic (e.g., the core logic of GET /api/stats/summary) with the provided params. This will fetch fresh data from ad networks and populate the cache. 4. Log the warming activity to `auditLogs` with action='cache_warmed'. SUCCESS (200): { tasksProcessed: number, tasksFailed: number }. ERRORS: 401 (invalid secret). SECURITY: Internal-only endpoint, protected by a shared secret. Not callable by end-users. Triggered by a scheduler like Cloud Scheduler.

---
Generated by VisualPRD