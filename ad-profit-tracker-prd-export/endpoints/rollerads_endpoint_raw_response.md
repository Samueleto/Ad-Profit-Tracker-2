# Rollerads Endpoint Raw Response

GET /api/networks/rollerads/raw-response

## Description

REQUEST: GET with query params: date (YYYY-MM-DD, required) — fetches the stored raw API response snapshots for a single day to power the API Explorer tab.
PROCESS:
1. Verify Firebase ID token — return 401 if invalid.
2. Validate date param: required, valid YYYY-MM-DD. Return 400 if invalid.
3. Query adStats Firestore collection: filter userId==uid AND networkId=='rollerads' AND date==date. Use getDocs.
4. If no documents found, return 404.
5. Map documents to an array of rawResponse objects (the sanitized API snapshot stored during sync). Include the country and date fields for context.
6. Return the raw response array alongside a fieldSchema array describing the RollerAds /sites/stat response fields for the API Explorer UI. fieldSchema describes: site_id (string, unique site identifier), date (string, YYYY-MM-DD), country (string, ISO alpha-2 country code), impressions (number, total ad impressions), clicks (number, total clicks), ctr (number, click-through rate as decimal), revenue (number, total earnings in USD), cpm (number, cost per mille), ecpm (number, effective CPM).
SUCCESS (200): { networkId: 'rollerads', date: string, records: [{ country: string, rawResponse: map }], fieldSchema: [{ field: string, type: string, description: string }] }
ERRORS:
- 400: missing/invalid date param
- 401: unauthenticated
- 404: no data found for this date (sync not yet run)
- 500: Firestore read failure
SECURITY: userId from verified token. rawResponse fields were sanitized at write time — no credentials stored.

---
Generated by VisualPRD