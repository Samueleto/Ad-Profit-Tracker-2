# Reconciliation Endpoint Rules Patch

PATCH /api/reconciliation/rules

## Description

REQUEST: PATCH { networkId: string (required), rules: { maxDailyRevenueUSD?: number (0–1000000), minDailyRevenueUSD?: number (0–1000000), maxDailyCostUSD?: number (0–1000000), maxCtrPercent?: number (0–100), minImpressions?: number (0–10000000), allowNullCountry?: boolean } }
PROCESS:
1. Verify Firebase ID token — return 401 if invalid. uid from verified token only.
2. Validate networkId: must be one of the 4 supported networks — return 400 if not.
3. Validate each provided rule field against its allowed range. Return 400 with specific field errors if any value is out of range.
4. At least one rule field must be provided — return 400 if rules object is empty.
5. Check rate limit: max 20 rule updates/hour per user — return 429 if exceeded.
6. Fetch networkConfigs document for userId+networkId — return 404 if not found.
7. Merge provided rule fields into the existing validationRules map (partial update — only overwrite provided fields). Use Firestore Admin SDK dot-notation field paths for partial map updates: e.g., 'validationRules.maxDailyRevenueUSD'. Set updatedAt=serverTimestamp().
8. Write audit log entry to auditLogs via Admin SDK: action='network_config_updated', resourceType='reconciliation', resourceId=networkId, metadata={ changedRules: { field: { from: oldValue, to: newValue } } }, status='success'.
9. Return the full updated rules object (merged with defaults for any still-null fields).
SUCCESS (200): { networkId: string, rules: object, updatedAt: timestamp }
ERRORS:
- 400: invalid networkId, rule value out of range, empty rules object
- 401: unauthenticated
- 404: networkConfigs document not found
- 429: rate limit exceeded (20/hour)
- 500: Firestore write failure
SECURITY: userId from verified token. Users can only update their own network configs. Audit logged for accountability.

---
Generated by VisualPRD