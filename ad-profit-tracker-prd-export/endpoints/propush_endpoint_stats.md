# Propush Endpoint Stats

GET /api/networks/propush/stats

## Description

REQUEST: GET with query params: dateFrom (YYYY-MM-DD, required), dateTo (YYYY-MM-DD, required), groupBy? ('day'|'total', default 'day'). PROCESS: 1. Verify Firebase ID token. 2. Validate date range (max 90 days). 3. Query the adStats Firestore collection, filtering by userId, networkId=='propush', and the date range. 4. Aggregate the results based on the 'groupBy' parameter, summing revenue and other metrics. 5. Return the structured, aggregated data. SECURITY: Query is always scoped to the authenticated user's data.

---
Generated by VisualPRD