# Feature Network Configuration Management Endpoint ConfigUpdate

PATCH /api/networks/config/update

## Description

REQUEST: PATCH { networkId: string, isActive?: boolean, syncSchedule?: 'hourly'|'daily'|'weekly', endpointOverride?: string|null, timeoutSeconds?: number, retryAttempts?: number, displayOrder?: number }. All fields except networkId are optional — only provided fields are updated. PROCESS: 1. Verify Firebase ID token — return 401 if invalid. 2. Validate networkId is one of ['exoclick','rollerads','zeydoo','propush'] — return 400 if not. 3. Validate optional fields: syncSchedule must be one of the allowed enum values; endpointOverride must be a valid URL or null; timeoutSeconds must be integer 5–60; retryAttempts must be integer 1–5; displayOrder must be non-negative integer. Return 400 with field-specific errors for any invalid values. 4. Build Firestore update object using dot-notation paths to avoid overwriting unrelated fields. Add updatedAt: admin.firestore.FieldValue.serverTimestamp(). 5. Upsert to networkConfigs document at path users/{uid}/networkConfigs/{networkId} (or top-level networkConfigs collection keyed by uid+networkId). 6. Return updated config document. SUCCESS (200): { networkId, isActive, syncSchedule, endpointOverride, timeoutSeconds, retryAttempts, displayOrder, updatedAt } ERRORS: 400 (invalid networkId, invalid field values), 401 (unauthenticated), 429 (rate limited), 500 (Firestore write failure). SECURITY: UID from verified token only. Whitelist-only field updates — dataRole, userId, createdAt are immutable and cannot be set via this endpoint.

---
Generated by VisualPRD