# Auth Endpoint Delete Account

DELETE /api/auth/delete-account

## Description

REQUEST: DELETE — no body. Bearer token in Authorization header.
PROCESS: 1. Verify Firebase ID token via Admin SDK. 2. Delete users/{uid} Firestore document. 3. Delete Firebase Auth user via admin.auth().deleteUser(uid). 4. Return success. Note: apiKeys subcollection/collection cleanup should be triggered here as a batch delete.
SUCCESS (200): { deleted: true }
ERRORS: 401 (invalid token), 404 (user not found), 500 (deletion failure — partial state possible, log for manual cleanup)
SECURITY: Admin SDK deleteUser ensures Firebase Auth account is removed. Firestore document deleted in same request. Irreversible — no soft delete.

---
Generated by VisualPRD