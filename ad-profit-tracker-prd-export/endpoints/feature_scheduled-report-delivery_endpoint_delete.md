# Feature Scheduled Report Delivery Endpoint Delete

DELETE /api/schedules/:id

## Description

REQUEST: DELETE /api/schedules/:id — :id is the schedule UUID in the URL path. Bearer token in Authorization header. PROCESS: 1. Verify Firebase ID token via admin.auth().verifyIdToken(token) — return 401 if invalid. uid from verified token only. 2. Validate :id: required, non-empty string — return 400 if missing. 3. Check rate limit: max 20 schedule deletes/hour per user — return 429 if exceeded. 4. Read users Firestore document for uid. Find the schedule with matching id in users.scheduledReports array. Return 404 if not found. 5. Use Firestore Admin SDK arrayRemove to remove the matching schedule object from users.scheduledReports. Set users.scheduledReportsUpdatedAt=serverTimestamp(). 6. Write audit log entry to auditLogs via Admin SDK: action='scheduled_report_deleted', resourceType='scheduledReport', resourceId=id, metadata={ reportName: deletedSchedule.reportName, frequency: deletedSchedule.frequency, deliveryEmail: deletedSchedule.deliveryEmail }, status='success'. 7. Return deletion confirmation. SUCCESS (200): { deleted: true, scheduleId: string }. ERRORS: 400 (missing id), 401 (unauthenticated), 404 (schedule not found), 429 (rate limit exceeded), 500 (Firestore write failure). SECURITY: userId from verified token. Schedule ownership verified by reading users document scoped to uid before deletion. Audit logged.

---
Generated by VisualPRD