# Exoclick Endpoint StatsByCountry

GET /api/networks/exoclick/stats/by-country

## Description

REQUEST: GET with query params: dateFrom (YYYY-MM-DD, required), dateTo (YYYY-MM-DD, required), limit? (integer 1–100, default 20 — top N countries by cost).
PROCESS:
1. Verify Firebase ID token — return 401 if invalid.
2. Validate dateFrom, dateTo, and limit. Return 400 if invalid.
3. Query adStats Firestore collection: filter userId==uid AND networkId=='exoclick' AND date>=dateFrom AND date<=dateTo AND country != null. Use getDocs.
4. Group results by country in memory: sum cost, impressions, clicks per country across the date range.
5. Sort by total cost descending. Slice to limit.
6. Compute percentage share of total cost for each country.
SUCCESS (200): { networkId: 'exoclick', dateFrom, dateTo, countries: [{ country: string, countryName: string, cost: number, impressions: number, clicks: number, ctr: number, costShare: number }], totalCost: number }
ERRORS:
- 400: missing/invalid params
- 401: unauthenticated
- 500: Firestore read failure
SECURITY: userId from verified token. Query scoped to authenticated user only.

---
Generated by VisualPRD