# Exoclick Endpoint RawResponse

GET /api/networks/exoclick/raw-response

## Description

REQUEST: GET with query params: date (YYYY-MM-DD, required) — fetches the stored raw API response snapshots for a single day to power the API Explorer tab.
PROCESS:
1. Verify Firebase ID token — return 401 if invalid.
2. Validate date param: required, valid YYYY-MM-DD. Return 400 if invalid.
3. Query adStats Firestore collection: filter userId==uid AND networkId=='exoclick' AND date==date. Use getDocs.
4. Map documents to an array of rawResponse objects (the sanitized API snapshot stored during sync). Include the country and date fields for context.
5. Return the raw response array alongside a schema description of the ExoClick API fields for the API Explorer UI.
SUCCESS (200): { networkId: 'exoclick', date: string, records: [{ country: string, rawResponse: map }], fieldSchema: [{ field: string, type: string, description: string }] }
ERRORS:
- 400: missing/invalid date param
- 401: unauthenticated
- 404: no data found for this date (sync not yet run)
- 500: Firestore read failure
SECURITY: userId from verified token. rawResponse fields were sanitized at write time — no credentials stored.

---
Generated by VisualPRD