# Feature In App Notifications Endpoint Clear

DELETE /api/notifications/clear

## Description

REQUEST: DELETE — no body. Bearer token in Authorization header.
PROCESS:
1. Verify Firebase ID token via admin.auth().verifyIdToken(token) — return 401 if invalid. uid from verified token only.
2. Check rate limit: max 10 clear-all actions/hour per user — return 429 if exceeded.
3. Read users Firestore document for uid. If users.notifications is null/empty, return { cleared: 0 } immediately.
4. Count total non-dismissed notifications to report cleared count.
5. Use Firestore Admin SDK to set users.notifications=[] (empty array), users.unreadNotificationCount=0, users.notificationsUpdatedAt=serverTimestamp().
6. Write audit log entry to auditLogs Firestore collection via Admin SDK: action='preferences_updated', resourceType='notification', resourceId='all', metadata={ clearedCount: number, action: 'clear_all' }, status='success'.
7. Return cleared count.
SUCCESS (200): { cleared: number, unreadCount: 0 }
ERRORS:
- 401: unauthenticated
- 404: users document not found
- 429: rate limit exceeded (10/hour)
- 500: Firestore write failure
SECURITY: userId from verified token. Destructive action — audit logged. Scoped to authenticated user only.

---
Generated by VisualPRD