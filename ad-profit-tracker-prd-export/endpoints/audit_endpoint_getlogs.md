# Audit Endpoint GetLogs

GET /api/audit/logs

## Description

REQUEST: GET with query params: action? (comma-separated enum values), resourceType? (enum), startDate? (ISO 8601), endDate? (ISO 8601), status? ('success'|'failure'), search? (text search on metadata stringified, max 100 chars), limit? (default 25, max 100), cursor? (Firestore document ID for pagination)
PROCESS: 1. Verify Firebase ID token via admin.auth().verifyIdToken(token) — return 401 if invalid. 2. Validate query params: action values must be from allowed enum; dates must be valid ISO 8601; limit must be integer 1–100; status must be 'success' or 'failure' if provided. Return 400 with field-specific errors for invalid params. 3. Build Firestore query on auditLogs collection: always filter userId == uid (from verified token). Apply action filter if provided (using 'in' operator, max 10 values). Apply date range filter on createdAt if provided. Apply status filter if provided. Order by createdAt DESC. Apply cursor-based pagination using startAfter(cursor document) if cursor provided. Limit to requested page size + 1 (to detect hasMore). 4. Execute getDocs query. 5. If results.length > limit, set hasMore=true and slice to limit. 6. Map documents to response shape (exclude internal Firestore metadata). 7. Return paginated response.
SUCCESS (200): { logs: [{ id, action, resourceType, resourceId, metadata, status, errorMessage, ipAddress, userAgent, createdAt }], total: number (approximate), hasMore: boolean, nextCursor: string|null }
ERRORS: 400 (invalid query params), 401 (unauthenticated), 429 (rate limited), 500 (Firestore read failure)
SECURITY: userId always from verified token — users can only query their own logs. Composite Firestore index on (userId, createdAt DESC) required. Text search is client-side filtering on returned results (Firestore doesn't support full-text search natively) — search param applied after getDocs for the current page.

---
Generated by VisualPRD