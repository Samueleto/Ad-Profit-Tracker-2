# Feature Scheduled Report Delivery Endpoint Update

PATCH /api/schedules/:id

## Description

REQUEST: PATCH { frequency?: 'daily'|'weekly'|'monthly', dayOfWeek?: number (0-6), dayOfMonth?: number (1-28), deliveryHour?: number (0-23), timezone?: string (IANA), deliveryEmail?: string, dateRangePreset?: 'yesterday'|'last_7'|'last_30'|'last_90'|'this_month', format?: 'excel'|'pdf', isActive?: boolean }. At least one field required. :id is the schedule UUID in the URL path. PROCESS: 1. Verify Firebase ID token via admin.auth().verifyIdToken(token) — return 401 if invalid. uid from verified token only. 2. Validate :id: required, non-empty string — return 400 if missing. 3. Validate all provided fields using same rules as POST /api/schedules/create. At least one field must be provided — return 400 if body is empty. 4. Check rate limit: max 20 schedule updates/hour per user — return 429 if exceeded. 5. Read users Firestore document for uid. Find the schedule with matching id in users.scheduledReports array. Return 404 if not found. 6. Build updated schedule object by merging provided fields into the existing schedule object. Recompute nextRunAt using date-fns-tz based on updated frequency/deliveryHour/timezone. Set updatedAt=serverTimestamp(). 7. Use Firestore Admin SDK to update the users document: use arrayRemove to remove the old schedule object, then arrayUnion to add the updated schedule object. Set users.scheduledReportsUpdatedAt=serverTimestamp(). Execute as a Firestore transaction to prevent race conditions. 8. Write audit log entry to auditLogs via Admin SDK: action='scheduled_report_updated', resourceType='scheduledReport', resourceId=id, metadata={ changedFields: Object.keys(body), reportName, frequency: updatedFrequency, isActive }, status='success'. 9. Return the full updated schedule object. SUCCESS (200): { schedule: { id, reportId, reportName, frequency, dayOfWeek, dayOfMonth, deliveryHour, timezone, deliveryEmail, dateRangePreset, format, isActive, lastRunAt, lastRunStatus, nextRunAt, updatedAt } }. ERRORS: 400 (invalid fields, empty body, missing id), 401 (unauthenticated), 404 (schedule not found), 429 (rate limit exceeded), 500 (Firestore transaction failure). SECURITY: userId from verified token. Schedule ownership verified by reading users document scoped to uid before update. Audit logged.

---
Generated by VisualPRD