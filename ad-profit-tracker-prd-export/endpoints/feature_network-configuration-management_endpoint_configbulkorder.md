# Feature Network Configuration Management Endpoint ConfigBulkOrder

PATCH /api/networks/config/reorder

## Description

REQUEST: PATCH { order: [{ networkId: string, displayOrder: number }] } — array of all 4 networks with their new display order values after drag-and-drop. PROCESS: 1. Verify Firebase ID token — return 401 if invalid. 2. Validate order array: must contain exactly 4 items, each with a valid networkId and non-negative integer displayOrder. Return 400 if validation fails. 3. Use Firestore batch write to atomically update displayOrder on all 4 networkConfigs documents for this user. Add updatedAt: serverTimestamp() to each. 4. Return success with updated order. SUCCESS (200): { updated: true, order: [{ networkId, displayOrder }] } ERRORS: 400 (invalid array, missing networks, duplicate orders), 401 (unauthenticated), 429 (rate limited), 500 (batch write failure). SECURITY: Batch write scoped to authenticated user's documents only. UID from verified token.

---
Generated by VisualPRD