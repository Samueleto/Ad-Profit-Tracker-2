# Zeydoo Endpoint Stats By Country

GET /api/networks/zeydoo/stats/by-country

## Description

REQUEST: GET with query params: dateFrom (YYYY-MM-DD, required), dateTo (YYYY-MM-DD, required), limit? (integer 1–100, default 20). PROCESS: 1. Verify Firebase ID token. 2. Validate query parameters. 3. Query adStats collection for networkId=='zeydoo' within the date range for the authenticated user. 4. Group the results by country in memory, summing revenue and other metrics. 5. Sort countries by total revenue descending and slice to the specified limit. 6. Calculate each country's share of the total revenue. 7. Return the list of top countries. SECURITY: Query is always scoped to the authenticated user's data.

---
Generated by VisualPRD