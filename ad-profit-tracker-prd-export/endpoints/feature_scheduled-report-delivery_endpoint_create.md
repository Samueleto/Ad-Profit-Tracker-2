# Feature Scheduled Report Delivery Endpoint Create

POST /api/schedules/create

## Description

REQUEST: POST { reportId: string (required — must exist in users.savedReports), frequency: 'daily'|'weekly'|'monthly' (required), dayOfWeek?: number (0-6, required if frequency=='weekly'), dayOfMonth?: number (1-28, required if frequency=='monthly'), deliveryHour?: number (0-23, default 8), timezone?: string (IANA timezone string, default from users.preferences.timezone), deliveryEmail: string (required, valid email format), dateRangePreset: 'yesterday'|'last_7'|'last_30'|'last_90'|'this_month' (required), format: 'excel'|'pdf' (required), isActive?: boolean (default true) }. PROCESS: 1. Verify Firebase ID token via admin.auth().verifyIdToken(token) — return 401 if invalid. uid from verified token only. 2. Validate all fields: reportId non-empty string, frequency one of 3 values, dayOfWeek 0-6 if weekly, dayOfMonth 1-28 if monthly, deliveryHour 0-23, timezone valid IANA string (validate against Intl.supportedValuesOf('timeZone')), deliveryEmail valid RFC 5322 format, dateRangePreset one of 5 values, format one of 2 values. Return 400 with specific field errors if invalid. 3. Check rate limit: max 10 schedule creates/hour per user — return 429 if exceeded. 4. Read users Firestore document for uid via Admin SDK. Verify reportId exists in users.savedReports array — return 404 if not found. Check users.scheduledReports array length — enforce max 10 scheduled reports per user, return 400 with 'Maximum 10 scheduled reports allowed' if at limit. 5. Check for duplicate: if a schedule already exists for this reportId in users.scheduledReports, return 409 with 'A schedule already exists for this report — use PATCH to update it'. 6. Generate UUID v4 for schedule id. Denormalize reportName from the matching savedReport object. Compute nextRunAt: based on frequency, deliveryHour, timezone, and current server time — use date-fns-tz to compute the next occurrence in the user's timezone. 7. Build schedule object with all fields plus createdAt=serverTimestamp(), updatedAt=serverTimestamp(), lastRunAt=null, lastRunStatus=null, lastRunError=null. 8. Use Firestore Admin SDK arrayUnion to append the new schedule to users.scheduledReports. Set users.scheduledReportsUpdatedAt=serverTimestamp(). 9. Write audit log entry to auditLogs via Admin SDK: action='scheduled_report_created', resourceType='scheduledReport', resourceId=id, metadata={ reportName, frequency, deliveryEmail, nextRunAt, format }, status='success'. 10. Return the created schedule object. SUCCESS (201): { schedule: { id, reportId, reportName, frequency, dayOfWeek, dayOfMonth, deliveryHour, timezone, deliveryEmail, dateRangePreset, format, isActive, nextRunAt, createdAt } }. ERRORS: 400 (invalid fields, max schedules reached), 401 (unauthenticated), 404 (reportId not found in savedReports), 409 (schedule already exists for this report), 429 (rate limit exceeded). SECURITY: userId from verified token only. reportId ownership verified by reading users document scoped to uid. Audit logged.

---
Generated by VisualPRD