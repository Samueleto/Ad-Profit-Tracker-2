# Help Endpoint View Article

PATCH /api/help/articles/:articleId/view

## Description

REQUEST: PATCH /api/help/articles/:articleId/view — articleId in URL path. No request body. Bearer token in Authorization header. PROCESS: 1. Verify Firebase ID token via admin.auth().verifyIdToken(token) — return 401 if invalid. uid from verified token only. 2. Validate articleId: required, non-empty string — return 400 if missing. 3. Check rate limit: max 1 view increment per user per article per hour — prevents artificial inflation. Key: '{uid}_view_{articleId}'. If rate limit hit, return 200 with { incremented: false } silently (do not return 429 — this is a fire-and-forget call). 4. Use Firestore Admin SDK to increment helpArticles.viewCount by 1 using FieldValue.increment(1). 5. Return confirmation. SUCCESS (200): { incremented: boolean } ERRORS: 400 (missing articleId), 401 (unauthenticated), 500 (Firestore write failure). SECURITY: uid from verified token. Rate limited per user per article to prevent view count manipulation. Fire-and-forget — client does not depend on response.

---
Generated by VisualPRD