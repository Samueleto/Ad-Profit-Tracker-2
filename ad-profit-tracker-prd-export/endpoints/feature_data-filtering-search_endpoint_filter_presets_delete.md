# Feature Data Filtering Search Endpoint Filter Presets Delete

DELETE /api/filters/presets/:presetId

## Description

REQUEST: DELETE /api/filters/presets/:presetId — presetId in URL path. Bearer token in Authorization header.
PROCESS:
1. Verify Firebase ID token via admin.auth().verifyIdToken(token) — return 401 if invalid. uid from verified token only.
2. Validate presetId: required, non-empty string — return 400 if missing.
3. Check rate limit: max 20 preset deletes/hour per user — return 429 if exceeded.
4. Read users Firestore document for uid. Find the preset with matching id in users.savedFilterPresets array. Return 404 if not found.
5. Use Firestore Admin SDK arrayRemove to remove the matching preset object from users.savedFilterPresets. Set users.updatedAt=serverTimestamp().
6. Write audit log entry to auditLogs Firestore collection via Admin SDK: action='preferences_updated', resourceType='preferences', resourceId='filter_preset', metadata={ presetId, action: 'deleted' }, status='success'.
7. Return deletion confirmation.
SUCCESS (200): { deleted: true, presetId: string }
ERRORS:
- 400: missing presetId
- 401: invalid/expired Firebase ID token
- 404: preset not found in user's savedFilterPresets
- 429: rate limit exceeded (20/hour)
- 500: Firestore write failure
SECURITY: userId from verified token. Only deletes presets from the authenticated user's users document. Preset ownership verified by reading users document scoped to uid before deletion. Audit logged.

---
Generated by VisualPRD