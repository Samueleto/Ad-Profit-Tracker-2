# Step 294 of 437: Wire responsive layout into all protected pages

================================================================================

**Category:** Feature Wiring & Integration
**Flow:** feature_mobile-responsive-design

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

Now we need to make sure the ResponsiveAppLayout is properly wrapping every protected page and that all the data tables across the dashboard are using MobileDataTableWrapper.

**Layout integration** — The ResponsiveAppLayout should be set as the `layout.tsx` for the `(app)` route group. Confirm that FinancialMetricsDashboardPage, SettingsPage, and OnboardingWizardPage all sit inside this route group so they automatically inherit the responsive nav chrome. You shouldn't need to touch each page file — the layout wraps them all. Double-check that the existing desktop sidebar is still rendering correctly on `md+` and that the new mobile header bar only appears below `md`. The current route name shown in the mobile header bar should update as the user navigates between pages — use Next.js `usePathname()` to derive a human-readable title from the current path (e.g., `/dashboard` → 'Dashboard', `/settings` → 'Settings', `/reports` → 'Reports').

**Table wrapper integration** — Apply MobileDataTableWrapper to the tables inside these five locations: GeoBreakdownSection, PerNetworkAnalyticsTabsSection, ComparativeNetworkAnalysisTab, PerformanceBenchmarkingTab, and the SettingsPage activity log. For each table, define the `columns` config prop — decide which one or two columns are 'primary' (always visible on mobile) and which are 'secondary' (hidden behind the expand chevron). As a guide: for GeoBreakdownSection use country name + net profit as primary; for network tables use network name + the main performance metric as primary; for the SettingsPage activity log use timestamp + action as primary. For ComparativeNetworkAnalysisTab, use the horizontal scroll approach instead of card layout since column comparison is the whole point of that table.

**Bottom nav active state** — Wire up the active tab detection in MobileBottomNavBar using Next.js `usePathname()`. The Dashboard tab is active on `/dashboard` (but not on `/dashboard#networks`), Networks is active on `/dashboard#networks`, Settings on `/settings`, and Reports on `/reports`. Use `pathname === '/dashboard'` style checks rather than `startsWith` to avoid false positives.

**Theme toggle in drawer** — Make sure the existing dark/light theme toggle component from the Dark Mode & UI Theming feature is imported and rendered inside MobileNavigationDrawer near the bottom, above the sign-out button. It should work identically to wherever it currently lives on desktop.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure everything is wired together correctly across the whole app.

Confirm that:
- [ ] FinancialMetricsDashboardPage, SettingsPage, and OnboardingWizardPage all show the mobile header + bottom nav on small screens
- [ ] The desktop sidebar still renders correctly on md+ for all those pages
- [ ] The mobile header bar shows the correct page title for each route
- [ ] The page title updates when navigating between pages
- [ ] MobileBottomNavBar correctly highlights Dashboard on /dashboard, Settings on /settings, Reports on /reports
- [ ] The Networks tab highlights on /dashboard#networks but not plain /dashboard
- [ ] GeoBreakdownSection shows country name + net profit as primary columns on mobile
- [ ] PerNetworkAnalyticsTabsSection and PerformanceBenchmarkingTab show network name + primary metric on mobile
- [ ] ComparativeNetworkAnalysisTab uses horizontal scroll layout (not card layout) on mobile
- [ ] SettingsPage activity log shows timestamp + action as primary columns on mobile
- [ ] The dark/light theme toggle in the drawer works the same as on desktop
- [ ] MetricShareBar components inside wrapped tables reflow to full-width on mobile
- [ ] Color-coded row backgrounds are preserved in card layout

Test the actual flow: On a mobile viewport, navigate to /dashboard → see 'Dashboard' in the header, Dashboard tab highlighted in bottom nav → scroll down to GeoBreakdownSection → see card layout with country + net profit → tap a card's chevron → see all columns → navigate to /settings → see 'Settings' in header, Settings tab highlighted → find the activity log → see card layout → navigate back to /dashboard → tap the Networks tab → URL changes to /dashboard#networks → Networks tab highlights → find the ComparativeNetworkAnalysisTab → see a horizontally scrollable table with shadow indicators → scroll it sideways → widen to desktop → all tables switch to full table layout, desktop sidebar appears.

Then give me your honest advice on:
- Whether the route group structure correctly scopes the layout to only protected pages
- Any pages that might be accidentally excluded from the responsive layout
- Whether the horizontal scroll shadow indicators actually work across browsers
- Performance impact of applying MobileDataTableWrapper to five different sections simultaneously
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: