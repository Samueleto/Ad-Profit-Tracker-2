# Step 26 of 437: Set up folder structure for date range toolbar

================================================================================

**Category:** Project Scaffolding
**Flow:** feature_date-range-selection

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We're building a date range selection toolbar that lives inside the FinancialMetricsDashboardPage. For this step, set up the folder structure for the DateRangeToolbar feature.

Create a date range feature area under something like src/features/date-range/ or src/components/dashboard/date-range/ — wherever makes sense given the project's existing structure. Inside it, create subfolders or files for: the main DateRangeToolbar component itself, any sub-components we'll need (like the preset pill controls, the custom date picker panel, and the data availability indicator dot), a Zustand store file for shared dashboard date range state, and any utility helpers for date formatting and validation.

The DateRangeToolbar is not a standalone page — it renders as a sticky section at the top of FinancialMetricsDashboardPage, above the KPI cards. So there's no new route to create here. Just make sure the component folder is in a place that FinancialMetricsDashboardPage can easily import from.

Also create a types file to define the shape of the date range state (selected preset, from/to dates, data availability status, last synced timestamp) — we'll flesh this out in the schema_models step, but having the file in place now keeps things organized.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure the folder structure is set up correctly.

Confirm that:
- [ ] The date-range feature folder exists with subfolders or files for components, store, utils, and types
- [ ] DateRangeToolbar.tsx, PresetPills.tsx, CustomDatePicker.tsx, and DataAvailabilityDot.tsx placeholder files exist
- [ ] The Zustand store file and types file are in place
- [ ] The project still builds without errors after adding these files

Test the actual flow: Run a build — this is just scaffolding so nothing should break.

Then give me your honest advice on:
- Whether this folder structure fits well with the existing project layout
- If there's a better place to put shared dashboard state given how the rest of the app is organized
- Anything you'd reorganize before we start building
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: