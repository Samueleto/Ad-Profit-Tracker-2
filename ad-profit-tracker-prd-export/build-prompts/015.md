# Step 15 of 437: Set up folder structure for reconciliation feature

================================================================================

**Category:** Project Scaffolding
**Flow:** feature_data-reconciliation-validation

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We're building a data reconciliation and validation feature for an ad network dashboard. For this step, set up the folder structure so everything has a clear home before we start writing real code.

On the backend (Firebase Functions or Next.js API routes), create a folder for the reconciliation endpoints. We'll have six of them: POST /api/reconciliation/run, GET /api/reconciliation/status, GET /api/reconciliation/anomalies, PATCH /api/reconciliation/resolve, GET /api/reconciliation/rules, and PATCH /api/reconciliation/rules. Group these under a reconciliation API folder — you can split them into individual files (run.ts, status.ts, anomalies.ts, resolve.ts, rules.ts) or use a single router file, whichever fits the project's existing API structure.

Also create a shared utilities folder for reconciliation-specific helpers — we'll need things like the validation rule engine, anomaly flag builders, and rate limit checkers. Keep these separate from the route handlers so the logic is testable and reusable.

Create a types folder for the TypeScript interfaces we'll define in the schema_models step: ReconciliationResult, AnomalyFlag, ValidationRules, NetworkStatus, and ResolveRequest.

This feature touches two existing Firestore collections (adStats and networkConfigs) and writes to a third (auditLogs). No new top-level collections need to be created, but make sure there's a clear place in the codebase for the Firestore access patterns for each of these three collections.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure the folder structure is set up correctly.

Confirm that:
- [ ] There's an API folder with route files for all six reconciliation endpoints (run, status, anomalies, resolve, rules GET, rules PATCH)
- [ ] There's a shared utilities folder for validation engine, anomaly builder, and rate limiter helpers
- [ ] There's a types file for reconciliation-specific TypeScript interfaces
- [ ] The project still builds without errors after adding these folders and empty files

Test the actual flow: Run a build — this is scaffolding only, so it should compile cleanly with no errors.

Then give me your honest advice on:
- Whether this folder structure fits well with the existing project layout
- If there's a better way to organize the six endpoints (single router vs. individual files)
- Any naming conventions in the existing codebase we should match
- Anything you'd reorganize before we start building the actual logic
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: