# Step 379 of 437: Handle cache API errors and SWR failures gracefully

================================================================================

**Category:** Error Handling
**Flow:** feature_data-caching-optimization

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We need to make sure that failures in the caching layer degrade gracefully and never break the core user experience — a cache miss or a failed invalidation should never crash a page.

For the `useCacheStatus` hook: if `GET /api/cache/status` returns a 500 or the request times out, the hook should return an error state with a clear message like 'Cache stats temporarily unavailable' rather than throwing. The polling should back off — if a request fails, wait longer before the next attempt (simple exponential backoff or just doubling the interval once). Don't keep hammering a failing endpoint every 30 seconds.

For the `useCacheInvalidation` hook: map each possible error response to a user-friendly message. A 400 (invalid scope or keys) should say something like 'Invalid cache scope — please try again'. A 401 means the session expired — trigger a re-authentication flow rather than just showing an error. A 429 should clearly communicate the rate limit and ideally show how long until they can try again (parse the `Retry-After` header if the server sends one). A 500 should say 'Something went wrong on our end — your cache was not cleared'.

For the `useCachedNetworkData` hook: if the underlying fetcher fails, SWR will serve stale data. Make sure the hook exposes an `isStale` flag so the UI can show a subtle indicator when it's serving cached data after a failed revalidation. Don't show a full error screen — just a small badge or tooltip.

For the `useNetworkCacheConfig` hook: if reading the `networkConfigs` document fails (Firestore permission error, network offline), fall back to the hardcoded defaults silently. Log the error to the console in development but don't surface it to the user — the caching still works, just with defaults.

Finally, make sure that if `POST /api/cache/warm` fails for one task in its list, it continues processing the remaining tasks and reports `tasksFailed` accurately in the response rather than aborting the whole batch.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure errors in the caching layer never break the user experience.

Confirm that:
- [ ] If `GET /api/cache/status` returns 500, `useCacheStatus` returns an error state without throwing
- [ ] The status polling backs off after a failure (doesn't keep hitting a failing endpoint every 30 seconds)
- [ ] A 400 from `POST /api/cache/invalidate` shows a clear, non-technical error message
- [ ] A 401 from the invalidate endpoint triggers re-authentication, not just an error toast
- [ ] A 429 from the invalidate endpoint shows a rate limit message (with time remaining if available)
- [ ] A 500 from the invalidate endpoint clearly states the cache was NOT cleared
- [ ] `useCachedNetworkData` exposes an `isStale` flag when serving data after a failed revalidation
- [ ] `useNetworkCacheConfig` falls back to defaults silently when Firestore is unreachable
- [ ] `POST /api/cache/warm` processes all tasks even if some fail, and reports `tasksFailed` accurately

Test the actual flow: Simulate a 500 from the status endpoint → see the error state, confirm polling slows down. Trigger invalidation while rate-limited → see the friendly 429 message. Take the app offline → confirm `useNetworkCacheConfig` uses defaults and no error is shown to the user. Call `POST /api/cache/warm` with one valid task and one invalid task → confirm `tasksProcessed: 1, tasksFailed: 1` in the response.

Then give me your honest advice on:
- Whether the error messages are clear enough for a non-technical user
- Cases where a silent failure (like the config fallback) could mask a real problem
- How errors in the cache layer interact with errors in the underlying data-fetching layer
- What monitoring or alerting you'd want in place before going live
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: