# Step 28 of 437: Set up folder structure for API Explorer

================================================================================

**Category:** Project Scaffolding
**Flow:** feature_api-response-explorer

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We're building an API Response Explorer tab that lives inside the existing FinancialMetricsDashboardPage. For this step, set up the folder structure so everything has a clean home before we start building.

Create a feature folder at something like `src/features/api-explorer/` with subfolders for: `components` (we'll have accordion sections, a JSON viewer wrapper, a field mapping table, and a fetch button), `hooks` (for per-network fetch logic and clipboard handling), and `types` (for the shape of raw API responses and network config).

On the API side, create a folder under `src/app/api/networks/` — we'll be adding five route handlers here: one for each of the four ad networks' raw responses (`exoclick`, `rollerads`, `zeydoo`, `propush`) and one for the test-connection action.

This is a read-only reference tool, so the folder structure is relatively flat — no need for complex state or form logic folders. Keep it simple.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure the folder structure is in place before we build anything.

Confirm that:
- [ ] The `src/features/api-explorer/` folder exists with `components/`, `hooks/`, and `types/` subfolders
- [ ] The `src/app/api/networks/` folder exists with subfolders for `exoclick/`, `rollerads/`, `zeydoo/`, `propush/`, and `config/`
- [ ] The project still compiles without errors after adding these folders

Test the actual flow: Run `npm run build` or `npm run dev` — the app should start cleanly with no import or resolution errors introduced by the new folders.

Then give me your honest advice on:
- Whether this folder structure fits naturally with the rest of the project's conventions
- If there's a better place to co-locate the API route handlers given the existing project layout
- Anything you'd reorganize before we start adding real files
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: