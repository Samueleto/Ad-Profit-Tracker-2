# Step 313 of 437: Wire dashboard into app navigation and layout

================================================================================

**Category:** Feature Wiring & Integration
**Flow:** feature_financial-metrics-dashboard

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

Now we need to connect the FinancialMetricsDashboardPage to the rest of the app so users can actually reach it and the surrounding chrome (nav, layout) works correctly.

**Route protection** — The /dashboard route must be protected. Make sure the Next.js middleware (or a layout-level auth check) verifies that the user has an active Firebase session before rendering the page. If there's no session, redirect to / (the login/landing page). Don't just hide the UI — actually redirect at the routing level so unauthenticated users can't even load the page shell.

**Navigation** — Add a link to /dashboard in the app's main navigation (sidebar, top nav, or wherever the app's nav lives). Label it something like 'Dashboard' or 'Financial Overview'. Mark it as active when the current route is /dashboard.

**Layout** — Wrap FinancialMetricsDashboardPage in whatever shared layout the app uses (header, sidebar, etc.). Make sure the sticky date range selector at the top of the page doesn't conflict with a sticky app header — adjust z-index or positioning as needed.

**ManualRefreshPanel integration** — The ManualRefreshPanel is already built. Import it into FinancialMetricsDashboardPage and place it as a collapsible section at the bottom. The 'Sync Now' CTA in the empty state should scroll to this panel (or expand it if collapsed) rather than navigating away.

**Export modal wiring** — The Export button opens a modal. For now, wire it so the modal shows placeholder text like 'Excel and PDF export coming soon' with a link to the reporting section of the app (wherever that lives). This keeps the button functional without blocking on the reporting feature.

**POST /api/cache/invalidate wiring** — Make sure the 'Refresh Data' button in the page correctly calls POST /api/cache/invalidate with the Bearer token before re-fetching. Confirm this endpoint exists and is reachable — if it's not built yet, add a stub that returns 200 so the dashboard doesn't break.

**GET /api/scheduled/sync-status wiring** — The per-network summary rows reference sync status. The GET /api/dashboard/metrics response includes lastSyncedAt and lastSyncStatus per network in the perNetwork array, so no separate call is needed — just make sure the NetworkSummaryRow component reads from that data rather than calling GET /api/scheduled/sync-status independently.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure the dashboard is properly wired into the app.

Confirm that:
- [ ] Navigating to /dashboard while logged out redirects to / (not a blank page or error)
- [ ] Navigating to /dashboard while logged in loads the page correctly
- [ ] There's a working link to /dashboard in the app's main navigation
- [ ] The nav link shows as active when on /dashboard
- [ ] The sticky date range selector doesn't overlap or conflict with the app header
- [ ] The ManualRefreshPanel is embedded at the bottom of the page and collapsed by default
- [ ] The 'Sync Now' CTA in the empty state scrolls to or expands the ManualRefreshPanel
- [ ] The Export button opens a modal (even if it's a placeholder)
- [ ] The 'Refresh Data' button calls POST /api/cache/invalidate before re-fetching
- [ ] Per-network sync status comes from the GET /api/dashboard/metrics perNetwork array (not a separate API call)

Test the actual flow: Sign out → try to navigate to /dashboard directly → redirected to /. Sign in → navigate to /dashboard via the nav link → page loads with data. Scroll to the bottom → ManualRefreshPanel is there and collapsed → click to expand → it opens. Trigger the empty state (use a date range with no data) → see the 'Sync Now' CTA → click it → page scrolls to or expands the ManualRefreshPanel. Click 'Refresh Data' → check network tab to confirm POST /api/cache/invalidate fires before the GET /api/dashboard/metrics re-fetch.

Then give me your honest advice on:
- Whether the route protection is solid (middleware vs. client-side guard tradeoffs)
- Any navigation dead ends or back-button weirdness
- Whether the ManualRefreshPanel integration feels natural in the page flow
- What a first-time user would find confusing about the dashboard layout
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: