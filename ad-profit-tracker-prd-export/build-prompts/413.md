# Step 413 of 437: Test responsive components and mobile interactions

================================================================================

**Category:** Testing
**Flow:** feature_mobile-responsive-design

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

Let's add tests to make sure the mobile responsive system works reliably and doesn't regress.

**Unit tests for useMediaQuery hook** — Test that it returns `false` during SSR (mock `window` as undefined). Test that it returns the correct boolean when `window.matchMedia` matches or doesn't match. Test that it updates reactively when the media query result changes (simulate a `change` event on the matchMedia object). Test that `useIsMobile()` returns true at 375px and false at 1024px.

**Component tests for MobileNavigationDrawer** — Render it with a mock Firebase Auth `currentUser` and confirm the user's display name and avatar appear. Render it with `currentUser` as null and confirm it doesn't crash. Test that tapping the backdrop calls the close callback. Test that tapping a nav link calls the close callback and navigates. Test that the sign-out button calls `Firebase Auth signOut()`. Test the fallback avatar renders when `displayName` and `photoURL` are null.

**Component tests for MobileBottomNavBar** — Test that the correct tab is highlighted based on the current pathname. Test that the Networks badge dot appears when Zustand health is 'degraded'. Test that the badge dot is absent when health is 'healthy'. Test that tapping a tab calls `router.push()` with the correct path.

**Component tests for MobileDataTableWrapper** — Test that on mobile viewport it renders card layout. Test that on desktop viewport it renders table layout. Test that tapping a card's chevron expands it to show secondary columns. Test that a missing `columns` prop falls back to showing all columns as primary. Test that zero rows renders nothing.

**Integration test — full mobile nav flow** — Simulate a mobile viewport (375px). Render the ResponsiveAppLayout with a mock authenticated user. Confirm the hamburger button is visible and the desktop sidebar is hidden. Tap the hamburger → drawer opens → body scroll is locked. Tap a nav link → drawer closes → navigation happens → body scroll is restored. Tap the sign-out button → `signOut()` is called.

**Snapshot tests** — Snapshot the MobileBottomNavBar in its default state and with the badge dot visible, so visual regressions are caught.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure all the tests pass and cover the important paths.

Confirm that:
- [ ] useMediaQuery unit tests pass — SSR default, match/no-match, reactive updates
- [ ] MobileNavigationDrawer tests pass — user info renders, null user doesn't crash, backdrop closes drawer, sign-out calls signOut()
- [ ] Fallback avatar test passes for users with no displayName or photoURL
- [ ] MobileBottomNavBar tests pass — active tab highlighting, badge dot on degraded health, router.push() called on tap
- [ ] MobileDataTableWrapper tests pass — card layout on mobile, table on desktop, expand/collapse, missing prop fallback, zero rows
- [ ] Integration test passes — hamburger opens drawer, body scroll locks, nav link closes drawer and navigates, scroll restores
- [ ] Snapshot tests pass and are committed
- [ ] All tests pass in CI without flakiness

Test the actual flow: Run the full test suite (`npm test` or `yarn test`). Then manually test on a real iOS device: open the app → tap the hamburger → try scrolling the background → it shouldn't scroll → tap a nav link → navigate correctly → check the bottom nav safe area on an iPhone with a notch → confirm the bar doesn't overlap the home indicator → open a dashboard page with a data table → see card layout → expand a card → rotate to landscape → see table layout.

Then give me your honest advice on:
- What's not covered by these tests that could break in production (especially iOS Safari quirks)
- Whether the body scroll lock test actually verifies the iOS Safari behavior or just the desktop behavior
- Any flaky test risks — timing issues with transitions, matchMedia mocks, or Firebase Auth async behavior
- What you'd want to manually test on real devices before shipping this to users
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: