# Step 408 of 437: Test preferences feature end-to-end

================================================================================

**Category:** Testing
**Flow:** feature_user-settings-preferences

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We're building a Next.js + Firebase app. For this step, I need you to add tests for the User Settings & Preferences feature covering the API endpoints, the usePreferences hook, and the PreferencesCard component.

**Unit tests for GET /api/settings/preferences:**
- Returns 200 with preferences object for a valid token and existing document
- Returns default values when the users document has no preferences field (first-time user)
- Returns 401 when no Authorization header is present
- Returns 401 when the token is invalid or expired
- Returns 500 when Firestore throws

**Unit tests for PATCH /api/settings/preferences:**
- Returns 200 and updates only the provided fields (not others)
- Rejects an invalid timezone string with 400 and a field-specific message
- Rejects an invalid currency code with 400
- Rejects an invalid defaultDateRange value with 400
- Rejects a non-boolean notification value with 400
- Rejects an extra unexpected field — confirm it's not written to Firestore
- Returns 401 for missing/invalid token
- Uses dot-notation field paths so only specified fields are updated (not a full overwrite)
- Sets preferences.updatedAt to a server timestamp

**Unit tests for usePreferences hook:**
- Initializes with props values correctly
- Falls back to defaults when props are missing fields
- Calls PATCH with only the changed field when `updatePreference` is called
- Debounces PATCH calls from the timezone combobox
- Sets saveStatus to 'saving' during `saveAll`, then 'success' on completion
- Handles a 401 response by attempting token refresh
- Handles a 500 response by setting saveStatus to 'error'
- Doesn't drop a pending update if one is already in flight

**Component tests for PreferencesCard:**
- Renders all four controls with correct initial values from props
- Shows shimmer skeleton while loading
- Shows 'Defaults applied' label when initialized with defaults
- Disables all controls and shows spinner on Save button while saveStatus is 'saving'
- Shows 'Preferences saved' toast when saveStatus is 'success'
- Shows inline error banner with Retry button on 500
- Shows field-level error under the correct field on 400

**Integration / E2E test:**
- Sign in → navigate to /settings → see preferences card with saved values → change timezone → confirm PATCH fires with correct field → change a notification toggle → click 'Save Preferences' → see success toast → refresh page → confirm all changes persisted.

Mock Firebase Admin SDK in API tests using jest mocks. Mock `firebase/auth` in hook and component tests. Use React Testing Library for component tests.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure all the preferences tests pass and cover the important paths.

Confirm that:
- [ ] All GET /api/settings/preferences unit tests pass (200, defaults, 401, 500)
- [ ] All PATCH /api/settings/preferences unit tests pass (200, 400 for each invalid field type, 401, field whitelist enforcement)
- [ ] The PATCH test confirms unexpected fields are NOT written to Firestore
- [ ] All usePreferences hook tests pass (initialization, auto-save, debounce, saveAll, error states, in-flight queuing)
- [ ] All PreferencesCard component tests pass (rendering, loading state, disabled state, toasts, error banners)
- [ ] The E2E test completes the full flow: sign in → change preferences → save → refresh → verify persistence
- [ ] No tests are flaky — run the suite 3 times and confirm consistent results

Test the actual flow: Run `jest --coverage` and check that the preferences-related files have good coverage (aim for 80%+ on the hook and API handlers). Then run the E2E test against a real Firebase emulator: sign in → navigate to /settings → change timezone to 'Europe/London' → see PATCH fire → change weekly report toggle → click Save → see toast → hard refresh → confirm 'Europe/London' and the toggle state are still set.

Then give me your honest advice on:
- What important scenarios aren't covered by these tests that could break in production
- Whether the E2E test is reliable against the Firebase emulator or has timing issues
- Any test that's testing implementation details rather than behavior (and should be rewritten)
- What you'd add before feeling confident about a release
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: