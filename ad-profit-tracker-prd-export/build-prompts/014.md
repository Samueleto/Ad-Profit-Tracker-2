# Step 14 of 437: Set up folder structure for error handling

================================================================================

**Category:** Project Scaffolding
**Flow:** feature_error-handling-retry-logic

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We're building an error handling and retry logic system for a network sync dashboard. This feature tracks retry state, circuit breakers, and error history across four ad networks (exoclick, rollerads, zeydoo, propush).

For this step, set up the folder structure that all the subsequent work will live in.

On the backend (API routes), create a folder at `src/app/api/errors/` with subfolders for `circuit-breaker/` (which will hold the status and reset routes) and the flat route files for `retry-state`, `log`, `summary`, and `retry-config`.

Create a shared utilities folder at `src/lib/errors/` — this is where we'll put helpers for things like backoff schedule computation, error code classification, and rate limit checking that multiple API routes will share.

Also create a constants file somewhere sensible (like `src/lib/errors/constants.ts`) that exports the list of supported network IDs: `['exoclick', 'rollerads', 'zeydoo', 'propush']`. Every API route will import from here so the list stays in one place.

The project uses Next.js with Firebase as the backend, so all API routes will be Next.js Route Handlers (the `app/api/` convention with `route.ts` files).

Don't build any logic yet — just get the folders and placeholder files in place so the project still compiles cleanly.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure the folder structure is set up correctly.

Confirm that:
- [ ] `src/app/api/errors/` exists with route files for `retry-state`, `log`, `summary`, and `retry-config`
- [ ] `src/app/api/errors/circuit-breaker/` exists with `status` and `reset` subfolders, each with a `route.ts`
- [ ] `src/lib/errors/` exists with `constants.ts`, `backoff.ts`, `classify.ts`, and `rateLimit.ts`
- [ ] `constants.ts` exports the four network IDs as a typed constant
- [ ] The project builds without errors after adding these files

Test the actual flow: Run `npm run build` or `npm run dev` — the app should start without any import or compilation errors. This is scaffolding only, so no functionality to test yet.

Then give me your honest advice on:
- Whether this folder structure fits well with the existing project layout
- If there's a better place to put shared error utilities given how the rest of the project is organized
- Anything you'd reorganize before we start building the actual logic
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: