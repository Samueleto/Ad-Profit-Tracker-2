# Step 21 of 437: Set up folder structure for profit trend chart

================================================================================

**Category:** Project Scaffolding
**Flow:** feature_daily-profit-trend-visualization

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We're building a daily profit trend visualization feature that integrates into the existing financial dashboard. For this step, set up the folder structure for the new chart section.

Create a feature area under the dashboard or charts part of the project (wherever the existing FinancialMetricsDashboardPage lives) with subfolders for: components (we'll build several reusable chart pieces here — the area chart itself, the metric toggle, the date range selector, the pattern insights strip, and data point markers), hooks (for fetching and transforming the ROI data), utils (for the 7-day moving average calculation, color-code mapping, and pattern insight derivation logic), and types (TypeScript interfaces for the chart data shapes).

This section integrates into the existing FinancialMetricsDashboardPage as a new card below the KPI row — so don't create a new route or page. The goal is a self-contained feature folder that the parent dashboard can import cleanly.

Make sure the project still builds without errors after adding these folders.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure the folder structure is set up correctly.

Confirm that:
- [ ] There's a daily-profit-trend feature folder inside the dashboard components area
- [ ] It has subfolders for components, hooks, utils, and types
- [ ] The project still builds without errors after adding these folders
- [ ] The existing FinancialMetricsDashboardPage is untouched and still renders correctly

Test the actual flow: Run a build and open the dashboard — everything should look exactly as it did before this step.

Then give me your honest advice on:
- Whether this folder structure fits naturally with how the rest of the project is organized
- If there's a better place to put this given the existing file layout
- Anything you'd reorganize before we start building the actual components
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: