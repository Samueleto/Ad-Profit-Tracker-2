# Step 31 of 437: Set up folder structure for Excel export

================================================================================

**Category:** Project Scaffolding
**Flow:** feature_excel-export-functionality

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We're building an Excel export feature for a financial metrics dashboard. For this step, I need you to set up the folder structure so everything has a clean home before we start writing real code.

On the frontend side, create a feature folder for the export functionality. Inside it, you'll want a place for the ExportModal component and any smaller sub-components it uses (like the sheet checkbox rows and the preview badge). Since this modal integrates into the existing FinancialMetricsDashboardPage, we don't need a new page — just a components folder under something like `src/features/export/` or `src/components/export/`, whichever fits the project's existing conventions.

On the backend side, create a folder for the export API routes. We'll have two endpoints: `POST /api/export/excel` and `GET /api/export/preview`. In Next.js, this means creating the route files under `src/app/api/export/excel/` and `src/app/api/export/preview/` (or the `pages/api/export/` equivalent if the project uses the Pages Router). Also create a shared utilities folder for the export logic — things like the ExcelJS workbook builder and the row-count computation will live here so the route handlers stay lean.

Finally, add a placeholder for a rate-limiting utility and a cache utility — we'll flesh those out in sdk_setup, but having the files stubbed out now keeps the imports clean.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure the folder structure is set up correctly.

Confirm that:
- [ ] There's a frontend folder for the ExportModal component and its sub-components
- [ ] There are route files (or stubs) at the correct paths for POST /api/export/excel and GET /api/export/preview
- [ ] There's a shared lib folder for the workbook builder and row-count utilities
- [ ] There's a placeholder for the cache utility
- [ ] The project still builds without errors after adding these folders and stub files

Test the actual flow: Run `npm run build` or `npm run dev` — the project should start without any import or module errors from the new stubs.

Then give me your honest advice on:
- Whether this folder structure fits the existing project conventions
- If there's a better place to put the export utilities given how the rest of the codebase is organized
- Anything you'd reorganize before we start building the actual feature
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: