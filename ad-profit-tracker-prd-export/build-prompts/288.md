# Step 288 of 437: Wire preferences into SettingsPage server fetch

================================================================================

**Category:** Feature Wiring & Integration
**Flow:** feature_user-settings-preferences

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We're building a Next.js + Firebase app. For this step, I need you to wire the preferences feature into the existing SettingsPage so the Server Component correctly fetches and passes preferences data, and so the PreferencesCard is properly integrated into the page layout.

In the SettingsPage Server Component at app/settings/page.tsx, update the data fetching to use Promise.all to fetch both /api/keys/status and /api/settings/preferences simultaneously. To make the authenticated server-side fetch work, you'll need to read the Firebase session cookie (or however the existing SettingsPage authenticates server-side requests) and pass the Bearer token in the Authorization header of both fetch calls.

If GET /api/settings/preferences returns a 404 (first-time user with no preferences document yet), treat this as a non-error — just pass the default preferences object as props to PreferencesCard. The defaults are: timezone 'UTC', currency 'USD', defaultDateRange 'last_7_days', notifications both false.

Import and render PreferencesCard below the existing API key card(s) in the page layout. Pass the fetched preferences (or defaults) as the `initialPreferences` prop. Make sure the visual ordering is: existing API key section first, then the General Preferences card below it, with consistent spacing and card styling.

Also wire up the success toast — when `usePreferences` reports saveStatus 'success', trigger a toast notification saying 'Preferences saved'. Use whatever toast library is already in the project. If there isn't one, add a simple fixed-position toast component.

Finally, make sure the /settings route is still protected — unauthenticated users should be redirected before the page even attempts to fetch preferences. Check how the existing SettingsPage handles auth protection and make sure the preferences fetch is inside that same guard.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure the preferences feature is properly wired into the SettingsPage.

Confirm that:
- [ ] The SettingsPage Server Component fetches /api/keys/status and /api/settings/preferences in parallel (check the network waterfall — they should start at the same time)
- [ ] The PreferencesCard renders below the existing API key cards with consistent styling
- [ ] A first-time user (no preferences document) sees the card with defaults, not an error
- [ ] The server-side fetch for preferences includes a valid Authorization header
- [ ] An unauthenticated user visiting /settings is redirected before any fetch happens
- [ ] The 'Preferences saved' toast appears after a successful save
- [ ] The toast disappears after a few seconds without user action

Test the actual flow: Sign in → navigate to /settings → open the network tab → confirm both /api/keys/status and /api/settings/preferences requests start simultaneously → see the preferences card rendered below the API key section → change a preference → see the PATCH fire → see the success toast → sign out → try to navigate to /settings directly → confirm you're redirected away.

Then give me your honest advice on:
- Whether the parallel fetch is actually saving meaningful time or if one is much faster than the other
- How the server-side auth token is obtained for the preferences fetch — is it secure and reliable?
- Edge cases around the first-time user flow — what if the users document doesn't exist at all?
- Anything that would break if the existing SettingsPage auth approach changes
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: