# Step 429 of 437: Test reconciliation feature end-to-end

================================================================================

**Category:** Testing
**Flow:** feature_data-reconciliation-validation

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We need tests covering the reconciliation feature's hooks, components, and critical user flows.

Unit tests for the hooks:
- **useReconciliationStatus**: mock GET /api/reconciliation/status — verify it returns data on success, sets error on failure, and refetch triggers a new call.
- **useRunReconciliation**: mock POST /api/reconciliation/run — verify isRunning is true during the call, the result summary is populated on success, and a status refetch is triggered after completion. Test that 429 responses set a specific rateLimitError state.
- **useAnomalies**: mock GET /api/reconciliation/anomalies — verify initial load populates the list, loadMore() appends (not replaces), and changing a filter resets the cursor and list. Test that a failed loadMore() doesn't clear existing records.
- **useResolveAnomalies**: mock PATCH /api/reconciliation/resolve — verify optimistic removal happens immediately, and that records are restored if the request fails. Test the skipped count handling.
- **useValidationRules**: mock both GET and PATCH /api/reconciliation/rules — verify rules load correctly, usingDefaults is populated, and a partial update only sends changed fields.

Component tests:
- **ReconciliationOverviewPanel**: renders network cards with correct status values, shows loading skeletons, shows error state with retry, and the 'Run All' button calls useRunReconciliation with no networkId.
- **AnomalyListView**: renders anomaly rows with correct flag badges, multi-select works up to 50 items, the resolve form appears on selection, and filter changes reset the list.
- **ValidationRulesEditor**: shows 'using default' labels for default fields, inline editing updates local state, saving calls the PATCH hook, and a 400 error highlights the correct field.
- **AnomalyFlagBadge**: renders 'warning' flags in yellow and 'critical' flags in red.

Integration / end-to-end tests (using a test Firebase project or emulator):
1. Full reconciliation flow: authenticate → navigate to /reconciliation → run reconciliation for 'exoclick' for yesterday → see summary results → switch to Anomalies tab → see anomalies listed → select two → resolve as 'false_positive' → see them disappear → check anomaly count decremented on the Overview tab.
2. Rules update flow: navigate to Rules tab → expand 'rollerads' → change maxDailyCostUSD → save → reload the page → confirm the new value persists and 'using default' label is gone for that field.
3. Rate limit flow: trigger POST /api/reconciliation/run 11 times → confirm the 11th shows the rate limit message.
4. Auth expiry flow: let the Firebase token expire (or mock a 401) → confirm redirect to login → sign back in → confirm return to /reconciliation.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure all tests pass and the critical paths are covered.

Confirm that:
- [ ] All hook unit tests pass (useReconciliationStatus, useRunReconciliation, useAnomalies, useResolveAnomalies, useValidationRules)
- [ ] All component tests pass (ReconciliationOverviewPanel, AnomalyListView, ValidationRules
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: