# Step 415 of 437: Test sync endpoints and client hooks

================================================================================

**Category:** Testing
**Flow:** feature_automated-daily-data-sync

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

Let's add tests to make sure the sync system works correctly and doesn't break silently.

Unit tests for the server-side sync logic:
- Test the `x-internal-secret` validation in POST /api/scheduled/daily-sync: missing header → 401, wrong value → 401, correct value → proceeds.
- Test the same for GET /api/scheduled/health.
- Test the Firebase ID token verification in GET /api/scheduled/sync-status, GET /api/scheduled/sync-history, and POST /api/scheduled/retry-failed: missing token → 401, invalid token → 401, valid token → proceeds.
- Test the networkId validation in GET /api/scheduled/sync-history: valid values ('exoclick', 'rollerads', 'zeydoo', 'propush') pass, anything else returns 400.
- Test the limit validation in GET /api/scheduled/sync-history: values 1–50 pass, 0 and 51 return 400.
- Test the retry rate limit logic: 5 calls succeed, 6th returns 429.
- Test the exponential backoff helper: verify it waits 1s, 2s, 4s between attempts and stops after `retryAttempts`.
- Test that the daily sync fan-out collects results from all tasks even when some throw — mock one network to throw and verify the others still resolve.

Unit tests for the client-side hooks:
- Test `useSyncStatus`: mock the fetch, verify it returns the networks array, verify `refresh()` triggers a new fetch, verify polling fires after 30 seconds.
- Test `useSyncHistory`: verify `loadMore()` appends results, verify the `networkId` filter is passed as a query param.
- Test `useRetrySync`: verify it POSTs with the correct networkId, verify it calls `refresh()` on success, verify it surfaces the right error message for 429, 404, and 502 responses.

Integration tests (end-to-end flows):
- Simulate a full daily sync: mock all four external network APIs to return valid response data → call POST /api/scheduled/daily-sync with the correct secret → verify adStats documents were batch-upserted → verify networkConfigs documents were updated with `lastSyncStatus='success'` → verify auditLogs entries were written for each network.
- Simulate a partial failure: mock exoclick to fail and the other three to succeed → verify the response shows `networksFailed: 1, networksTriggered: 4` → verify only exoclick's networkConfigs shows 'failed'.
- Simulate a retry: set up a networkConfigs document with `lastSyncStatus='failed'` → call POST /api/scheduled/retry-failed → mock the external API to succeed → verify networkConfigs updates to 'success' and a new audit log entry is written.
- Test the health endpoint: mock Firestore to succeed → verify 200 with all checks 'ok'. Mock Firestore to fail → verify 503 with `firestore: 'error'`.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure all tests pass and cover the important paths.

Confirm that:
- [ ] All unit tests pass for the internal secret validation (daily-sync and health endpoints)
- [ ] All unit tests pass for Firebase token verification (sync-status, sync-history, retry-failed)
- [ ] networkId and limit validation tests pass for sync-history
- [ ] Rate limit test passes (5 succeed, 6th returns 429)
- [ ] Exponential backoff test verifies the correct wait times
- [ ] Fan-out test confirms partial failures don't block other tasks
- [ ] `useSyncStatus` hook tests pass including polling behavior
- [ ] `useSyncHistory` hook tests pass including `loadMore()` append behavior
- [ ] `useRetrySync` hook tests pass including error message mapping for 429, 404, 502
- [ ] Integration test for a full successful sync passes (all four networks)
- [ ] Integration test for a partial failure passes (correct counts in response)
- [ ] Integration test for retry flow passes
- [ ] Health endpoint integration tests pass for both healthy and degraded states

Test the actual flow: Run the full test suite with `jest` and confirm all tests pass with no skips. Then manually call POST /api/scheduled/daily-sync with the correct secret in a staging environment → verify the response summary matches what's written to Firestore → check the auditLogs collection for entries → check networkConfigs for updated statuses. Then trigger a retry for a failed network → verify the status flips to 'success' in the UI without a page refresh.

Then give me your honest advice on:
- What's not covered by these tests that could silently break in production (e.g., Firestore batch write limits, API response shape changes from external networks)
- Whether the integration tests are too tightly coupled to Firestore internals and might become brittle
- Any flaky test risks (timing-dependent polling tests, race conditions in the fan-out test)
- What you'd add to feel confident deploying this to production with real user data
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: