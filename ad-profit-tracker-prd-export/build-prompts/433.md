# Step 433 of 437: Test the scheduled report delivery feature

================================================================================

**Category:** Testing
**Flow:** feature_scheduled-report-delivery

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

Let's add tests to make sure the scheduled report delivery feature works correctly and doesn't break.

**Unit tests for useScheduleForm hook:**
- Initializes with correct defaults (Daily, user email, Last 7 days, Excel, active=true, hour=8)
- fetchSchedule() with a matching reportId populates form fields from the response
- fetchSchedule() with no matching schedule keeps defaults
- saveSchedule() calls POST /api/schedules/create when no existing schedule is loaded
- saveSchedule() calls PATCH /api/schedules/:id when an existing schedule is loaded
- deleteSchedule() calls DELETE /api/schedules/:id with the correct id
- sendTestNow() calls POST /api/schedules/:id/send-now with no request body
- A 401 response triggers getIdToken(true) and retries once
- A persistent 401 calls the onSessionExpired callback
- A 400 response with field errors populates the error state correctly
- A 409 response triggers a re-fetch of the existing schedule
- updateField() updates the correct field and clears its error

**Unit tests for computeNextDelivery utility:**
- Daily at 8am in America/New_York: returns tomorrow at 8:00 AM EST
- Weekly on Monday at 8am: returns the next Monday
- Monthly on the 15th: returns the 15th of this month (or next month if today is past the 15th)
- Edge case: monthly on the 28th in February
- Timezone handling: same hour in different timezones produces different UTC times

**Component tests for ScheduleReportModal:**
- Renders shimmer rows while loading
- Renders the form with defaults when no schedule exists
- Renders the form pre-populated when a schedule exists
- Selecting Weekly shows the day-of-week picker
- Selecting Monthly shows the day-of-month picker
- The 'Delete Schedule' button only renders when an existing schedule is loaded
- The 'Send Test Now' button only renders when an existing schedule is loaded
- Clicking 'Delete Schedule' shows a confirmation dialog
- Confirming delete calls deleteSchedule()
- Canceling delete does not call deleteSchedule()
- Shows inline field errors when the error state has field errors
- Shows the error banner with Retry when there's a general error
- Shows 'Access Denied' content when error is 403
- Shows 'Report not found' content when error is 404
- Closes when Escape is pressed
- Closes when the backdrop is clicked

**Integration test (end-to-end flow):**
- Open the modal for a report with no schedule → form shows defaults → fill in all fields → click Save → POST /api/schedules/create called with correct payload → 'Schedule saved' toast → modal closes → saved report row shows schedule indicator
- Open the modal for the same report → form pre-populated → change frequency → click Save → PATCH /api

================================================================================

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: