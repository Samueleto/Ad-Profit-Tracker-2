# Step 42 of 437: Confirm Firebase Auth and react-hook-form are ready

================================================================================

**Category:** SDK Setup & Configuration
**Flow:** feature_user-onboarding-flow

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We're building the onboarding wizard on top of Firebase Auth and react-hook-form. For this step, I need you to make sure both are properly installed and accessible to the onboarding feature.

First, check that firebase/auth is already installed and that there's a shared Firebase client config somewhere in the project (typically src/lib/firebase.ts or similar). If it doesn't exist yet, create it — initialize the Firebase app using environment variables (NEXT_PUBLIC_FIREBASE_API_KEY, NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN, NEXT_PUBLIC_FIREBASE_PROJECT_ID, etc.) and export a getAuth() instance. This is what the onboarding page will use to get the current user's displayName and ID token.

Next, check that react-hook-form is installed. If not, add it. We'll use it on Step 2 of the wizard (the Profile Setup step) to manage the displayName field and the two dropdown selectors (timezone and defaultDateRange). No complex validation library is needed — react-hook-form's built-in validation is enough here.

On the server side, confirm that firebase-admin is installed and that there's a shared Admin SDK initializer (typically src/lib/firebase-admin.ts). This is used by the PATCH /api/auth/update-profile endpoint to verify ID tokens and write to Firestore. If the initializer doesn't exist, create it — initialize the Admin app using a service account (FIREBASE_SERVICE_ACCOUNT_KEY env var as a JSON string, or individual credential env vars) and export getFirestore() and getAuth() from the Admin SDK.

Make sure all required environment variables are documented in .env.example so nothing is missing when this runs in a new environment.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure both SDKs are properly set up and accessible.

Confirm that:
- [ ] firebase and firebase/auth are installed
- [ ] A Firebase client config file exists and exports a working auth instance
- [ ] react-hook-form is installed
- [ ] firebase-admin is installed
- [ ] A Firebase Admin initializer exists and exports Firestore and Auth admin instances
- [ ] All required environment variables are listed in .env.example
- [ ] The project builds without errors

Test the actual flow: Import the Firebase client auth instance in a test component and call getAuth().currentUser — it should return null (no user yet) without throwing. Import the Admin SDK initializer in an API route and call getFirestore() — it should return a Firestore instance without errors. Check that no Firebase credentials are hardcoded anywhere in source files.

Then give me your honest advice on:
- Whether the Firebase client and Admin SDK are initialized correctly (avoiding duplicate app initialization)
- Any environment variable gaps that would cause silent failures
- Whether the Admin SDK credential approach is secure for the deployment environment
- Anything that could cause SDK initialization to fail in production
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: