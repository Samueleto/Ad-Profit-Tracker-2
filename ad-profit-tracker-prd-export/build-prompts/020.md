# Step 20 of 437: Set up folder structure for ROI feature

================================================================================

**Category:** Project Scaffolding
**Flow:** feature_roi-calculation-engine

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We're building an ROI Calculation Engine that integrates into an existing financial dashboard. For this step, set up the folder structure so everything has a home before we start writing real code.

On the frontend (Next.js), create a feature area under something like `src/features/roi/` with subfolders for: `components` (we'll build ROIIndicatorSection and its sub-components here), `hooks` (for data fetching and threshold state), and `types` (TypeScript interfaces for the ROI payload shapes).

On the API side, create the route folder `src/app/api/roi/` with three sub-route folders inside it: `compute/`, `thresholds/`, and `breakdown/`. Each will get a `route.ts` file later. These map to the four endpoints we're building: `GET /api/roi/compute`, `GET /api/roi/thresholds`, `PATCH /api/roi/thresholds`, and `GET /api/roi/breakdown`.

Also create a shared server utility folder at `src/lib/roi/` — we'll put the ROI formula logic, cache helpers, and threshold merging logic here so all three route handlers can share them without duplication.

The ROIIndicatorSection page component lives at `/dashboard` and integrates into the existing FinancialMetricsDashboardPage, so no new page route is needed — just the component files.

Make sure the project still builds cleanly after adding these empty folders and placeholder files.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure the folder structure is set up correctly.

Confirm that:
- [ ] `src/features/roi/` exists with `components/`, `hooks/`, and `types/` subfolders
- [ ] `src/app/api/roi/compute/route.ts` exists (even if empty)
- [ ] `src/app/api/roi/thresholds/route.ts` exists (even if empty)
- [ ] `src/app/api/roi/breakdown/route.ts` exists (even if empty)
- [ ] `src/lib/roi/` exists for shared server utilities
- [ ] The project builds without errors after adding these files

Test the actual flow: Run `npm run build` (or `next build`) and confirm no errors. This is scaffolding only — nothing functional yet.

Then give me your honest advice on:
- Whether this folder structure fits cleanly with the existing project layout
- If there's a better place to put shared ROI formula utilities given how the project is already organized
- Anything you'd restructure before we start filling in the real code
- Whether the three-route split for `/api/roi/` makes sense or if anything should be consolidated
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: