# Step 2 of 437: Set up onboarding feature folder structure

================================================================================

**Category:** Project Scaffolding
**Flow:** feature_user-onboarding-flow

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We're building a one-time onboarding wizard for Ad Profit Tracker. For this step, I need you to set up the folder structure for the onboarding feature.

Create an onboarding feature area under src/features/onboarding/ (or the equivalent features directory your project uses). Inside it, create subfolders for: components (we'll build step-specific sub-components like WelcomeStep, ProfileSetupStep, ApiKeysStep, and DoneStep), hooks (for onboarding-specific logic like useOnboardingState), and types (for the TypeScript interfaces we'll need).

On the page side, create the route file for /onboarding — in Next.js this means adding a page at app/onboarding/page.tsx (or pages/onboarding.tsx if using the pages router). Also create a layout file at app/onboarding/layout.tsx that renders a minimal shell — no sidebar, no main nav frame. Onboarding should feel distraction-free, so this layout just centers content on the page with a clean background.

Finally, make sure there's a placeholder middleware file (or add to the existing one at middleware.ts in the project root) where we'll later add the onboarding redirect logic. Don't implement the logic yet — just make sure the file exists and is wired into Next.js.

The goal here is just clean scaffolding so every subsequent step has a clear home.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure the onboarding scaffolding is in place.

Confirm that:
- [ ] The onboarding feature folder exists with components/, hooks/, and types/ subfolders
- [ ] The /onboarding route exists as a Next.js page file
- [ ] A layout file exists for /onboarding that renders without a sidebar or main nav
- [ ] A middleware.ts file exists at the project root (or is already present)
- [ ] The project builds without errors after adding these files

Test the actual flow: Run the dev server and navigate to /onboarding — you should get a blank or placeholder page with no errors in the console. The layout should not show any sidebar or navigation chrome.

Then give me your honest advice on:
- Whether this folder structure fits well with the rest of the project's conventions
- If the Next.js router version (app vs pages) is being used consistently
- Any scaffolding gaps before we start building on top of this
- Anything you'd reorganize now before it gets harder to change
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: