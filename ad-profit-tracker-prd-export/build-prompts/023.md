# Step 23 of 437: Set up folder structure for network analytics tabs

================================================================================

**Category:** Project Scaffolding
**Flow:** feature_per-network-analytics-tabs

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We're building a per-network analytics feature that integrates into the existing FinancialMetricsDashboardPage. For this step, set up the folder structure so everything has a home before we start building.

Create a feature area under something like `src/features/network-analytics/` (or wherever the project keeps feature-level code). Inside it, create subfolders for:
- `components/` — this is where the tab bar, individual network panels, KPI rows, the API Explorer accordion, and the Network Insights strip will live
- `hooks/` — for the lazy-loading data fetching logic per tab
- `types/` — for TypeScript interfaces describing the network stats payload and related shapes
- `utils/` — for helpers like the country name lookup, metric formatting, and metricShare calculations

Also create a folder for the new API route: `src/app/api/networks/stats/` (Next.js App Router style). We'll put the GET handler there.

If the project uses a shared components directory (for things like MetricToggle, MetricShareBar, TopNSelector, DrawerModal that are already built in GeoBreakdownSection), make sure you know where those live — we'll be importing them directly rather than duplicating them.

This feature touches two integrated pages — PerNetworkAnalyticsTabsSection and ApiExplorerTab — both of which render as sections inside the existing FinancialMetricsDashboardPage, not as standalone routes. Keep that in mind as you organize: there are no new page files, just new components that get dropped into the dashboard.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure the folder structure is in place before we build anything.

Confirm that:
- [ ] The `network-analytics` feature folder exists with `components/`, `hooks/`, `types/`, and `utils/` subfolders
- [ ] The `src/app/api/networks/stats/` folder exists for the API route
- [ ] The project still compiles without errors after adding these folders
- [ ] You've identified where the shared components (MetricToggle, MetricShareBar, TopNSelector, DrawerModal) currently live so we can import them later

Test the actual flow: Run a build or dev server start — nothing should be broken since we've only added empty folders.

Then give me your honest advice on:
- Whether this folder structure fits the project's existing conventions
- If there's a better place to put the API route given how other routes are organized
- Any naming conflicts with existing files or folders
- Whether the shared component imports will be straightforward or if there's any refactoring needed first
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: