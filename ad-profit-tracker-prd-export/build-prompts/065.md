# Step 65 of 437: Install recharts and node-cache packages

================================================================================

**Category:** SDK Setup & Configuration
**Flow:** feature_performance-benchmarking

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We need two packages set up before building the benchmarking feature:

1. **recharts** — This is used in PerformanceBenchmarkingTab to render the sparkline trend strip showing the user's selected metric over the last 30 days vs their historical average line. Install `recharts` and its TypeScript types (`@types/recharts` if needed). Since this is a Next.js app, recharts components need to render client-side only — make sure any file that imports recharts has `'use client'` at the top, or wrap the chart component in a dynamic import with `ssr: false`. Test that a basic `<LineChart>` renders without errors.

2. **node-cache** — This is used server-side in the `GET /api/benchmarks/performance` route to cache benchmark computation results for 5 minutes per user. Install `node-cache`. Create a shared cache instance at `lib/benchmarks/cache.ts` that exports a single `node-cache` instance configured with a default TTL of 300 seconds (5 minutes) and `checkperiod` of 60 seconds. Export it as a singleton so all API routes share the same cache instance across requests in the same Node.js process.

Also confirm that `firebase-admin` is already installed and initialized in the project (it should be from earlier features). If there's no shared Firebase Admin initialization file yet, create one at `lib/firebase-admin.ts` that initializes the Admin SDK once using `getApps().length === 0` guard and exports `db` (Firestore) and `auth` (Auth). All three benchmark API routes will import from this shared file.

Don't add any API routes yet — just get the packages installed and the shared utilities in place.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure both packages are installed and the shared utilities work.

Confirm that:
- [ ] `recharts` is in package.json and a basic `<LineChart>` renders in a client component without errors
- [ ] `node-cache` is in package.json
- [ ] `lib/benchmarks/cache.ts` exports a singleton node-cache instance with 5-minute TTL
- [ ] `lib/firebase-admin.ts` exports `db` and `auth` without re-initializing on every import
- [ ] The project still builds and runs without errors

Test the actual flow: Create a quick test component that imports `LineChart` from recharts and renders it — confirm no SSR errors. Import the cache singleton in two different files and verify it's the same instance (same object reference).

Then give me your honest advice on:
- Whether the node-cache singleton pattern works correctly in Next.js (serverless vs long-running process concerns)
- Any SSR issues with recharts we should handle upfront
- Whether the Firebase Admin singleton is set up correctly for Next.js API routes
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: