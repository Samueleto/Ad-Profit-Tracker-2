# Step 163 of 437: Build the POST /api/sync/manual endpoint

================================================================================

**Category:** API Endpoints
**Flow:** feature_manual-data-refresh-trigger

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We need to build the `POST /api/sync/manual` endpoint and the `syncNetworkForUser` helper it depends on. This is the core of the feature — everything else in the UI just calls this.

**Start with `lib/sync/syncNetworkForUser.ts`** — This is a shared server-side helper (not an API route). It takes `(uid: string, networkId: NetworkId, dateFrom: string, dateTo: string)` and does the heavy lifting: decrypts the API key using `cryptr`, calls the appropriate ad network API, retries with exponential backoff (1s, 2s, 4s — up to `retryAttempts` from the network's `networkConfigs` doc, defaulting to 3), parses the response rows, batch-upserts into `adStats` in chunks of 500 using the Admin SDK, updates the `networkConfigs` document with `lastSyncedAt`, `lastSyncStatus`, and `lastSyncError`, and writes an audit log entry to `auditLogs`. Each network has a different API shape: ExoClick uses its Global Statistics endpoint with `groupByCountry`; RollerAds uses `GET /sites/stat` with `group_by=country` via axios; Zeydoo uses its CPA statistics endpoint with country grouping via axios; Propush uses `/v5/pub-statistics` with geo grouping via axios. The helper should return `{ rowsFetched: number, latencyMs: number }` on success or throw with a descriptive error message on failure.

**Then build the API route at `POST /api/sync/manual`** — Walk through these steps in order:
1. Extract the `Authorization: Bearer <token>` header and call `admin.auth().verifyIdToken(token)` — return 401 immediately if it fails or is missing.
2. Parse and validate the request body against `ManualSyncRequest`. If `networkId` is provided, check it's one of the four valid `NetworkId` values — return 400 if not. If `dateFrom`/`dateTo` are provided, validate they're valid `YYYY-MM-DD` strings, that `dateFrom <= dateTo`, and that the range is 90 days or fewer — return 400 if any check fails. Default both to yesterday if omitted.
3. Check rate limits: if `networkId` is provided, allow max 10 manual syncs/hour for that user; if omitted (sync-all), allow max 3/hour. Return 429 with a `Retry-After` header (seconds until reset) if exceeded. Reuse the project's existing rate-limit utility if one exists.
4. Determine target networks: if `networkId` is given, use `[networkId]`; otherwise query `networkConfigs` for all docs where `userId == uid AND isActive == true`.
5. For each target network, check `apiKeys` for a doc where `userId == uid AND networkId == network`. If none found, add to `skipped` array and move on.
6. For networks with valid keys, call `syncNetworkForUser(uid, networkId, dateFrom, dateTo)` — fan out across all targets in parallel using `Promise.allSettled()` so one failure doesn't block others.
7. Collect results into `triggered`, `skipped`, and `failed` arrays plus the `results` array matching `ManualSyncResponse`.
8. Return 200 with the full `ManualSyncResponse` object.

Never log or return decrypted API keys. Always derive `uid` from the verified token, never from the request body.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure the endpoint and helper work correctly end-to-end.

Confirm that:
- [ ] `POST /api/sync/manual` without an auth token returns 401
- [ ] `POST /api/sync/manual` with an expired token returns 401
- [ ] Sending `networkId: 'invalid'` returns 400
- [ ] Sending a date range over 90 days returns 400
- [ ] Sending `dateFrom` after `dateTo` returns 400
- [ ] A valid request with no `networkId` queries `networkConfigs` and triggers all active networks
- [ ] A valid request with `networkId: 'exoclick'` only syncs ExoClick
- [ ] Networks without an entry in `apiKeys` appear in the `skipped` array
- [ ] One network failing doesn't prevent others from syncing (Promise.allSettled)
- [ ] After a successful sync, `networkConfigs` shows updated `lastSyncedAt` and `lastSyncStatus: 'success'`
- [ ] An audit log entry appears in `auditLogs` with `action: 'manual_sync_triggered'` and `triggeredBy: 'user'`
- [ ] Exceeding the rate limit returns 429 with a `Retry-After` header
- [ ] The decrypted API key never appears in logs or the response

Test the actual flow: Using a valid Firebase ID token, call `POST /api/sync/manual` with no body → see all active networks triggered → check Firestore for updated `lastSyncedAt` on each `networkConfigs` doc → check `auditLogs` for new entries → check `adStats` for upserted rows. Then call it again rapidly until you hit the rate limit → see 429 with `Retry-After`.

Then give me your honest advice on:
- Whether `Promise.allSettled` with parallel network calls could cause Firestore write conflicts
- How the exponential backoff interacts with the rate limits (a slow retry could look like a new request)
- Whether chunking `adStats` writes at 500 is the right batch size for Firestore
- What happens if the endpoint times out mid-sync (Next.js has a default function timeout)
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: