# Step 100 of 437: Verify Firestore indexes for comparison queries

================================================================================

**Category:** Database Setup
**Flow:** feature_comparative-network-analysis

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

The GET /api/networks/comparison endpoint does a single Firestore query on the adStats collection — filtering by userId == uid AND date >= dateFrom AND date <= dateTo, covering all four networks in one read. This is intentionally designed to be cost-efficient. For this step, make sure the Firestore indexes exist to support this query efficiently.

Check the project's firestore.indexes.json (or the Firebase console) for an existing composite index on the adStats collection that covers: userId (ascending) + date (ascending). This index is almost certainly already in place since the existing /api/networks/stats endpoint does similar per-network queries. If it exists, great — no changes needed. If it doesn't exist, add it to firestore.indexes.json and deploy it.

Also check the networkConfigs collection — the comparison endpoint does a second getDocs query on networkConfigs filtered by userId == uid to pull lastSyncedAt, lastSyncStatus, and circuitBreakerOpen per network. Confirm there's an index (or that the collection is small enough that no composite index is needed — typically a single equality filter on userId doesn't require a composite index in Firestore).

No schema changes are needed — this feature reads from the existing adStats and networkConfigs collections without writing anything new. The comparison endpoint is purely read-only. Just confirm the indexes are in place and the collections have the fields the endpoint expects: adStats needs networkId, userId, date, cost, revenue, impressions, clicks fields; networkConfigs needs networkId, userId, lastSyncedAt, lastSyncStatus, circuitBreakerOpen.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure the Firestore setup supports the comparison query.

Confirm that:
- [ ] There's a composite index on adStats for userId + date (ascending)
- [ ] The adStats documents have the fields: networkId, userId, date, cost, revenue, impressions, clicks
- [ ] The networkConfigs documents have: networkId, userId, lastSyncedAt, lastSyncStatus, circuitBreakerOpen
- [ ] A test query filtering adStats by userId AND date range returns results without a 'missing index' error in the Firebase console
- [ ] No new indexes needed for the networkConfigs userId equality filter

Test the actual flow: In the Firebase console or using the Admin SDK in a test script, run a query on adStats filtered by a known userId and a date range — confirm it returns documents for multiple networks and doesn't throw a missing-index error. Check the Firestore console for any index-building warnings.

Then give me your honest advice on:
- Whether the single cross-network query approach is actually more cost-efficient than the per-network queries the existing endpoints use
- Any Firestore read limits or quota concerns given the 90-day max date range
- Fields that might be missing or named differently in the actual documents vs. what the endpoint expects
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: