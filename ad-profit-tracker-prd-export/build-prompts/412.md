# Step 412 of 437: Test the date range toolbar and store

================================================================================

**Category:** Testing
**Flow:** feature_date-range-selection

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

Let's add tests to make sure the DateRangeToolbar and its Zustand store work correctly and don't break under edge cases.

**Unit tests for the Zustand store (dashboardDateRangeStore):**
- `setPreset('last7')` produces the correct startDate and endDate in YYYY-MM-DD format
- `setPreset('last30')` produces the correct dates
- `setPreset('thisMonth')` sets startDate to the first day of the current month
- `setCustomRange` stores the provided dates and sets preset to 'custom'
- The store initializes with 'last30' and valid dates
- `setDataAvailability` and `setLastSynced` update the correct fields

**Unit tests for date validation logic:**
- A range of exactly 90 days passes validation
- A range of 91 days fails validation
- A 'To' date before 'From' date fails validation
- Both validation errors produce the correct distinct error messages

**Component tests for DateRangeToolbar:**
- All six preset pills render
- Clicking a preset (not Custom) highlights it and does not show the date picker
- Clicking 'Custom' reveals the react-day-picker inputs inline
- Selecting a valid custom range enables the Apply button
- Selecting an invalid range (>90 days) shows 'Maximum range is 90 days' and disables Apply
- The data availability dot shows a spinner while GET /api/stats/dates is loading
- The dot shows green/amber/gray based on mocked API responses
- A mocked 403 response shows 'Access Denied' inline
- A mocked 500 response shows the amber warning icon
- On mobile viewport, presets render as a dropdown select

**Integration test (full flow):**
- Load FinancialMetricsDashboardPage → toolbar renders with 'Last 30 Days' active → click 'Last 7 Days' → store updates → dashboard sections re-fetch → click 'Custom' → pick a valid range → Apply → store updates → sections re-fetch → pick an invalid range → Apply is disabled → switch tabs → come back → date range is preserved.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure all the tests pass and cover the important paths.

Confirm that:
- [ ] All Zustand store unit tests pass — preset calculations, custom range, initialization
- [ ] All date validation unit tests pass — 90-day boundary, reversed dates, correct error messages
- [ ] All DateRangeToolbar component tests pass — presets, custom picker, Apply button states, dot states, error states
- [ ] The mobile dropdown test passes
- [ ] The integration test passes end-to-end
- [ ] No tests are making real network calls — GET /api/stats/dates is mocked (use msw or jest.mock)

Test the actual flow: Run the full test suite with `npm test`. Then manually test on a real browser: open FinancialMetricsDashboardPage → click through all six presets → confirm each one updates the dashboard → use the custom picker → test the 90-day boundary (89 days valid, 91 days invalid) → test on a mobile viewport → confirm the dropdown and bottom sheet work.

Then give me your honest advice on:
- What's not covered by these tests that could break in production (e.g., timezone edge cases with date-fns, react-day-picker interaction quirks)
- Whether the integration test is too tightly coupled to implementation details
- Any flaky test risks (timing with debounce, async state updates)
- Tests you'd want to add before feeling confident about a release
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: