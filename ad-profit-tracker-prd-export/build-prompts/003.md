# Step 3 of 437: Set up folder structure for API key management

================================================================================

**Category:** Project Scaffolding
**Flow:** feature_api-key-management-storage

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We're building an API key management feature that lets users securely connect their ad network accounts (ExoClick, RollerAds, Zeydoo, and Propush). For this step, I need you to set up the folder structure so everything has a home before we start building.

On the frontend side, create a settings feature area. We'll need a folder for the SettingsPage itself, a components folder for the reusable card widgets we'll build (one per ad network), and a hooks folder for any data-fetching logic.

On the backend side, create a folder structure for the three API route handlers we'll need: POST /api/keys/save, GET /api/keys/status, and DELETE /api/keys/delete. Since this is a Next.js + Firebase project, these will live under the app's API routes directory. Create a `keys` folder under `src/app/api/` (or `pages/api/` depending on your router setup) with separate files for `save`, `status`, and `delete`.

Also create a `lib` folder (if one doesn't already exist) at the project root level — we'll use this for shared server-side utilities like the Firebase Admin SDK initialization and the encryption helper.

Keep the encryption logic completely separate from the route handlers. It should be a standalone utility that the route handlers import. This makes it easier to swap out the encryption library later if needed.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure the folder structure is set up correctly.

Confirm that:
- [ ] There's an `api/keys/` folder with separate route files for save, status, and delete
- [ ] There's a settings feature folder with components/ and hooks/ subfolders
- [ ] There's a `lib/` folder at the project root for shared utilities
- [ ] The project still builds without errors after adding these empty folders and files

Test the actual flow: Run a build (`npm run build` or `next build`) and make sure nothing is broken — this is just scaffolding.

Then give me your honest advice on:
- Whether this folder structure fits well with the existing project layout
- If there's a better place for the API routes given how the project is currently organized
- Any naming conventions in the existing codebase we should follow
- Anything you'd reorganize before we start building the actual feature
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: