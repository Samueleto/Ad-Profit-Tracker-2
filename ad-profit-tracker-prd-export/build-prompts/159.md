# Step 159 of 437: Build all six RollerAds API endpoints

================================================================================

**Category:** API Endpoints
**Flow:** feature_rollerads-integration

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We're building six API endpoints for the RollerAds integration. They share a lot of patterns — auth verification, date validation, Firestore reads/writes — so build them in dependency order and reuse helpers wherever possible.

**Shared pattern for all user-facing endpoints:** Start every handler by calling `admin.auth().verifyIdToken()` with the Bearer token from the Authorization header. Return 401 immediately if it fails or is missing. The `uid` always comes from the verified token — never from the request body or query params.

**Date validation helper:** Create a shared `validateDateRange(dateFrom, dateTo, maxDays = 90)` utility that checks both are valid YYYY-MM-DD strings, that `dateFrom <= dateTo`, and that the range doesn't exceed 90 days. Return a descriptive 400 error message if any check fails. All five user-facing endpoints use this.

---

**POST /api/networks/rollerads/sync** — The manual sync endpoint. After auth and date validation (defaulting both to yesterday if not provided), check the rate limit: query `auditLogs` for this user's manual syncs in the last hour — if 10 or more exist, return 429. Fetch the `networkConfigs` document for `userId + networkId='rollerads'`; if `isActive=false`, return 400. Fetch the `apiKeys` document for this user and network; if not found, return 404. Decrypt the key using `decryptApiKey`. Build a RollerAds axios client using `createRollerAdsClient` with the config's `endpointOverride` and `timeoutSeconds`. Record the start time, then call `GET /sites/stat` with `{ date_from, date_to, group_by: 'country' }`. Parse `response.data` rows as `RollerAdsStatRow[]`. For each row, build an `AdStatDocument` and call `sanitizeRawResponse` on the raw row before storing it. Upsert documents into `adStats` using Firestore batch writes in chunks of 500, using the deterministic document ID format. Update `networkConfigs` with `lastSyncedAt`, `lastSyncStatus='success'`, `lastSyncError=null`. Write an audit log entry to `auditLogs` with `action='manual_sync_triggered'`, `latencyMs`, `rowsFetched`, `dateFrom`, `dateTo`, `status='success'`. On RollerAds API error, update `networkConfigs` with `lastSyncStatus='failed'` and write a failure audit log before returning 502.

**GET /api/networks/rollerads/stats** — After auth and date validation, query `adStats` where `userId==uid AND networkId=='rollerads' AND date>=dateFrom AND date<=dateTo`, ordered by date ASC. If `groupBy='total'`, aggregate all rows into a single totals object (sum revenue, impressions, clicks; average ctr and cpm). If `groupBy='day'` (default), group by date and sum across countries per day. Return the structured response with `cost` omitted (RollerAds is revenue-only).

**GET /api/networks/rollerads/stats/by-country** — After auth and date validation, query `adStats` with the same filters plus `country != null`. Group results in memory by country, summing revenue, impressions, clicks. Sort by total revenue descending, slice to `limit` (default 20, max 100). Compute `revenueShare` as `(countryRevenue / totalRevenue) * 100`. Enrich country codes with full names using a static ISO 3166-1 alpha-2 lookup map (include at minimum the top 50 countries by web traffic — you can hardcode this map in `src/lib/countryNames.ts`).

**GET /api/networks/rollerads/stats/latest** — After auth, query `adStats` for `userId==uid AND networkId=='rollerads'`, ordered by `date DESC`, limit 1. If no documents, return 404. Use the latest date to fetch all documents for that day. Aggregate across countries. Also fetch the `networkConfigs` document to include `lastSyncedAt` and `lastSyncStatus` in the response.

**GET /api/networks/rollerads/raw-response** — After auth, validate the `date` query param. Query `adStats` for `userId==uid AND networkId=='rollerads' AND date==date`. If no documents, return 404. Map each document to `{ country, rawResponse }`. Return alongside the static `ROLLERADS_FIELD_SCHEMA` array from `fieldSchema.ts`.

**POST /api/networks/rollerads/scheduled-sync** — This endpoint is NOT protected by Firebase auth. Instead, check that the `x-internal-secret` request header exactly matches the `INTERNAL_SYNC_SECRET` environment variable — return 401 immediately if it doesn't match or is missing. Query `networkConfigs` for all documents where `networkId=='rollerads' AND isActive==true AND syncSchedule=='daily'` using the Admin SDK. For each matching user, fetch and decrypt their API key (skip users without a key — add to skipped count). Call the RollerAds API for yesterday's date, parse rows, batch-upsert into `adStats`, update `networkConfigs`, and write an audit log entry with `scheduledSync: true` in metadata. Return a summary `{ processed, succeeded, failed, skipped, date }`.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure all six endpoints work correctly.

Confirm that:
- [ ] POST /api/networks/rollerads/sync returns 401 without a valid Firebase ID token
- [ ] POST /api/networks/rollerads/sync returns 400 if `isActive=false` in networkConfigs
- [ ] POST /api/networks/rollerads/sync returns 404 if no API key is saved for this user
- [ ] POST /api/networks/rollerads/sync returns 429 after 10 calls within an hour
- [ ] POST /api/networks/rollerads/sync returns 400 if dateFrom > dateTo or range exceeds 90 days
- [ ] A successful sync upserts documents into adStats with the correct deterministic document IDs
- [ ] A successful sync updates networkConfigs with lastSyncedAt and lastSyncStatus='success'
- [ ] A successful sync writes an audit log entry to auditLogs
- [ ] rawResponse stored in adStats does NOT contain any key/token/secret/authorization fields
- [ ] GET /api/networks/rollerads/stats returns data grouped by day by default
- [ ] GET /api/networks/rollerads/stats returns a single totals object when groupBy=total
- [ ] GET /api/networks/rollerads/stats/by-country returns countries sorted by revenue with revenueShare percentages
- [ ] GET /api/networks/rollerads/stats/latest returns 404 when no data has been synced
- [ ] GET /api/networks/rollerads/raw-response returns the fieldSchema array alongside records
- [ ] POST /api/networks/rollerads/scheduled-sync returns 401 without the correct x-internal-secret header
- [ ] POST /api/networks/rollerads/scheduled-sync processes all active daily-schedule users and returns a summary
- [ ] All user-facing endpoints return 401 if the Firebase token is expired or tampered with

Test the actual flow: Using a valid Firebase ID token, call POST /api/networks/rollerads/sync with a date range of the last 7 days → confirm rows appear in Firestore adStats → call GET /api/networks/rollerads/stats with the same date range → confirm revenue/impressions/clicks are correct → call GET /api/networks/rollerads/stats/by-country → confirm countries are sorted by revenue with percentages summing to ~100% → call GET /api/networks/rollerads/stats/latest → confirm it returns the most recent day → call GET /api/networks/rollerads/raw-response with that date → confirm records and fieldSchema are returned → call POST /api/networks/rollerads/scheduled-sync with the correct x-internal-secret → confirm the summary shows processed/succeeded counts.

Then give me your honest advice on:
- Whether the decrypted API key could leak through error messages, logs, or stack traces
- How the batch-write chunking handles partial failures (what if chunk 2 of 3 fails?)
- Race conditions in the scheduled-sync when processing many users concurrently
- What would break first if the RollerAds API changes its response format
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: