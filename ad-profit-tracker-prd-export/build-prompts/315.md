# Step 315 of 437: Wire benchmarking tab into the dashboard

================================================================================

**Category:** Feature Wiring & Integration
**Flow:** feature_performance-benchmarking

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

Now we need to connect the PerformanceBenchmarkingTab and BenchmarkSettingsModal into the existing FinancialMetricsDashboardPage.

**Tab Registration** — Find where the existing tabs (including the Compare tab) are defined in FinancialMetricsDashboardPage. Add the 'Benchmarks' tab immediately after the Compare tab. The tab panel content should be the PerformanceBenchmarkingTab component. Make sure the tab system supports lazy rendering — PerformanceBenchmarkingTab should only mount when the Benchmarks tab is active for the first time, and stay mounted after that (don't unmount and re-fetch every time the user switches away and back).

**Date Range Sync** — FinancialMetricsDashboardPage already has a shared date range state. Pass dateFrom and dateTo down to PerformanceBenchmarkingTab as props. The tab should not have its own date selector — it reads entirely from the parent. When the parent date range changes, the tab's `useBenchmarkingData` hook should automatically re-fetch.

**MetricToggle and MetricShareBar Reuse** — PerformanceBenchmarkingTab reuses MetricToggle and MetricShareBar from ComparativeNetworkAnalysisTab. Make sure these components are imported from their shared location (not duplicated). If they're currently co-located inside ComparativeNetworkAnalysisTab's folder, move them to a shared components location so both tabs can import them cleanly.

**BenchmarkSettingsModal Wiring** — The modal is triggered from inside PerformanceBenchmarkingTab. Manage the modal's open/closed state inside PerformanceBenchmarkingTab (a simple boolean useState is fine). The 'Benchmark Settings' button sets it to open; the modal's Cancel and successful Save both set it to closed. After a successful save, the tab should re-fetch GET /api/benchmarks/performance so the updated targets are reflected immediately in the comparison table — don't require a page reload.

**Performance Score Integration** — The Performance Score gauge at the top of the tab needs data from GET /api/benchmarks/score. Wire this into `useBenchmarkingData` so it fetches alongside the other data on tab activation. The score is also stored in the benchmarks Firestore collection as a cached value (performanceScore, performanceScoreComputedAt) — if the API returns a staleness indicator, surface it near the gauge.

**ManualRefreshPanel Link** — The empty state CTA ('Sync Now') should link to or trigger the ManualRefreshPanel. Find where ManualRefreshPanel is accessible in the existing dashboard and wire the CTA to navigate there or open it — whichever pattern the rest of the dashboard uses for this.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure everything is properly wired together in the dashboard.

Confirm that:
- [ ] The 'Benchmarks' tab appears in FinancialMetricsDashboardPage after the Compare tab
- [ ] Clicking the Benchmarks tab for the first time triggers data fetching
- [ ] Switching away from the tab and back does NOT re-fetch (data is preserved)
- [ ] Changing the dashboard's date range while on the Benchmarks tab triggers a re-fetch
- [ ] Changing the date range while on a different tab and then switching to Benchmarks shows fresh data
- [ ] MetricToggle and MetricShareBar render correctly inside PerformanceBenchmarkingTab (no import errors)
- [ ] The 'Benchmark Settings' button opens BenchmarkSettingsModal
- [ ] Clicking Cancel in the modal closes it without changing the tab's data
- [ ] Saving settings in the modal closes it AND re-fetches GET /api/benchmarks/performance
- [ ] The Performance Score gauge shows data from GET /api/benchmarks/score
- [ ] The 'Sync Now' CTA in the empty state navigates to or opens ManualRefreshPanel
- [ ] No console errors about missing props or broken imports

Test the actual flow: Open FinancialMetricsDashboardPage → confirm Benchmarks tab is visible after Compare → click it → watch data load → switch to another tab → switch back → confirm no re-fetch → change the date range → switch to Benchmarks → confirm data updated → click 'Benchmark Settings' → change a target → save → confirm the comparison table reflects the new target without a page reload → change the date range to one with no data → confirm the empty state with Sync Now CTA appears → click Sync Now → confirm it reaches ManualRefreshPanel.

Then give me your honest advice on:
- Whether the 'stay mounted after first activation' approach could cause stale data issues if the user leaves the dashboard and comes back
- Whether passing dateFrom/dateTo as props is the right pattern or if a context/store would be cleaner given how many tabs share this state
- Any prop-drilling issues introduced by wiring the modal's save callback back up to trigger a re-fetch
- What would break if the Compare tab's MetricToggle or MetricShareBar components are refactored in the future
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: