# Step 194 of 437: Build reusable API key card components

================================================================================

**Category:** UI Components
**Flow:** feature_api-key-management-storage

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We need a reusable card component that gets used four times on the SettingsPage — once for each ad network (ExoClick, RollerAds, Zeydoo, and Propush). Building it as a shared component means we only need to get the logic right once.

**`ApiKeyCard` component** — This is the main reusable piece. It takes props for: `networkId` (one of the four network IDs), `networkName` (display name like 'ExoClick'), `status` ('connected' | 'not_connected'), `updatedAt` (string | null), and callbacks for `onSave` and `onDisconnect`.

The card has two visual states based on `status`:
- **Not connected**: Shows a password-style input field for the API key (using `react-hook-form` for form handling and validation), a show/hide toggle button on the input, and a 'Save' button. The input should have placeholder text like 'Enter your ExoClick API key'.
- **Connected**: Shows a 'Connected' badge (green), the last updated date, an 'Update Key' button that reveals the input form again, and a 'Disconnect' button.

The form should validate that the key field isn't empty before submitting. Use `react-hook-form`'s `register`, `handleSubmit`, and `formState.errors` for this. When the form is in a submitting state (passed as a prop), show a spinner on the Save button and disable the input field.

The 'Disconnect' button should show a small confirmation step inline (something like 'Are you sure?' with a Confirm and Cancel button) rather than immediately disconnecting — this prevents accidental disconnections.

**`ApiKeyCardSkeleton` component** — A loading placeholder that matches the dimensions of `ApiKeyCard`. Use CSS animation or a library like `react-loading-skeleton` to show a shimmer effect. This gets shown on the SettingsPage while the status data is loading.

Both components should live in `src/features/settings/components/`. They should be Client Components (add `'use client'` at the top) since they handle form state and user interactions.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure both components work correctly in all their states.

Confirm that:
- [ ] `ApiKeyCard` with `status='not_connected'` shows the input field and Save button
- [ ] `ApiKeyCard` with `status='connected'` shows the Connected badge, last updated date, Update Key button, and Disconnect button
- [ ] The show/hide toggle on the password input actually toggles the input type between 'password' and 'text'
- [ ] Submitting the form with an empty key shows a validation error (not a network request)
- [ ] When `isSubmitting` is true, the Save button shows a spinner and the input is disabled
- [ ] Clicking Disconnect shows an inline confirmation step, not an immediate disconnect
- [ ] Clicking 'Cancel' on the confirmation dismisses it without disconnecting
- [ ] `ApiKeyCardSkeleton` renders with a shimmer/loading animation
- [ ] Both components are marked as Client Components

Test the actual flow: Render `ApiKeyCard` in a Storybook story or a test page. Toggle between connected and not-connected states. Submit the empty form — see the validation error. Fill in a key and submit — see the spinner. Click Disconnect — see the confirmation. Confirm — see the `onDisconnect` callback fire. Render `ApiKeyCardSkeleton` and confirm it looks like a loading placeholder that matches the card dimensions.

Then give me your honest advice on:
- Whether the UX for the connected/disconnected states is clear and intuitive
- Any accessibility issues (keyboard navigation, screen reader labels on the show/hide toggle)
- Whether `react-hook-form` is being used efficiently here or if there's a simpler approach
- Edge cases (very long API keys, special characters in keys, rapid clicking of Save)
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: