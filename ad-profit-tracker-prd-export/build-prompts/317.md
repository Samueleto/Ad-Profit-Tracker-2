# Step 317 of 437: Wire API Explorer into the dashboard tab system

================================================================================

**Category:** Feature Wiring & Integration
**Flow:** feature_api-response-explorer

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

Now we need to connect the ApiExplorerTab into the existing FinancialMetricsDashboardPage tab system at /dashboard.

First, find where the existing dashboard tabs are defined and add 'API Explorer' as a new tab entry. Make sure it renders the ApiExplorerTab component when selected. The tab should only be visible to authenticated users — it inherits auth from the parent dashboard page, so no extra auth logic is needed here, but double-check that the parent page's auth guard covers this tab.

Wire up the network configuration check: when the ApiExplorerTab mounts, it should check whether each network has an API key configured. This means reading from the apiKeys and networkConfigs data (however the rest of the app accesses those — match the existing pattern). Call setConfigured in the Zustand store for each network based on this check. This determines whether each accordion shows the JSON viewer or the 'API key not configured' message.

Wire up the accordion expand behavior: when a user expands an accordion for the first time and isConfigured is true for that network, trigger loadCachedResponse for that network from the Zustand store.

Wire up the 'Fetch Fresh Sample' button to call fetchFreshSample from the store, passing the correct network identifier.

Wire up the 'Copy JSON' button using the browser's navigator.clipboard.writeText API. Pass the currently displayed JSON (stringified with pretty-printing) to it. Show a brief 'Copied!' confirmation on the button after a successful copy.

Make sure the link in the 'API key not configured' message navigates to /settings using Next.js's Link component (not a plain anchor tag).

Finally, confirm that the /api/networks/exoclick/raw-response, /api/networks/rollerads/raw-response, /api/networks/zeydoo/raw-response, /api/networks/propush/raw-response, and /api/networks/config/test-connection endpoints are all being called correctly from the store actions — check the request format matches what those endpoints expect.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure everything is properly connected inside the dashboard.

Confirm that:
- [ ] The 'API Explorer' tab appears in the FinancialMetricsDashboardPage tab bar
- [ ] Clicking the tab renders the ApiExplorerTab component
- [ ] On mount, the tab checks API key configuration for all four networks and updates the Zustand store
- [ ] Networks without API keys show the 'not configured' message; networks with keys show the JSON viewer
- [ ] The 'API key not configured' link navigates to /settings using Next.js routing (no full page reload)
- [ ] Expanding an accordion for the first time triggers loadCachedResponse
- [ ] The 'Fetch Fresh Sample' button calls fetchFreshSample with the correct network identifier
- [ ] The 'Copy JSON' button copies prettified JSON and shows a 'Copied!' confirmation
- [ ] All five endpoint URLs are being called correctly from the store
- [ ] An unauthenticated user cannot access this tab (redirected by the parent page's auth guard)

Test the actual flow: Log in → go to /dashboard → click 'API Explorer' tab → expand ExoClick (configured) → see cached response load → expand Zeydoo (not configured) → see settings link → click it → /settings loads without full reload → go back → click 'Fetch Fresh Sample' on ExoClick → see spinner → response updates → click 'Copy JSON' → see 'Copied!' → paste into a text editor to verify the JSON is pretty-printed.

Then give me your honest advice on:
- Whether the tab integration follows the existing dashboard's tab patterns correctly
- Any navigation issues with the settings link (back button behavior, scroll position)
- Edge cases in the clipboard API (browser support, permissions, HTTPS requirement)
- What would break if a fifth ad network were added later
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: