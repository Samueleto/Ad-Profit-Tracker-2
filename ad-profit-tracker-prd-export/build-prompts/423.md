# Step 423 of 437: Test the ExoClick integration end-to-end

================================================================================

**Category:** Testing
**Flow:** feature_exoclick-integration

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We're building an ad network analytics dashboard. For this step, I need you to add tests covering the ExoClick integration's critical paths — API endpoints, hooks, and UI components.

**API endpoint tests (integration tests using a test Firebase project or mocked Admin SDK):**

- `POST /api/networks/exoclick/sync`: Test that it returns 401 without a token, 400 for an invalid date range (91 days), 400 for a disabled network, 404 for a missing API key, 429 after 10 syncs in an hour, and 200 with the correct response shape on success. Mock the `exoclick-api` client and `cryptr` decryption in these tests — don't call the real ExoClick API.

- `GET /api/networks/exoclick/stats`: Test 401 without auth, 400 for missing `dateFrom`/`dateTo`, 400 for a range over 90 days, and 200 with correct `groupBy='day'` and `groupBy='total'` responses using seeded Firestore data.

- `GET /api/networks/exoclick/stats/by-country`: Test 401, 400 for invalid `limit` (0, 101), and 200 with correct country aggregation and sorting.

- `GET /api/networks/exoclick/raw-response`: Test 401, 400 for missing date, 404 for a date with no data, and 200 with the correct `records` and `fieldSchema` shape.

- `GET /api/networks/exoclick/stats/latest`: Test 401, 404 when no data exists, and 200 with correct aggregation across multiple country rows.

- `POST /api/networks/exoclick/scheduled-sync`: Test 401 without the `x-internal-secret` header, 401 with a Firebase ID token instead, and 200 with the correct summary response when called with the correct secret.

**Hook unit tests (using React Testing Library + mocked fetch):**

- `useExoClickSync`: Test that `isSyncing` is true during the request and false after. Test that a successful sync calls `refetch` on the latest stats hook. Test that a 429 response surfaces the correct error message.

- `useExoClickStats`: Test that it debounces rapid date range changes and only fires one request.

**Component tests:**

- `ExoClickSummaryCard`: Renders loading state, renders data correctly, renders the 404 empty state prompt, renders error state with Retry button.

- `ExoClickSyncPanel`: Disables the button while syncing, shows success details after a successful sync, shows the correct error message for each error code (400, 404, 429, 502).

- `ExoClickCountryBreakdown`: Renders the table with correct columns, sorts correctly when a column header is clicked

================================================================================

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: