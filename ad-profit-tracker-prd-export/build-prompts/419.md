# Step 419 of 437: Test the onboarding wizard end-to-end

================================================================================

**Category:** Testing
**Flow:** feature_user-onboarding-flow

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

Let's add tests to make sure the OnboardingWizardPage works correctly and doesn't break as the app evolves.

**Unit tests:**
- Timezone validation: confirm that valid IANA strings pass and invalid strings are rejected by the server-side validator used in PATCH /api/auth/update-profile.
- defaultDateRange validation: confirm only 'last7days', 'last30days', and 'thisMonth' are accepted.
- onboardingCompletedAt server timestamp: confirm the endpoint always uses serverTimestamp() regardless of what the client sends.
- The 404 → sync-user → retry logic in useOnboardingProfile: mock GET /api/auth/get-user returning 404, then POST /api/auth/sync-user succeeding, then GET /api/auth/get-user succeeding on retry.

**Component tests:**
- OnboardingWizardPage renders Step 1 with the user's displayName.
- Progress bar shows '1 of 4' on Step 1, '2 of 4' on Step 2, etc.
- Step 2 form is pre-filled with values from GET /api/auth/get-user.
- Submitting Step 2 with an empty displayName shows a validation error (react-hook-form).
- 'Skip for now' on Step 3 advances to Step 4 and sets the skipped flag.
- Error banner appears when the PATCH call returns a 500.
- Retry button re-attempts the failed request.

**Integration tests (full flows):**
- Happy path: new user → Step 1 → Step 2 (fill form) → Step 3 → Step 4 → /dashboard. Confirm PATCH /api/auth/update-profile is called twice (once on Step 2, once on Step 4) with the correct payloads.
- Skip path: new user → Step 1 → Step 2 → Step 3 → 'Skip for now' → Step 4 → /dashboard. Confirm the final PATCH includes onboardingSkipped: true.
- Settings path: Step 3 → 'Go to Settings' → confirm PATCH is called with onboardingCompletedAt before navigating to /settings.
- Already onboarded: user with onboardingCompletedAt set visits /onboarding → redirected to /dashboard immediately.
- Unauthenticated: visiting /onboarding without a session → redirected to login.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure all the onboarding tests pass and cover the important paths.

Confirm that:
- [ ] All unit tests pass (timezone validation, defaultDateRange validation, serverTimestamp enforcement, 404 retry logic)
- [ ] All component tests pass (step rendering, progress bar, form pre-fill, validation errors, error banner, retry button)
- [ ] All integration tests pass (happy path, skip path, settings path, already-onboarded redirect, unauthenticated redirect)
- [ ] PATCH /api/auth/update-profile is called exactly twice in the happy path (Step 2 and Step 4)
- [ ] The final PATCH payload includes onboardingSkipped: true in the skip path
- [ ] The middleware redirect tests pass for both the 'no onboarding' and 'already onboarded' cases

Test the actual flow: Run the full test suite with no failures. Then run the e2e tests against a real browser: start as a new user → complete the full wizard → land on /dashboard → check Firestore to confirm onboardingCompletedAt is set and preferences.timezone and preferences.defaultDateRange are saved correctly. Run the skip path → confirm onboardingSkipped: true in Firestore. Try visiting /onboarding as that same user → confirm immediate redirect to /dashboard.

Then give me your honest advice on:
- What's not covered by these tests that could break in production
- Whether the integration tests are testing the right things or just testing implementation details
- Any flaky test risks (timing issues, Firebase emulator dependencies, async state)
- Tests you'd add before feeling confident about releasing this to real users
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: