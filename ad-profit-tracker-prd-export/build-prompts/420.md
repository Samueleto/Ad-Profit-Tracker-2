# Step 420 of 437: Test the API key management feature

================================================================================

**Category:** Testing
**Flow:** feature_api-key-management-storage

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

Let's add tests to make sure the API key management feature works correctly and stays secure.

**Unit tests for the API endpoints:**
- POST /api/keys/save: test that a valid request encrypts the key and writes to Firestore; test that a missing token returns 401; test that an invalid networkId returns 400; test that an empty key returns 400; test that the stored value is not the raw key.
- GET /api/keys/status: test that it returns status objects for all four networks; test that connected networks show 'connected' and missing ones show 'not_connected'; test that the response never includes the encryptedKey field; test that a missing token returns 401.
- DELETE /api/keys/delete: test that a valid request deletes the Firestore document; test that a missing token returns 401; test that an invalid networkId returns 400; test that deleting a non-existent key returns 404.

**Unit tests for the useApiKeyStatus hook:**
- Test that it calls /api/keys/status on mount
- Test that markConnected updates the right network's status without affecting others
- Test that markDisconnected updates the right network's status without affecting others
- Test that the loading flag transitions correctly

**Component tests for SettingsPage:**
- Test that skeleton cards are shown while the status fetch is loading
- Test that 'Not connected' cards show the input form
- Test that 'Connected' cards show the status badge and Disconnect button
- Test that submitting an empty form shows a validation error (no network call made)
- Test that a successful save updates the card to 'Connected' and shows a toast
- Test that a successful disconnect updates the card to 'Not connected'

**Integration test (end-to-end):**
- Sign in → navigate to /settings → see all four network cards → connect ExoClick with a test key → card shows 'Connected' → disconnect ExoClick → card shows 'Not connected' → refresh page → status is still correct.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure all the tests pass and cover the important paths.

Confirm that:
- [ ] All API endpoint unit tests pass (save, status, delete — including auth and validation cases)
- [ ] The useApiKeyStatus hook tests pass
- [ ] The SettingsPage component tests pass (loading, empty, connected, save, disconnect states)
- [ ] The end-to-end integration test passes
- [ ] The test for POST /api/keys/save confirms the stored value is encrypted (not the raw key)
- [ ] The test for GET /api/keys/status confirms encryptedKey is never in the response
- [ ] Auth rejection tests pass for all three endpoints

Test the actual flow: Run the full test suite with no failures. Then run the e2e test on a real browser: sign in → /settings → connect ExoClick → card shows Connected → disconnect → card shows Not Connected → refresh → status persists correctly.

Then give me your honest advice on:
- What's not covered by these tests that could break in production (especially around encryption or token handling)
- Whether the component tests are testing behavior or just implementation details
- Any flaky test risks (timing issues, Firebase emulator dependencies, token expiry)
- Tests you'd add before feeling confident shipping this to users who are storing real API credentials
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: