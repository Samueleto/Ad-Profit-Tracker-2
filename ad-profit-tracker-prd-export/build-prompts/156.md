# Step 156 of 437: Implement network configuration API endpoints

================================================================================

**Category:** API Endpoints
**Flow:** feature_network-configuration-management

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We need to build several API endpoints to manage network configurations.

1. GET /api/networks/config/list — Fetch all network configurations for the authenticated user. Ensure the endpoint verifies the Firebase ID token and returns only the user's configurations.

2. PATCH /api/networks/config/update — Update specific fields of a network configuration. Validate inputs and ensure only allowed fields are updated.

3. PATCH /api/networks/config/reorder — Update the display order of network configurations using a batch write.

4. POST /api/networks/config/test-connection — Test the connection to a network's API using the stored configuration.

5. POST /api/networks/sync-all — Trigger a sync for all active networks.

6. GET /api/networks/config/:networkId — Fetch a specific network configuration.

7. DELETE /api/networks/config/reset — Reset a network configuration to default values.

Ensure all endpoints use Firebase authentication to verify the user's identity and enforce security rules.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Confirm that:
- [ ] All API endpoints authenticate using Firebase ID tokens
- [ ] Endpoints return correct data and handle errors gracefully
- [ ] Security rules are enforced for all operations

Test: Use Postman or a similar tool to call each endpoint and verify responses.
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: