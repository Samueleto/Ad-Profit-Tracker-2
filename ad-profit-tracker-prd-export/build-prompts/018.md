# Step 18 of 437: Set up folder structure for caching feature

================================================================================

**Category:** Project Scaffolding
**Flow:** feature_data-caching-optimization

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We're building a data caching and optimization layer for a Next.js + Firebase app. For this step, I need you to set up the folder structure for the caching feature.

On the API side, create a folder at `src/app/api/cache/` (or `pages/api/cache/` depending on your Next.js routing setup). Inside it, create subfolders or route files for the three endpoints we'll build: `invalidate`, `status`, and `warm`.

Create a shared server-side utility folder at `src/lib/cache/` (or `lib/cache/`). This is where the node-cache instance will live, along with helper functions for building cache keys, checking cache stats, and writing audit logs. Keeping this separate from the route handlers means all three endpoints can share the same cache instance without re-initializing it on every request.

Also create a `src/lib/cache/cacheKeys.ts` file (empty for now) where we'll define the key-naming conventions — things like how to namespace keys by user UID and network name. This will be important for the invalidation logic later.

Make sure the project still builds after adding these folders.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure the folder structure is set up correctly.

Confirm that:
- [ ] There's an `api/cache/` folder with route files for `invalidate`, `status`, and `warm`
- [ ] There's a `lib/cache/` folder with `cacheProvider.ts` and `cacheKeys.ts`
- [ ] The project still builds without errors after adding these folders

Test the actual flow: Run a build — this is scaffolding only, so nothing should break.

Then give me your honest advice on:
- Whether this folder structure fits well with the existing project layout
- If there's a better place to put the shared cache provider given how Next.js handles server-side singletons
- Anything you'd reorganize before we start building on top of this
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: