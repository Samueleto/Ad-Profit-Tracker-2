# Step 61 of 437: Install and configure recharts

================================================================================

**Category:** SDK Setup & Configuration
**Flow:** feature_daily-profit-trend-visualization

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We need recharts set up before building the chart components. This is the only new package this feature requires — Firebase Auth is already used elsewhere in the project.

Install recharts. Since this is a Next.js project, recharts uses browser APIs that aren't available during server-side rendering, so you'll need to make sure any component that imports from recharts is rendered client-side only. The cleanest way to handle this in Next.js is to use dynamic imports with `ssr: false` when the DailyProfitTrendSection is imported into FinancialMetricsDashboardPage, or to add a `'use client'` directive at the top of the chart components if you're using the App Router.

Also confirm that the existing Firebase Auth setup (firebase/auth) is accessible from this feature — specifically that there's a utility or hook already in the project that returns the current user's ID token (we'll need to attach it as a Bearer token to calls to GET /api/roi/compute, GET /api/roi/breakdown, and GET /api/stats/snapshot). If that utility doesn't exist yet, create a small helper that calls `getIdToken(true)` on the current Firebase user and returns the token string. This will be used by the data-fetching hooks in this feature.

Don't wire up any API calls yet — just make sure recharts is importable without SSR errors and the Firebase token helper is ready to use.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure recharts and the auth token helper are properly set up.

Confirm that:
- [ ] recharts is installed and listed in package.json
- [ ] A simple recharts component (e.g., a bare AreaChart) can be imported and rendered on a client-side page without SSR errors
- [ ] The Firebase token helper exists and calls getIdToken(true) on the current user
- [ ] The token helper returns null (or throws gracefully) when no user is signed in
- [ ] The existing dashboard page still builds and renders without errors

Test the actual flow: Create a throwaway test component that imports AreaChart from recharts and renders it — make sure there are no 'window is not defined' or hydration errors in the browser console. Then call the token helper while signed in and confirm it returns a non-empty string.

Then give me your honest advice on:
- Whether the SSR handling approach is correct for this Next.js setup (App Router vs Pages Router matters here)
- Any version conflicts between recharts and the existing React version
- Whether the Firebase token helper needs any error handling improvements before we build on top of it
- Anything that could cause hydration mismatches in production
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: