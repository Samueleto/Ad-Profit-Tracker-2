# Step 22 of 437: Set up folder structure for geo breakdown

================================================================================

**Category:** Project Scaffolding
**Flow:** feature_geographic-performance-breakdown

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We're building a Geographic Performance Breakdown feature that lives inside the existing FinancialMetricsDashboardPage. For this step, I need you to set up the folder structure so we have a clean home for all the pieces we're about to build.

Create a geo-breakdown feature area under the dashboard features folder. Inside it, create separate subfolders for: components (we'll have GeoBreakdownSection and GeoCountryDrilldownModal as the two main UI pieces, plus smaller sub-components like the metric share bar, GEO insights strip, and per-network sub-row), hooks (for data fetching and state logic), and types (TypeScript interfaces for the geo data shapes).

The two main components we'll be building are:
- GeoBreakdownSection — a full-width card that integrates into the existing FinancialMetricsDashboardPage below the DailyProfitTrendSection card
- GeoCountryDrilldownModal — a slide-over drawer that opens when a user clicks a country row

Neither of these gets its own route — they both live on /dashboard. GeoBreakdownSection is a section rendered inline on the page, and GeoCountryDrilldownModal is a modal triggered from within it.

Make sure the project still compiles cleanly after adding these folders. No logic yet — just the structure.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure the folder structure is in place and nothing is broken.

Confirm that:
- [ ] A geo-breakdown feature folder exists with components/, hooks/, and types/ subfolders
- [ ] The project still builds without errors after adding these folders
- [ ] No existing dashboard pages or components were accidentally modified

Test the actual flow: Run a build — this is scaffolding only, so it should compile cleanly with no new errors.

Then give me your honest advice on:
- Whether this folder structure fits naturally with how the rest of the project is organized
- If there's a better place to put this given the existing dashboard feature layout
- Anything you'd reorganize before we start building the actual components
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: