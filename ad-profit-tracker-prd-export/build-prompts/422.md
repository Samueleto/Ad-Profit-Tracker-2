# Step 422 of 437: Test the audit logging feature end-to-end

================================================================================

**Category:** Testing
**Flow:** feature_audit-logging-activity-tracking

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

Let's add tests to make sure the audit logging feature works correctly and doesn't break.

**Unit tests — hooks:**
- useAuditLogs: initial fetch fires on mount with default params; changing action filter resets list and re-fetches; loadMore appends results using nextCursor; resetFilters clears state and re-fetches; hasMore is false when API returns null nextCursor; client-side search filtering correctly narrows results against stringified metadata.
- useExportLogs: calls GET /api/audit/logs/export with correct filter params; triggers a browser file download; filename includes today's date formatted with date-fns.
- useClearLogs: calls DELETE /api/audit/logs/clear; returns deletedCount on success; exposes error state on failure.

**Unit tests — metadata sanitizer:**
Test the auditMetadataSanitizer utility directly: { apiKey: 'abc', syncSchedule: 'daily' } → only syncSchedule survives. { token: 'xyz', status: 'ok' } → only status survives. { password: '123', latencyMs: 342 } → only latencyMs survives. { secretKey: 'hidden', count: 5 } → only count survives (partial key match). Empty object → empty object. Object with no sensitive keys → unchanged.

**Component tests — ActivityLogTab:**
- Renders shimmer rows during loading state
- Renders the table with correct columns when data is present
- Action type badges render with correct semantic colors for each action type
- Expanding a row shows the metadata JSON inline (no modal)
- Timestamp shows relative time; hovering shows absolute time
- 'Load more' button calls loadMore from the hook
- 'Clear All Logs' button opens a confirmation dialog; canceling does not call useClearLogs; confirming does
- 'Export CSV' button calls useExportLogs with current active filters
- Empty state (no logs ever) shows the correct illustration and message
- Empty state (filtered, no results) shows 'No logs found' with reset button
- 401 error triggers redirect to / (mock the router)
- 500 error shows inline error banner with Retry button

**Integration tests (end-to-end):**
- Full flow: navigate to /settings → click Activity Log tab → logs load → filter by action type → results update → expand a row → see metadata → load more → rows append → export CSV → file downloads → clear logs (confirm) → empty state shows.
- Auth flow: navigate to /settings while unauthenticated → redirected to /.
- Token refresh flow: simulate 401 → token refresh fires → request retries with new token → logs load successfully.

Mock firebase/auth and all three API endpoints (GET /api/audit/logs, GET /api/audit/logs/export, DELETE /api/audit/logs/clear) in unit and component tests. Use real API calls only in integration tests against a test Firebase project.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure all tests pass and cover the important paths.

Confirm that:
- [ ] All useAuditLogs unit tests pass (fetch, filter, pagination, reset, search)
- [ ] All useExportLogs unit tests pass (correct params, file download, filename format)
- [ ] All useClearLogs unit tests pass (API call, deletedCount, error state)
- [ ] All auditMetadataSanitizer unit tests pass (all sensitive key patterns stripped, safe keys preserved)
- [ ] All ActivityLogTab component tests pass (loading, table, badges, expand, clear dialog, export, empty states, error states)
- [ ] Integration test completes the full browse → filter → expand → export → clear flow
- [ ] Integration test confirms unauthenticated users are redirected from /settings
- [ ] Integration test confirms the 401 → token refresh → retry flow works
- [ ] No tests are flaky (re-run the suite 3 times to check)

Test the actual flow: Run the full test suite with coverage. Then run the integration test on a real browser against a test Firebase project: open /settings → Activity Log tab → logs appear → filter by 'api_key_saved' → results narrow → click a row → JSON expands → load more → rows append → export → file downloads → clear → confirm → empty state. Check coverage report — hooks, sanitizer, and the ActivityLogTab component should be well
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: