# Step 426 of 437: Test all Propush endpoints and client logic

================================================================================

**Category:** Testing
**Flow:** feature_propush-integration

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We're building a Propush ad network integration. For this step, I need you to add tests that cover the critical paths across all six endpoints and the client-side API module.

**Unit tests for the API route handlers:**

For POST /api/networks/propush/sync:
- Mock `admin.auth().verifyIdToken()` to return a test uid
- Mock the Firestore reads for networkConfigs and apiKeys
- Mock the axios call to the Propush `/v5/pub-statistics` endpoint — test both a successful response and a failure
- Assert that on success: adStats documents are batch-upserted, networkConfigs is updated with `lastSyncStatus='success'` and `lastSyncedAt`, and an audit log entry is written
- Assert that on Propush API failure: networkConfigs is updated with `lastSyncStatus='failed'` and the endpoint returns 502
- Assert that rate limiting blocks the 11th request within an hour
- Assert that an inactive networkConfig returns 400

For POST /api/networks/propush/scheduled-sync:
- Test that a missing or wrong `x-internal-secret` returns 401
- Test that with a valid secret, it queries networkConfigs for all active Propush users with `syncSchedule='daily'`
- Mock two users — one whose Propush API call succeeds and one that fails — and assert the response summary shows `{ succeeded: 1, failed: 1 }`
- Assert that audit logs are written for both users

For GET /api/networks/propush/stats:
- Test that results are scoped to the authenticated user's uid
- Test `groupBy='total'` aggregates all rows into one
- Test `groupBy='day'` returns one entry per day
- Test that a 90+ day range returns 400

For GET /api/networks/propush/stats/by-country:
- Test that countries are sorted by revenue descending
- Test that the `limit` param slices the results correctly
- Test that each country's revenue share percentage is calculated correctly

For GET /api/networks/propush/stats/latest:
- Test that it returns 404 when no adStats documents exist for the user
- Test that it returns the correct aggregated summary for the most recent date

For GET /api/networks/propush/raw-response:
- Test that it returns the rawResponse field from each matching adStats document
- Test that the schema definition is always included in the response
- Test that an empty result returns an empty array plus the schema

**Unit tests for the client-side API module (`lib/api/propush.ts`):**
- Test that each function attaches the Firebase ID token as a Bearer token
- Test that calling any function without a signed-in user throws before making a network request
- Test that a 429 response maps to the rate limit error message
- Test that a 502 response maps to the 'temporarily unavailable' message
- Test that a network failure maps to the connection error message

**Integration test (end-to-end flow):**
- Sign in as a test user with a configured Propush networkConfig
- Call the sync endpoint for yesterday's date
- Assert that adStats documents were created in Firestore
- Call GET /api/networks/propush/stats/latest and assert it returns data
- Call GET /api/networks/propush/stats/by-country and assert countries are returned sorted by revenue

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure all the tests pass and cover the important paths.

Confirm that:
- [ ] All unit tests for the six route handlers pass
- [ ] The sync endpoint test verifies both success and failure paths including Firestore writes
- [ ] The scheduled-sync test verifies per-user isolation (one failure doesn't stop others)
- [ ] The stats endpoint tests verify uid scoping (User A can't get User B's data)
- [ ] The by-country test verifies sorting and percentage calculation
- [ ] The latest stats test covers the 404 case
- [ ] The raw-response test verifies the schema definition is always returned
- [ ] Client-side API module tests verify token attachment and error message mapping
- [ ] The integration test runs against a real (emulated) Firestore and passes
- [ ] No test makes a real HTTP call to the Propush API (all mocked)
- [ ] Test coverage is solid on the sync and scheduled-sync handlers (most complex logic)

Test the actual flow: Run the full test suite with `npm test` — all tests should pass with no skips. Run the integration test separately against the Firebase emulator: trigger a sync → check Firestore for adStats documents → call latest stats → verify the response matches what was synced. Check that the scheduled-sync test correctly simulates two users and asserts the summary response.

Then give me your honest advice on:
- What's not covered by these tests that could break in production
- Whether the mocking approach for the Propush API is realistic enough to catch real bugs
- Any flaky test risks (timing issues, emulator state leaking between tests)
- Tests you'd add before feeling confident about a release
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: