# Step 25 of 437: Set up folder structure for performance benchmarking

================================================================================

**Category:** Project Scaffolding
**Flow:** feature_performance-benchmarking

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We're building a Performance Benchmarking feature for a financial metrics dashboard. For this step, set up the folder structure so everything has a home before we start writing real code.

Create a benchmarking feature area with the following structure:

- A `components/benchmarking/` folder for the UI pieces we'll build: `PerformanceBenchmarkingTab`, `BenchmarkSettingsModal`, and a `BenchmarkScoreGauge` component for the 0–100 gauge card.
- A `hooks/benchmarking/` folder for data-fetching hooks that the tab and modal will use.
- A `types/benchmarking/` folder for TypeScript interfaces matching the benchmarks Firestore schema and the API response shapes.
- An `app/api/benchmarks/` folder on the backend side with three subfolders: `performance/`, `settings/`, and `score/` — one for each API route we'll build.
- A `lib/benchmarks/` folder for shared server-side utilities: the system defaults config, metric validation helpers, and the performance score computation logic.

The benchmarking tab integrates into the existing FinancialMetricsDashboardPage, so don't create any new top-level pages — just the component and API folders.

Make sure the project still builds cleanly after adding these empty folders (add placeholder index files if needed).

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure the folder structure is set up correctly.

Confirm that:
- [ ] `components/benchmarking/` exists with placeholder files for PerformanceBenchmarkingTab, BenchmarkSettingsModal, and BenchmarkScoreGauge
- [ ] `hooks/benchmarking/` exists
- [ ] `types/benchmarking/` exists
- [ ] `app/api/benchmarks/performance/`, `app/api/benchmarks/settings/`, and `app/api/benchmarks/score/` all exist
- [ ] `lib/benchmarks/` exists for shared server utilities
- [ ] The project builds without errors after adding these folders

Test the actual flow: Run `next build` or `next dev` — no errors should appear from the new empty folders.

Then give me your honest advice on:
- Whether this folder structure fits well with the existing project layout
- If there's a better place to put shared benchmark utilities given how the rest of the app is organized
- Anything you'd reorganize before we start building
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: