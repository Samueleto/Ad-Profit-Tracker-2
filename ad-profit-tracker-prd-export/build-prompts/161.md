# Step 161 of 437: Build all six Propush API endpoints

================================================================================

**Category:** API Endpoints
**Flow:** feature_propush-integration

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We need six API route handlers. Here's what each one does:

**POST /api/networks/propush/sync** — The manual sync trigger. First, verify the Firebase ID token from the `Authorization` header using `admin.auth().verifyIdToken()` to get the `uid`. Validate that `dateFrom` and `dateTo` are valid YYYY-MM-DD strings and the range is at most 90 days. Check a rate limit of 10 calls per hour per user (you can track this in Firestore or in-memory with a simple counter). Look up the user's `networkConfigs` document for `networkId='propush'` — if it doesn't exist or `isActive` is false, return 400. Fetch the `apiKeys` document for this user and network, then call `decryptApiKey()` to get the usable key. Use the Propush API client to call `/v5/pub-statistics` with `groupBy=geo`. Parse the response and batch-upsert documents into `adStats` using Firestore batch writes in chunks of 500 — each document should have `networkId='propush'`, `cost=null`, and the `rawResponse` field populated (sanitize it to remove any sensitive fields before storing). Update `networkConfigs` with `lastSyncedAt=serverTimestamp()` and `lastSyncStatus='success'`. Write an entry to `auditLogs`. If the Propush API call fails, set `lastSyncStatus='failed'` and return 502.

**POST /api/networks/propush/scheduled-sync** — The automated version, called by a cron job. Validate the `x-internal-secret` header against the `INTERNAL_SYNC_SECRET` environment variable — reject with 401 if it doesn't match. Do a collection-group query on `networkConfigs` where `networkId=='propush'`, `isActive==true`, and `syncSchedule=='daily'`. For each matching config, fetch and decrypt that user's API key, call the Propush API for yesterday's stats, batch-upsert into `adStats`, update `networkConfigs`, and write an `auditLogs` entry. Process users sequentially or in small batches to avoid overwhelming Firestore or the Propush API.

**GET /api/networks/propush/stats** — Returns aggregated stats. Verify the Firebase ID token. Validate `dateFrom`, `dateTo` (required, max 90 days), and optional `groupBy` (`'day'` or `'total'`, default `'day'`). Query `adStats` filtered by `userId`, `networkId=='propush'`, and the date range. If `groupBy='day'`, group by date and sum revenue and other metrics per day. If `groupBy='total'`, sum everything into one object. Return the structured result.

**GET /api/networks/propush/stats/by-country** — Returns top countries by revenue. Verify the Firebase ID token. Validate `dateFrom`, `dateTo`, and optional `limit` (integer 1–100, default 20). Query `adStats` for the user's Propush data in the date range. Group by `geo` in memory, summing revenue. Sort by total revenue descending, slice to `limit`, and calculate each country's percentage share of total revenue. Return the list.

**GET /api/networks/propush/stats/latest** — Returns the most recent day's summary. Verify the Firebase ID token. Query `adStats` for the user's Propush data ordered by `date` descending, limit 1, to find the latest date. If nothing exists, return 404. Then query all `adStats` documents for that date and aggregate totals. Also fetch the user's `networkConfigs` document to include `lastSyncedAt` and `lastSyncStatus` in the response.

**GET /api/networks/propush/raw-response** — Returns raw API response data for the API Explorer UI. Verify the Firebase ID token. Validate the required `date` query param. Query `adStats` for the user's Propush data on that date. Map over the results and extract just the `rawResponse` field from each document. Also return a schema definition object describing the fields in the raw response (this powers the API Explorer UI). Return both the array of raw responses and the schema.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure all six Propush endpoints work correctly.

Confirm that:
- [ ] POST /api/networks/propush/sync returns 401 without a valid Firebase ID token
- [ ] POST /api/networks/propush/sync returns 400 if the user's networkConfig is inactive or missing
- [ ] POST /api/networks/propush/sync returns 400 if the date range exceeds 90 days
- [ ] POST /api/networks/propush/sync successfully upserts records into `adStats` and updates `networkConfigs` with `lastSyncedAt` and `lastSyncStatus='success'`
- [ ] POST /api/networks/propush/sync writes an entry to `auditLogs` on both success and failure
- [ ] POST /api/networks/propush/sync returns 502 if the Propush API call fails, and sets `lastSyncStatus='failed'`
- [ ] POST /api/networks/propush/scheduled-sync returns 401 if `x-internal-secret` is wrong or missing
- [ ] POST /api/networks/propush/scheduled-sync processes all active daily-schedule users
- [ ] GET /api/networks/propush/stats returns 401 without auth, 400 for invalid dates, and correct aggregated data
- [ ] GET /api/networks/propush/stats respects the `groupBy` parameter
- [ ] GET /api/networks/propush/stats/by-country returns countries sorted by revenue with percentage shares
- [ ] GET /api/networks/propush/stats/latest returns 404 when no data exists, and correct summary when it does
- [ ] GET /api/networks/propush/raw-response returns the `rawResponse` fields plus a schema definition
- [ ] All GET endpoints only return data belonging to the authenticated user

Test the actual flow: Set up a test user with an active `networkConfigs` document and an encrypted API key in `apiKeys`. Call POST /api/networks/propush/sync with a valid token and a 1-day range → check Firestore for new `adStats` documents and an updated `networkConfigs`. Then call GET /api/networks/propush/stats/latest → should return that day's summary. Call GET /api/networks/propush/stats/by-country → should return countries from the synced data. Call GET /api/networks/propush/raw-response with that date → should return raw response objects. Finally, call POST /api/networks/propush/scheduled-sync with the correct internal secret → should process the test user.

Then give me your honest advice on:
- Whether the batch-write chunking (500 per batch) is implemented correctly and handles partial failures
- Race conditions in the scheduled sync if two jobs run simultaneously for the same user
- Whether the rate limiting implementation is robust enough for production
- Any data consistency issues between `adStats` upserts and `networkConfigs` status updates
- What happens if the Propush API returns partial data or an unexpected response shape
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: