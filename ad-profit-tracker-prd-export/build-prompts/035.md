# Step 35 of 437: Set up folder structure for notifications

================================================================================

**Category:** Project Scaffolding
**Flow:** feature_in-app-notifications

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We're building an in-app notification system for a Next.js + Firebase app. For this step, set up the folder structure so everything has a home before we start writing real code.

On the frontend, create a notifications feature area. Inside it, make separate folders for: components (we'll build NotificationCenterPanel and its sub-pieces here), hooks (for data fetching and state logic), and types (for the TypeScript interfaces). The NotificationCenterPanel is a slide-over panel triggered from the top nav bar, so also make sure there's a place in the shared layout or nav components where we can drop the bell icon trigger.

On the backend, create a folder under your API routes for notifications. We'll be building seven endpoints here: GET /api/notifications, PATCH /api/notifications/dismiss/:id, PATCH /api/notifications/mark-all-read, DELETE /api/notifications/clear, POST /api/notifications/create, GET /api/notifications/preferences, and PATCH /api/notifications/preferences. Each of these can live in its own file or be grouped logically — whatever fits the project's existing API route conventions.

Also create a shared utilities folder (or add to an existing one) for things like the Firebase Admin SDK client and rate-limiting helpers, since multiple endpoints will need those.

Make sure the project still builds cleanly after adding these folders — no broken imports, no missing modules.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure the folder structure is set up correctly.

Confirm that:
- [ ] There's a notifications feature folder with components/, hooks/, and types/ subfolders
- [ ] There are API route folders for all seven notification endpoints: GET /api/notifications, PATCH /api/notifications/dismiss/:id, PATCH /api/notifications/mark-all-read, DELETE /api/notifications/clear, POST /api/notifications/create, GET /api/notifications/preferences, and PATCH /api/notifications/preferences
- [ ] There's a shared lib folder (or equivalent) ready for Firebase Admin utilities
- [ ] The project builds without errors after adding these folders

Test the actual flow: Run the dev server and make sure it starts without errors. This is just scaffolding, so nothing should be broken.

Then give me your honest advice on:
- Whether this folder structure fits the existing project conventions
- If the API route structure handles the dynamic :id segment correctly for Next.js App Router
- Anything you'd reorganize before we start building
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: