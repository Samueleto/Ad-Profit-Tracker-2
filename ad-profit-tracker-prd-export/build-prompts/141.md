# Step 141 of 437: Define TypeScript types for export request and response

================================================================================

**Category:** Schema & Model Definitions
**Flow:** feature_excel-export-functionality

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We don't have new Firestore schemas to create, but we do need clean TypeScript types for the export feature so the API routes, the workbook builder, and the ExportModal all speak the same language.

Create a types file at something like `src/features/export/types.ts` (or `src/lib/export/types.ts`) and define the following:

**ExportSheetKey** — A union type of the 8 allowed sheet identifiers: `'summary' | 'daily_trend' | 'geo_breakdown' | 'exoclick' | 'rollerads' | 'zeydoo' | 'propush' | 'activity_log'`. Export this as a const array too so we can validate against it at runtime.

**ExcelExportRequest** — The shape of the POST /api/export/excel request body: `dateFrom` (string), `dateTo` (string), `sheets` (ExportSheetKey[]), `filename` (optional string), `includeHeaders` (optional boolean, defaults to true).

**ExportPreviewResponse** — The shape of the GET /api/export/preview response: `dateFrom`, `dateTo`, a `sheets` object with each ExportSheetKey mapped to a number (row count), `totalRows` (number), `hasData` (boolean), and `cachedAt` (string or null).

**ExportSheetConfig** — An internal type used by the workbook builder: `{ key: ExportSheetKey, label: string, columns: { header: string, key: string, width: number }[] }`. This makes it easy to loop over sheets when building the workbook.

Also add a small Zod validation schema for the POST /api/export/excel request body — validate that `dateFrom` and `dateTo` are valid YYYY-MM-DD strings, that `sheets` is a non-empty array of valid ExportSheetKey values, that `filename` (if provided) only contains alphanumeric characters, dashes, and underscores and is max 100 chars, and that the date range is no more than 90 days.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure the types and validation are solid.

Confirm that:
- [ ] `ExportSheetKey` union type includes all 8 sheet keys
- [ ] The const array of allowed sheet keys can be used for runtime validation
- [ ] `ExcelExportRequest` has all the right fields with correct optionality
- [ ] `ExportPreviewResponse` matches the shape described in the GET /api/export/preview spec
- [ ] The Zod schema rejects a request with an empty `sheets` array
- [ ] The Zod schema rejects a `filename` with special characters like `../` or spaces
- [ ] The Zod schema rejects a date range wider than 90 days
- [ ] The Zod schema rejects `dateFrom` that is after `dateTo`
- [ ] TypeScript compiles without errors

Test the actual flow: Write a few quick inline tests — pass a valid request object through the Zod schema and confirm it passes. Pass an invalid filename (`my file!`), an empty sheets array, and a 91-day date range — confirm each one fails with a descriptive error message.

Then give me your honest advice on:
- Whether there are any edge cases in the date validation (timezone handling, leap years)
- If the `ExportSheetConfig` type covers everything the workbook builder will need
- Any fields we might be missing before the API routes start using these types
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: