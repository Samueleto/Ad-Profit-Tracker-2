# Step 196 of 437: Build reusable network configuration components

================================================================================

**Category:** UI Components
**Flow:** feature_network-configuration-management

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We need to create reusable UI components for the network configuration management feature.

1. NetworkCard — Displays a network's configuration details, including name, logo, connection status, and sync schedule. It should support expanding to show advanced settings like endpoint override and retry attempts.

2. AdvancedSettings — A collapsible component that shows additional configuration options for a network, such as API endpoint override, request timeout, and retry attempts.

3. SyncScheduleSelector — A dropdown component for selecting the sync schedule (hourly, daily, weekly).

4. ConnectionStatusBadge — A visual indicator showing the connection status of a network (connected, not_connected, error).

These components will be used in the NetworkConfigTab to provide a consistent and user-friendly interface for managing network configurations.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Confirm that:
- [ ] NetworkCard displays all necessary information and supports expansion
- [ ] AdvancedSettings is collapsible and shows additional options
- [ ] SyncScheduleSelector works correctly
- [ ] ConnectionStatusBadge accurately reflects the network status

Test: Render these components in a sample page to verify their appearance and functionality.
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: