# Step 427 of 437: Test all stats endpoints and hooks

================================================================================

**Category:** Testing
**Flow:** feature_historical-data-storage

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

Let's add tests to make sure the historical data storage feature doesn't break silently.

**API endpoint unit tests** — Test each of the eight endpoints in isolation by mocking the Firebase Admin SDK and Firestore calls:

- GET /api/stats/snapshot: test with valid date + groupBy='total' returns correct aggregated networks and summary; test groupBy='country' returns per-country breakdown; test missing date returns 400; test invalid networkId returns 400; test no matching documents returns 404; test missing auth returns 401.

- GET /api/stats/trend: test valid date range returns series with correct metric values; test range > 90 days returns 400; test dateFrom > dateTo returns 400; test each metric value ('revenue', 'cost', 'profit', 'roi', 'impressions', 'clicks') returns the right field; test 401 without auth.

- GET /api/stats/summary: test aggregation across multiple networks and dates is correct (revenue from rollerads+zeydoo+propush, cost from exoclick only); test roi is null when totalCost is 0; test 401 without auth.

- GET /api/stats/geo-breakdown: test countries are sorted by the requested metric descending; test limit param slices correctly; test metricShare percentages sum to ~100%; test country codes are enriched with full names; test 401 without auth.

- GET /api/stats/coverage: test coverage map correctly marks dates as present/missing/partial; test gaps array contains only dates with missing networks; test syncHistory is populated from auditLogs; test 401 without auth.

- POST /api/stats/backfill: test rate limit returns 429 after 3 requests; test networks without API keys appear in skipped array; test Promise.allSettled behavior (one network failure doesn't block others); test audit log is written; test 401 without auth.

- DELETE /api/stats/snapshot: test rate limit returns 429 after 5 deletes; test batch delete removes correct documents; test audit log is written with deletedCount; test 404 when no documents match; test 401 without auth.

- GET /api/stats/dates: test returns dates sorted descending; test isComplete is true only when all 4 networks have data; test defaults to last 90 days when no params provided; test 401 without auth.

**Hook unit tests** — Test each hook (useSnapshot, useTrend, useSummary, useGeoBreakdown, useCoverage, useDates, useBackfill, useDeleteSnapshot) using mocked fetch responses:
- Verify the hook attaches an Authorization header to every request
- Verify loading=true during fetch, loading=false after
- Verify data populates correctly on success
- Verify error populates on failure
- Verify 404 sets data to null (empty state) without setting error
- Verify 401 sets a distinct auth-error flag
- Verify useBackfill and useDeleteSnapshot expose a reset() function that clears results
- Verify useDates fires on mount without requiring date params

**Integration test** — One end-to-end flow: mock a set of adStats documents for a user across 7 days and 3 networks → call GET /api/stats/summary for that range → verify totals are correct → call GET /api/stats/coverage → verify gaps show the missing network → call POST /api/stats/backfill for the missing network → verify the audit log was written → call GET /api/stats/coverage again → verify the gap is gone.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure all tests pass and cover the important paths.

Confirm that:
- [ ] All 8 API endpoint test files pass with no failures
- [ ] Auth is tested on every endpoint (missing token, fake token, valid token)
- [ ] Rate limit tests pass for POST /api/stats/backfill (3/hour) and DELETE /api/stats/snapshot (5/hour)
- [ ] The aggregation logic tests pass — revenue from rollerads+zeydoo+propush, cost from exoclick only
- [ ] ROI is null when totalCost is 0 (no division by zero)
- [ ] All 8 hook test files pass
- [ ] Hooks attach Authorization headers in every test
- [ ] 404 responses result in empty state (not error state) in hook tests
- [ ] 401 responses set a distinct auth-error flag in hook tests
- [ ] The integration test passes end-to-end
- [ ] Test coverage is solid on the aggregation and grouping logic (this is where bugs hide)

Test the actual flow: Run the full test suite with `jest`. Then manually test the integration flow: seed mock adStats data for a user across 7 days with one network missing on day 3 → call /api/stats/coverage → verify day 3 shows in gaps → call /api/stats/backfill for that network and date → call /api/stats/coverage again → gap is gone → check auditLogs → entry exists with correct metadata.

Then give me your honest advice on:
- What's not covered by these tests that could silently break in production (especially the aggregation math)
- Whether the mocked Firestore responses are realistic enough to catch real bugs
- Any flaky test risks (timing, async state, shared state between tests)
- Tests you'd add before feeling confident about a production release
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: