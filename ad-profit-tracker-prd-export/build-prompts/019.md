# Step 19 of 437: Set up folder structure for financial dashboard

================================================================================

**Category:** Project Scaffolding
**Flow:** feature_financial-metrics-dashboard

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We're building a financial metrics dashboard for an ad network portfolio tracker. For this step, set up the folder structure so everything has a clean home before we start writing real code.

On the frontend side, create a dashboard feature area under something like src/features/dashboard/ or src/app/dashboard/ (wherever makes sense given the existing Next.js project structure). Inside it, make folders for: components (we'll build KPI cards, a trend chart, a GEO breakdown table, a per-network summary section, and a date range selector), hooks (for data fetching and cache status logic), and types (for the dashboard payload shape).

The main page itself will live at the /dashboard route — in Next.js App Router that's src/app/dashboard/page.tsx, or in Pages Router it's src/pages/dashboard.tsx. Create that file as a placeholder for now.

On the backend side, create a folder for the dashboard API route. We need one endpoint at GET /api/dashboard/metrics — in App Router that's src/app/api/dashboard/metrics/route.ts, in Pages Router it's src/pages/api/dashboard/metrics.ts.

Also create a src/lib/ folder (if it doesn't already exist) for shared server utilities — we'll put the node-cache instance and Firebase Admin initialization there.

Keep the component folder separate from the page file so the page stays thin and just wires things together.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure the folder structure is set up correctly.

Confirm that:
- [ ] There's a dashboard page file at the /dashboard route (placeholder is fine)
- [ ] There's an API route file at /api/dashboard/metrics (placeholder is fine)
- [ ] There are component, hooks, and types subfolders under the dashboard feature area
- [ ] There's a src/lib/ folder for shared server utilities
- [ ] The project still builds without errors after adding these files and folders

Test the actual flow: Run the dev server and navigate to /dashboard — it should load without a 404 (even if it's just a blank page). Hit /api/dashboard/metrics in the browser — it should return something (even a 405 or empty response) rather than a 404.

Then give me your honest advice on:
- Whether this folder structure fits well with the existing project layout
- If there's a better way to co-locate things given how the rest of the app is organized
- Any naming conventions in the project I should follow before we build on top of this
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: