# Step 431 of 437: Test the filter feature end-to-end

================================================================================

**Category:** Testing
**Flow:** feature_data-filtering-search

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

Let's add tests to make sure the FilterToolbar and its connected endpoints work correctly and don't regress.

**Unit tests — Zustand store**:
- All filter fields initialize to their correct defaults
- setStagedNetworks updates staged but not selectedNetworks
- applyFilters() copies staged values into committed fields
- setSelectedMetric and setDataQuality update immediately
- clearAllFilters() resets every field to defaults
- activeFilterCount returns 0 at defaults, increments correctly for each active dimension, and returns the right count with multiple active filters
- Existing dateFrom/dateTo actions are unaffected by the new fields

**Unit tests — API endpoint handlers** (mock firebase-admin and node-cache):
- GET /api/filters/options: returns 401 without token, 400 for invalid type, 400 for date range over 90 days, 200 with country list for valid request, returns X-Cache: HIT header on cache hit
- GET /api/filters/stats: returns 401 without token, 400 for unknown network IDs, 400 for invalid metric, 200 with filtered rows and summary for valid request, 404 when no data matches
- GET /api/filters/search: returns 401 without token, 400 for empty q after trim, 200 with results for valid query
- GET /api/filters/presets: returns 401 without token, 200 with empty array when no presets exist
- POST /api/filters/presets: returns 401 without token, 400 for name over 50 chars, 400 when 20-preset limit reached, 429 when rate limited, 201 with created preset on success
- DELETE /api/filters/presets/:presetId: returns 401 without token, 404 for non-existent preset, 200 with deletion confirmation on success

**Component tests — FilterToolbar**:
- Renders all five controls
- Network checkboxes update staged state, not committed state
- 'All Networks' toggle checks/unchecks all four networks
- Metric pills apply immediately to the store
- Data quality toggle applies immediately
- Free-text search debounces 300ms before updating the store
- 'Apply Filters' button commits staged selections
- 'Clear All' resets all store fields
- Active filter count badge shows correct count
- Country combobox shows loading state while fetch is in flight
- Country combobox shows error state with Retry link on 500
- On mobile viewport, controls collapse into a Filters button

**Integration test — full filter flow**:
- Open /dashboard → apply a network filter → verify GET /api/filters/stats is called with the correct networks param → verify dashboard sections update → apply a country filter → verify stats re-fetch with both params → type in free-text search → verify table rows filter client-side without a network call → save as a preset → reload the page → load the preset → verify filters restore → delete the preset → verify it's gone.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure all tests pass and cover the important paths.

Confirm that:
- [ ] All Zustand store unit tests pass (defaults, staged vs. committed, clearAllFilters, activeFilterCount)
- [ ] All API endpoint unit tests pass for all six endpoints (auth, validation, success, error cases)
- [ ] All FilterToolbar component tests pass (controls, debounce, loading/error states, mobile collapse)
- [ ] The integration test passes end-to-end
- [ ] No existing dashboard tests are broken by the store changes

Test the actual flow: Run the full test suite with no failures. Then run
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: