# Step 37 of 437: Set up folder structure for multi-user team feature

================================================================================

**Category:** Project Scaffolding
**Flow:** feature_multi-user-account-support

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We're building multi-user account support — the ability to invite teammates to a shared workspace, manage their roles, and accept invitations via a link. For this step, I need you to set up the folder structure so everything has a home before we start building.

On the frontend, create a feature area for team management. Inside it, add folders for: components (we'll have an InviteMemberModal and some shared pieces like role badges and member table rows), hooks (for fetching members, invitations, and workspace metadata), and types (TypeScript interfaces for Member, WorkspaceInvitation, and WorkspaceMetadata).

For pages, we need two new route locations:
- A team settings tab that will live inside the existing SettingsPage at /settings — create a folder at app/(app)/settings/team/ for the TeamManagementPage
- A public invitation acceptance page at app/invite/accept/ for the AcceptInvitationPage — this one lives outside the authenticated route group since it's publicly accessible

On the backend, create an API routes folder structure for the team endpoints. We'll need route handlers under app/api/team/ with subfolders for: members/, workspace/, and invitations/. The members folder will need a [memberId] dynamic segment folder with a role/ subfolder inside it. The invitations folder will need subfolders for validate/, accept/, and decline/.

Also create a server-side utilities folder (e.g., lib/team/) where we'll put shared helpers like token hashing, rate limiting, and audit logging that multiple API routes will reuse.

The project should still build cleanly after this step — we're just creating the skeleton.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure the folder structure is set up correctly.

Confirm that:
- [ ] There's a team feature folder with components/, hooks/, and types/ subfolders
- [ ] The settings/team/ route folder exists inside the (app) route group
- [ ] The invite/accept/ route folder exists outside the (app) route group (public)
- [ ] The API route folders exist: api/team/members/, api/team/members/[memberId]/role/, api/team/workspace/, api/team/invitations/, api/team/invitations/[invitationId]/, api/team/invitations/validate/, api/team/invitations/accept/, api/team/invitations/decline/
- [ ] A shared server utilities folder exists at lib/team/
- [ ] The project builds without errors after adding all these folders

Test the actual flow: Run the build (npm run build or next build) and confirm it completes without errors. This is scaffolding only — nothing should be broken.

Then give me your honest advice on:
- Whether this folder structure fits well with the existing project layout
- If the invite/accept page being outside the (app) route group is the right call for a public page
- Any naming conventions in the existing codebase we should match
- Anything you'd reorganize before we start filling these folders
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: