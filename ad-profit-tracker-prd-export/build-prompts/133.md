# Step 133 of 437: Add caching fields to NetworkConfig model

================================================================================

**Category:** Schema & Model Definitions
**Flow:** feature_data-caching-optimization

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We need to update the TypeScript model for the `networkConfigs` collection to include the three new caching configuration fields.

Find the existing `NetworkConfig` type or interface in the codebase (it likely lives somewhere like `src/types/networkConfig.ts` or `src/models/networkConfig.ts`). Add the following optional fields:

- `serverCacheTTLSeconds?: number` — how long (in seconds) the server-side node-cache should store API responses for this network. If not set, the system default of 3600 seconds is used. This governs expensive ad network API calls.
- `clientCacheStaleSeconds?: number` — the stale-while-revalidate window in seconds for the client-side SWR cache. Data is considered fresh for this duration; after that, stale data is served while a background revalidation happens. Defaults to 60.
- `clientCacheFocusRevalidate?: boolean` — whether the SWR cache should automatically revalidate when the browser window regains focus. Defaults to `true`.

All three fields are optional because existing `networkConfigs` documents won't have them. Create a helper function `getNetworkCacheConfig(config: NetworkConfig)` that returns the resolved values with defaults applied — so callers don't have to check for undefined everywhere:
```
{
  serverCacheTTLSeconds: config.serverCacheTTLSeconds ?? 3600,
  clientCacheStaleSeconds: config.clientCacheStaleSeconds ?? 60,
  clientCacheFocusRevalidate: config.clientCacheFocusRevalidate ?? true
}
```

Also create a Zod validation schema for the cache invalidation request body used by `POST /api/cache/invalidate`: `scope` must be one of `'all'`, `'network'`, or `'endpoint'`; `keys` is an optional array of strings. Export this as `CacheInvalidateRequestSchema`.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure the models and schemas are correct.

Confirm that:
- [ ] The `NetworkConfig` type now includes `serverCacheTTLSeconds`, `clientCacheStaleSeconds`, and `clientCacheFocusRevalidate` as optional fields
- [ ] Existing code that uses `NetworkConfig` still compiles (the new fields are optional, so nothing should break)
- [ ] `getNetworkCacheConfig` returns 3600, 60, and true when all fields are undefined
- [ ] `getNetworkCacheConfig` returns the actual values when fields are set
- [ ] `CacheInvalidateRequestSchema` accepts `{ scope: 'network', keys: ['exoclick'] }`
- [ ] `CacheInvalidateRequestSchema` rejects `{ scope: 'invalid' }`
- [ ] `CacheInvalidateRequestSchema` accepts `{ scope: 'all' }` (keys is optional)

Test the actual flow: Create a mock `NetworkConfig` with no caching fields → run through `getNetworkCacheConfig` → confirm defaults. Create one with `serverCacheTTLSeconds: 7200` → confirm that value comes through. Run the Zod schema against valid and invalid inputs.

Then give me your honest advice on:
- Whether the defaults make sense for a real ad network dashboard
- Any TypeScript issues with the optional fields and existing code
- Edge cases (what if `serverCacheTTLSeconds` is set to 0 or a negative number?)
- Anything you'd add to the validation before building the endpoints on top of this
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: