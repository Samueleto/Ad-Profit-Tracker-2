# Step 64 of 437: Verify recharts and node-cache are installed

================================================================================

**Category:** SDK Setup & Configuration
**Flow:** feature_comparative-network-analysis

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We're building a Comparative Network Analysis tab that includes a grouped bar chart (using recharts) on the frontend and a caching layer (using node-cache) on the backend API route. For this step, make sure both packages are installed and working.

On the frontend, check if recharts is already in the project's package.json — it may already be installed since other dashboard tabs likely use it. If it's not there, add it. Recharts needs to be imported carefully in Next.js because it's a client-side library — make sure any component that imports from recharts has the 'use client' directive at the top, or is wrapped in a dynamic import with ssr: false. Create a quick smoke-test component (just a placeholder, not the real chart) to confirm recharts renders without hydration errors.

On the backend, check if node-cache is already installed — it's likely used by the existing /api/networks/stats and /api/roi/compute endpoints. If it's not installed, add it. Create or locate the shared cache instance that the other API routes use. The comparison endpoint will use the same cache instance with keys namespaced by uid in the format '{uid}_network_comparison_{dateFrom}_{dateTo}_{metric}' with a 5-minute TTL. If there's already a shared cache utility file in the project, note its location — the new endpoint will import from there rather than creating a second cache instance.

Also confirm that firebase-admin is already initialized in the project (it should be, since other endpoints use it). Locate the shared firebase-admin initialization so the new comparison endpoint can import the admin Firestore and auth instances from the same place.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure both SDKs are ready to use.

Confirm that:
- [ ] recharts is in package.json and installs without errors
- [ ] A simple recharts BarChart renders in the browser without hydration errors (test with a 'use client' component)
- [ ] node-cache is in package.json
- [ ] There's a shared cache instance the new endpoint can import
- [ ] firebase-admin is initialized and the Firestore + auth instances are importable
- [ ] The project still builds and runs cleanly

Test the actual flow: Create a throwaway client component that renders a recharts BarChart with hardcoded data — confirm it renders in the browser with no console errors. Then confirm the firebase-admin and node-cache imports resolve correctly in a test API route.

Then give me your honest advice on:
- Whether recharts is the right choice given how other charts are built in this project (consistency matters)
- Any known Next.js + recharts SSR gotchas we should handle upfront
- Whether the shared cache instance approach is solid or if there are concurrency concerns
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: