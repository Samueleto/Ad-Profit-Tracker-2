# Step 432 of 437: Test the API Explorer feature

================================================================================

**Category:** Testing
**Flow:** feature_api-response-explorer

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

Let's add tests to make sure the API Explorer works correctly and doesn't break when things go wrong.

**Unit tests for the Zustand store (apiExplorerStore)**:
- Initial state: all four networks start with null cachedResponse, null lastFetched, false for all loading flags, null error, and false for isConfigured
- loadCachedResponse: sets isLoadingCached to true during the call, populates cachedResponse and lastFetched on success, sets error on failure, only affects the targeted network's state
- fetchFreshSample: sets isFetchingFresh to true during the call, updates cachedResponse and lastFetched on success, sets error on failure
- clearError: resets only the targeted network's error field
- setConfigured: updates only the targeted network's isConfigured field
- Calling loadCachedResponse when isConfigured is false: should not make a fetch call

**Component tests for NetworkAccordion**:
- Renders collapsed by default
- Shows shimmer placeholder while isLoadingCached is true
- Shows the react-json-view component when cachedResponse is populated
- Shows 'API key not configured' message with a /settings link when isConfigured is false
- Shows 'No response cached yet' message when isConfigured is true but cachedResponse is null and not loading
- Shows inline error with Retry button when error is set
- 'Fetch Fresh Sample' button is disabled and shows spinner when isFetchingFresh is true
- 'Copy JSON' button triggers clipboard write
- Expanding the accordion for the first time triggers loadCachedResponse
- Expanding a second time does NOT trigger loadCachedResponse again

**Component tests for FieldMappingTable**:
- Renders a two-column table with 'Raw API Field' and 'Internal adStats Field' headers
- Renders the correct number of rows for each network's field mapping

**Integration test (end-to-end flow)**:
- Navigate to /dashboard as an authenticated user
- Click the 'API Explorer' tab
- Expand the ExoClick accordion
- Confirm the cached response loads and displays in the JSON viewer
- Click 'Fetch Fresh Sample'
- Confirm the response updates and the timestamp refreshes
- Click 'Copy JSON'
- Confirm the clipboard contains valid JSON

Mock all fetch calls in unit and component tests — don't make real network calls. Use the actual Zustand store in integration tests.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure all the tests pass and cover the important paths.

Confirm that:
- [ ] All Zustand store unit tests pass (initial state, all four actions, per-network isolation)
- [ ] All NetworkAccordion component tests pass (all states: loading, empty, configured, error, fetching)
- [ ] FieldMappingTable renders correctly for each network
- [ ] ApiExplorerTab integration test passes (tab renders, accordion expands, fetch works, copy works)
- [ ] No tests make real network calls (all fetches are mocked)
- [ ] Tests cover the 'one network errors, others unaffected' scenario

Test the actual flow: Run the full test suite with `npm test` — all tests should pass with no failures. Then run the integration test against a real dev environment: log in → /dashboard → API Explorer tab → expand ExoClick → see response → fetch fresh → copy JSON → verify clipboard content is valid JSON.

Then give me your honest advice on:
- What important scenarios aren't covered by these tests that could break in production
- Whether the component tests are testing behavior (what the user sees) vs. implementation details
- Any flaky test risks (timing issues with the accordion animation, async state updates)
- Tests you'd want to add before feeling confident shipping this to real developer users
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: