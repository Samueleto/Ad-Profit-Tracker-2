# Step 60 of 437: Set up node-cache and verify firebase-admin

================================================================================

**Category:** SDK Setup & Configuration
**Flow:** feature_roi-calculation-engine

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We're building the ROI Calculation Engine. For this step, we need to make sure the two server-side packages — `firebase-admin` and `node-cache` — are properly installed and initialized, since all four ROI endpoints depend on them.

First, check if `firebase-admin` is already initialized in the project (there's likely an existing `src/lib/firebaseAdmin.ts` or similar). If it is, just make sure it's exporting the `admin` instance and the Firestore `db` reference so our new ROI route handlers can import them. If it's not set up yet, initialize it using the `FIREBASE_SERVICE_ACCOUNT` environment variable (a JSON string of the service account credentials) and export both `admin` and `db`.

Second, install `node-cache` if it's not already in `package.json`. Then create a shared cache instance at `src/lib/roi/cache.ts`. Export a single `roiCache` instance configured with a default TTL of 300 seconds (5 minutes) and a check period of 60 seconds. Both `GET /api/roi/compute` and `GET /api/roi/breakdown` will import this same instance — we want one shared cache, not separate instances per route.

Also install `recharts` on the frontend side if it's not already present — the ROIIndicatorSection component will use it to render the daily profit trend chart with the breakeven threshold line.

Add any missing environment variables to `.env.local`: `FIREBASE_SERVICE_ACCOUNT` (if not already there). Don't commit actual credentials — just document the variable name in a `.env.example` file.

Make sure everything imports cleanly and the project still builds.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure both SDKs are properly set up.

Confirm that:
- [ ] `firebase-admin` is initialized and exports `admin` and `db` (Firestore instance)
- [ ] `node-cache` is installed and a shared `roiCache` instance is exported from `src/lib/roi/cache.ts`
- [ ] The cache is configured with a 5-minute TTL
- [ ] `recharts` is installed and importable in a frontend component
- [ ] `FIREBASE_SERVICE_ACCOUNT` is documented in `.env.example`
- [ ] The project builds without errors

Test the actual flow: Import `roiCache` in a test file and call `roiCache.set('test', 'value')` then `roiCache.get('test')` — confirm it returns `'value'`. Import `db` from the Firebase admin setup and confirm it's a Firestore instance without throwing.

Then give me your honest advice on:
- Whether the single shared `node-cache` instance will behave correctly across Next.js API route invocations (hot module reloading, serverless cold starts, etc.)
- Any Firebase Admin initialization issues specific to Next.js (double-initialization on hot reload)
- Whether `recharts` needs any additional Next.js config (SSR compatibility, dynamic imports)
- Environment variable security — anything that could accidentally leak server credentials to the client
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: