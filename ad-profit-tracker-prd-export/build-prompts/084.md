# Step 84 of 437: Configure Firestore for network configurations

================================================================================

**Category:** Database Setup
**Flow:** feature_network-configuration-management

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We need to configure Firestore to store network configurations securely and efficiently.

Set up the networkConfigs collection in Firestore with the following fields: userId, networkId, isActive, syncSchedule, dataRole, endpointOverride, timeoutSeconds, retryAttempts, lastSyncedAt, lastSyncStatus, lastSyncError, displayOrder, createdAt, and updatedAt. Ensure that the userId is indexed for fast lookups.

Implement Firestore security rules to ensure that users can only read and write their own network configurations. The rules should enforce that the userId in the document matches the authenticated user's ID. Also, set up rules to prevent unauthorized creation, updates, or deletions of documents.

This setup will ensure that network configurations are stored securely and can be accessed efficiently.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Confirm that:
- [ ] The networkConfigs collection is set up with all required fields
- [ ] Firestore security rules enforce user-specific access
- [ ] Indexes are created for efficient queries

Test: Use the Firestore console to verify that documents can be read and written according to the security rules.
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: