# Step 140 of 437: Define TypeScript types for API Explorer data

================================================================================

**Category:** Schema & Model Definitions
**Flow:** feature_api-response-explorer

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We need clear TypeScript types so the API Explorer components and API routes all speak the same language. Create these in `src/features/api-explorer/types/`.

First, define a `NetworkId` type — a union of the four supported networks: `'exoclick' | 'rollerads' | 'zeydoo' | 'propush'`. Use this everywhere instead of raw strings so typos get caught at compile time.

Second, define a `NetworkConfig` type that mirrors the `networkConfigs` Firestore schema: `networkId` (NetworkId), `endpointUrl` (string), `httpMethod` (string, e.g. 'GET' or 'POST'), `requiredParams` (an array of objects with `name` and `description` fields), and `displayName` (a human-readable label like 'ExoClick').

Third, define a `CachedResponse` type matching the `cachedResponses` collection: `networkId` (NetworkId), `rawResponse` (Record<string, unknown> — the raw JSON), `fetchedAt` (ISO string or Firestore Timestamp), and `httpStatus` (number).

Fourth, define a `FieldMapping` type for the field mapping table: `rawField` (string — the field name as it comes from the ad network API), `internalField` (string — the corresponding field in the app's `adStats` schema), and optionally `notes` (string — for any transformation logic worth documenting).

Finally, create a static `FIELD_MAPPINGS` constant (not fetched from the database — this is hardcoded reference data) that maps each network's raw API fields to the `adStats` schema fields. This is what the field mapping table in the UI will render. Include mappings for all four networks.

Also define the shape of the API responses for the five endpoints: `/api/networks/exoclick/raw-response`, `/api/networks/rollerads/raw-response`, `/api/networks/zeydoo/raw-response`, `/api/networks/propush/raw-response`, and `/api/networks/config/test-connection`. Each raw-response endpoint returns `{ networkId, rawResponse, fetchedAt, httpStatus, config: NetworkConfig }`. The test-connection endpoint returns `{ success: boolean, networkId, latencyMs: number, error?: string }`.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure all the types are defined correctly and the static data is complete.

Confirm that:
- [ ] `NetworkId` is a union type covering all four networks
- [ ] `NetworkConfig` has all fields: networkId, endpointUrl, httpMethod, requiredParams, displayName
- [ ] `CachedResponse` has all fields: networkId, rawResponse, fetchedAt, httpStatus
- [ ] `FieldMapping` has rawField, internalField, and optional notes
- [ ] `FIELD_MAPPINGS` has entries for all four networks mapping to `adStats` schema fields
- [ ] The response shapes for all five API endpoints are typed
- [ ] TypeScript compiles without errors after adding these types

Test the actual flow: Import `NetworkId` in a test file and try assigning an invalid string — TypeScript should catch it. Import `FIELD_MAPPINGS` and check that all four networks have at least a few mappings defined. Run `tsc --noEmit` to confirm no type errors.

Then give me your honest advice on:
- Whether the `rawResponse` typed as `Record<string, unknown>` is flexible enough or if we should type each network's response more specifically
- Any fields in the `adStats` schema that don't have a clear mapping from the raw API responses
- Whether the static `FIELD_MAPPINGS` approach is maintainable or if this should eventually come from the database
- Missing types that will become obvious once we start building the components
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: