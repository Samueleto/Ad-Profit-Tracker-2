# Step 437 of 437: Test all three deployment pipeline endpoints

================================================================================

**Category:** Testing
**Flow:** feature_cloud-run-deployment-pipeline

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We're building a Cloud Run deployment pipeline for a Next.js + Firebase app. For this step, I need you to write tests for all three endpoints: GET /api/health, GET /api/ready, and GET /api/deployment/status.

For GET /api/health, write unit tests that mock the Firebase Admin Firestore client. Test the happy path: Firestore responds quickly → status is 'healthy', firestore is 'ok'. Test the degraded path: mock Firestore to respond after 1.5 seconds → status is 'degraded', firestore is 'slow'. Test the unhealthy path: mock Firestore to throw or time out → response is 503 with status 'unhealthy'. Test the memory check: mock process.memoryUsage() to return a heapUsed value over 400MB and verify it's flagged in the response.

For GET /api/ready, write unit tests that cover: all env vars present and Firebase Admin initialized → 200 with ready: true and both checks 'ok'. Each required env var missing one at a time (FIREBASE_PROJECT_ID, INTERNAL_SYNC_SECRET, EMAIL_PROVIDER_API_KEY, EMAIL_FROM_ADDRESS) → 503 with failedCheck: 'envVars'. Firebase Admin not initialized (mock admin.apps.length to return 0) → 503 with failedCheck: 'firebaseAdmin'. Verify that the response never includes actual env var values.

For GET /api/deployment/status, write unit tests that cover: no Authorization header → 401. Invalid/expired token (mock admin.auth().verifyIdToken to throw) → 401. Valid token but user has workspaceRole 'member' → 403. Valid token, user has workspaceRole 'admin' → 200 with correct deployment metadata shape. Valid token, user has workspaceRole 'owner' → 200. Firestore role lookup throws → 500 with sanitized error. Verify the response shape includes service, revision, region, environment, uptimeSeconds, nodeVersion, and deployedAt fields.

Also write a simple integration smoke test that calls all three endpoints against a running local dev server: /api/health and /api/ready should return 200, and /api/deployment/status should return 401 when called without a token.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure all tests pass and cover the important paths.

Confirm that:
- [ ] GET /api/health tests pass: healthy, degraded (slow Firestore), unhealthy (Firestore timeout), and high memory cases
- [ ] GET /api/ready tests pass: all checks ok, each env var missing individually, Firebase Admin not initialized
- [ ] GET /api/deployment/status tests pass: no token (401), invalid token (401), member role (403), admin role (200), owner role (200), Firestore lookup failure (500)
- [ ] The deployment/status 200 response shape is validated — all fields (service, revision, region, environment, uptimeSeconds, nodeVersion, deployedAt) are present
- [ ] No test accidentally exposes real env var values in assertions
- [ ] The integration smoke test confirms /api/health and /api/ready return 200 and /api/deployment/status returns 401 without a token
- [ ] All tests run without hitting real Firestore or real Firebase Auth (everything is mocked)

Test the actual flow: Run the full test suite with jest → all tests should pass with no real network calls. Then run the integration smoke test against a local dev server → /api/health returns 200, /api/ready returns 200 (assuming env vars are set), /api/deployment/status returns 401 with no token.

Then give me your honest advice on:
- What's not covered by these tests that could break in production — particularly around the Cloud Run-specific env vars (K_REVISION, K_SERVICE, K_CONFIGURATION) that are only injected at runtime
- Whether the Firestore timeout test actually validates that the timeout is enforced within 2 seconds, or just that the error path is hit
- Flaky test risks — especially around timing-based tests for the degraded/slow Firestore path
- Tests you'd add before feeling confident deploying this to a production Cloud Run service
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: