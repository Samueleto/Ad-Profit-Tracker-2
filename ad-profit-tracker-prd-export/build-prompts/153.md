# Step 153 of 437: Extend update-profile endpoint for onboarding fields

================================================================================

**Category:** API Endpoints
**Flow:** feature_user-onboarding-flow

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We're not creating a new endpoint — we're extending the existing PATCH /api/auth/update-profile endpoint to accept the new onboarding fields. Here's exactly what needs to change.

Open the existing update-profile route handler (likely at app/api/auth/update-profile/route.ts or pages/api/auth/update-profile.ts). It already handles things like displayName. We need to add support for three new fields in the request body: onboardingCompletedAt, onboardingSkipped, and preferences (which contains timezone and defaultDateRange).

The process should work like this: First, verify the Firebase ID token from the Authorization header using the Admin SDK's verifyIdToken() — this gives you the uid. Second, expand the whitelist of allowed fields to include onboardingCompletedAt, onboardingSkipped, and preferences.timezone and preferences.defaultDateRange. Reject any fields not on the whitelist with a 400 error. Third, if onboardingCompletedAt is present in the request body and its value is the string 'serverTimestamp', replace it with FieldValue.serverTimestamp() before writing to Firestore — the client should never be able to forge a specific timestamp. Fourth, if preferences is present, validate that timezone is a non-empty string and that defaultDateRange is one of the three allowed values ('last7days', 'last30days', 'thisMonth') — return 400 if either is invalid. Fifth, do a merge-update on the users/{uid} document using the Admin Firestore SDK. Sixth, read back the updated document and return it as JSON with a 200 status.

The response shape should include: uid, displayName, preferences (the full preferences map), onboardingCompletedAt, onboardingSkipped, and lastLoginAt.

Make sure the endpoint handles these error cases: 400 for unknown fields or invalid timezone/defaultDateRange values, 401 for a missing or invalid ID token, 404 if the users/{uid} document doesn't exist, and 500 for any Firestore write failure.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure the PATCH /api/auth/update-profile endpoint handles the new onboarding fields correctly.

Confirm that:
- [ ] The endpoint accepts onboardingCompletedAt, onboardingSkipped, and preferences in the request body
- [ ] Sending onboardingCompletedAt: 'serverTimestamp' writes a real Firestore server timestamp (not the string)
- [ ] Sending an invalid defaultDateRange (e.g. 'last90days') returns 400
- [ ] Sending an unknown field returns 400
- [ ] A request without a valid Authorization header returns 401
- [ ] A request for a non-existent user document returns 404
- [ ] A successful update returns 200 with uid, displayName, preferences, onboardingCompletedAt, onboardingSkipped, and lastLoginAt
- [ ] The endpoint only updates the authenticated user's own document

Test the actual flow: Using a valid Firebase ID token, send PATCH /api/auth/update-profile with { preferences: { timezone: 'America/New_York', defaultDateRange: 'last7days' }, displayName: 'Test User' } — should return 200 with the updated fields. Then send { onboardingCompletedAt: 'serverTimestamp', onboardingSkipped: false } — should return 200 with a real timestamp in onboardingCompletedAt. Then try { preferences: { defaultDateRange: 'last90days' } } — should return 400. Then try without an auth header — should return 401.

Then give me your honest advice on:
- Whether the server-side serverTimestamp() conversion is bulletproof (can the client bypass it?)
- Any field validation gaps that could let bad data into Firestore
- Whether the merge-update approach could accidentally overwrite existing preferences fields
- Performance concerns and anything you'd harden before real users hit this endpoint
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: