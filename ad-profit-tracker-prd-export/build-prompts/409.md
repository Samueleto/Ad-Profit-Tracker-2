# Step 409 of 437: Test the manual refresh feature end-to-end

================================================================================

**Category:** Testing
**Flow:** feature_manual-data-refresh-trigger

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

Let's add tests to make sure the ManualRefreshPanel and its underlying hook work correctly and don't break under edge cases.

**Unit tests for `useManualRefresh` hook:**
- On mount, it calls GET /api/scheduled/sync-status and populates per-network status
- `triggerAll()` calls POST /api/sync/manual with no networkId and adds all networks to the triggered set
- `triggerNetwork('exoclick')` calls POST /api/sync/manual with networkId='exoclick'
- Every API call includes an Authorization: Bearer header with a Firebase token
- Polling starts after a trigger and fires every 3 seconds
- Polling stops when all triggered networks reach terminal status
- Polling stops after 60 seconds regardless of status
- Polling interval is cleared on unmount
- A 429 response sets a rate limit countdown from the Retry-After header
- A 429 with no Retry-After header defaults to 60 seconds
- A 401 response triggers getIdToken(true); if that fails, calls the redirect handler
- 3 consecutive polling failures trigger the 'Status may be outdated' state

**Component tests for ManualRefreshPanel:**
- Renders skeleton rows while isInitialLoading is true
- Renders all four network rows (ExoClick, RollerAds, Zeydoo, Propush) when loaded
- Shows the correct status badge color for each status value
- 'Refresh All Data Now' button shows a confirmation tooltip on first click
- After confirmation, the button shows 'Syncing...' and is disabled
- A per-network Refresh button disables and shows spinner when that network is in-progress
- Empty state renders with a link to /settings when no networks are configured
- Inline error banner renders with a Retry button on 500
- 'Access Denied' banner renders on 403
- Rate limit countdown renders and decrements

**Component tests for SyncHistoryDrawer:**
- Renders a list of sync events with status, rowsFetched, latencyMs, and timestamp
- Shows a loading state while history is fetching
- Shows an error state with Retry button if the fetch fails
- Closes when the X button is clicked

**Integration test (full user flow):**
- Load /dashboard → ManualRefreshPanel appears → network rows populate → click 'Refresh All Data Now' → confirm → button shows 'Syncing...' → polling updates badges → all networks reach terminal status → polling stops → buttons re-enable → click 'View Sync History' → drawer opens with 10 events per network → close drawer → click a per-network Refresh → only that row shows spinner → that network reaches terminal → its button re-enables.

Use mocked fetch responses for all API calls in unit and component tests. Use a real (test environment) Firebase project for the integration test.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure all tests pass and cover the important paths.

Confirm that:
- [ ] All useManualRefresh unit tests pass (mount, trigger, polling, rate limit, 401, 403, polling failure)
- [ ] All ManualRefreshPanel component tests pass (skeleton, rows, badges, confirmation, disabled states, empty, error, rate limit)
- [ ] All SyncHistoryDrawer component tests pass (list, loading, error, close)
- [ ] The integration test passes end-to-end
- [ ] Polling interval cleanup is verified (no memory leak warnings in tests)
- [ ] No tests rely on real timers — use fake timers for polling and countdown intervals

Test the actual flow: Run the full test suite with `npm test`. Then run the integration test against a real Firebase test environment: open /dashboard → trigger a full sync → watch badges update → open history drawer → close it → trigger a per-network sync → verify only that row updates. All steps should complete without errors or console warnings.

Then give me your honest advice on:
- What's not covered by these tests that could break in production (especially around polling edge cases)
- Whether the integration test is reliable or likely to be flaky (timing, Firebase latency)
- Any test isolation issues (shared state between tests, timer leaks)
- Tests you'd add before feeling confident shipping this to real users
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: