# Step 12 of 437: Set up folder structure for manual refresh

================================================================================

**Category:** Project Scaffolding
**Flow:** feature_manual-data-refresh-trigger

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We're building a manual data refresh feature for an ad network dashboard. For this step, set up the folder structure so everything has a home before we start writing real code.

On the frontend, create a feature area under something like `src/features/manual-refresh/` (or wherever the project keeps feature-level code). Inside it, add folders for: `components` (we'll build the ManualRefreshPanel and its sub-pieces here), and `hooks` (for the polling logic and rate-limit state). We'll also need a `lib/sync/` folder at the project root (or wherever shared server utilities live) — this is where the `syncNetworkForUser` helper will live, shared between the manual trigger and any scheduled sync jobs.

On the backend, create a route file for `POST /api/sync/manual`. The project already has `/api/scheduled/` routes (for `sync-status` and `sync-history` which this feature calls but doesn't own), so just make sure the new manual sync route sits alongside the existing API structure — probably `src/app/api/sync/manual/route.ts` if this is Next.js App Router, or `src/pages/api/sync/manual.ts` for Pages Router.

Also confirm there's a place for rate-limiting utilities — if the project already has a shared rate-limit helper (likely, since other sync endpoints use it), note where it lives so we can reuse it. If not, create a `lib/rate-limit.ts` placeholder.

The goal here is just clean structure — no logic yet. Make sure the project still builds after adding these empty folders and placeholder files.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure the folder structure is set up correctly.

Confirm that:
- [ ] There's a `manual-refresh` feature folder with `components/` and `hooks/` subfolders
- [ ] There's a `lib/sync/` folder with a placeholder `syncNetworkForUser.ts` file
- [ ] The API route file for `POST /api/sync/manual` exists (even if empty)
- [ ] The project still builds without errors after adding these files
- [ ] The rate-limit utility location is identified (existing or new placeholder)

Test the actual flow: Run `npm run build` or `npm run dev` — the app should start without any import or file-not-found errors.

Then give me your honest advice on:
- Whether this folder structure fits naturally with how the rest of the project is organized
- If there's a better place for the `syncNetworkForUser` helper given the existing codebase layout
- Any naming conventions in the project I should follow
- Anything you'd reorganize before we start building
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: