# Step 30 of 437: Set up folder structure for mobile responsive components

================================================================================

**Category:** Project Scaffolding
**Flow:** feature_mobile-responsive-design

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We're building a mobile-responsive navigation and layout system for a Next.js + Firebase app. For this step, set up the folder structure that will house all the responsive components we're about to build.

Create a components folder (or extend the existing one) with a dedicated area for mobile navigation and layout pieces. We'll need homes for: MobileNavigationDrawer, MobileBottomNavBar, ResponsiveAppLayout, and MobileDataTableWrapper. If the project already has a components/ directory, add a mobile/ or layout/ subfolder to keep these organized and separate from existing UI components.

Also create a hooks/ folder (or extend the existing one) for a useMediaQuery hook — this will be a small utility that tells components whether we're on a mobile or desktop viewport. We'll use it to conditionally render interactive components like the drawer open/close state.

Finally, make sure there's a place for the (app) route group layout file (layout.tsx) if it doesn't already exist. The ResponsiveAppLayout will live here and wrap every protected page — FinancialMetricsDashboardPage, SettingsPage, OnboardingWizardPage, and all dashboard sections — so they all get responsive nav automatically.

The project should still build cleanly after this step — we're just creating empty folders and placeholder files.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure the folder structure is set up correctly.

Confirm that:
- [ ] There's a folder for mobile/layout components (mobile/ or layout/ under components/)
- [ ] There's a hooks/ folder with a placeholder for useMediaQuery
- [ ] The (app) route group has a layout.tsx file (even if empty)
- [ ] The project still builds without errors after adding these folders and files

Test the actual flow: Run the dev server and make sure nothing is broken — this is scaffolding only.

Then give me your honest advice on:
- Whether this folder structure fits the existing project conventions
- If there's a better place to put mobile-specific components given how the project is already organized
- Any naming conflicts with existing files or folders
- Anything you'd reorganize before we start building the actual components
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: