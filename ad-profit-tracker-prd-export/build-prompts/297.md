# Step 297 of 437: Wire cache invalidation and status endpoints to the UI

================================================================================

**Category:** Feature Wiring & Integration
**Flow:** feature_data-caching-optimization

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We're building the client-side integration for the three cache management API endpoints: `POST /api/cache/invalidate`, `GET /api/cache/status`, and `POST /api/cache/warm`.

Start by creating a `cacheApi` service module that wraps all three endpoints. Each function should get the current user's Firebase ID token and attach it as a Bearer token in the Authorization header before making the fetch call. The `invalidate` function should accept a scope (`'all'`, `'network'`, or `'endpoint'`) and an optional array of keys. The `getStatus` function takes no arguments and returns the stats object. The `warm` function is internal-only and uses the `x-internal-secret` header instead of a user token — include it for completeness but make it clear in comments it's not called from user-facing UI.

Next, create a `useCacheStatus` hook that calls `GET /api/cache/status` using SWR with a reasonable polling interval (something like every 30 seconds) so the stats stay reasonably fresh. It should expose the hits, misses, keyCount, size, and keys array, plus loading and error states.

Create a `useCacheInvalidation` hook that wraps the `POST /api/cache/invalidate` call. It should manage a loading state while the request is in flight, handle the 429 rate limit response specifically (surface a clear message like 'You've invalidated the cache too many times this hour — try again later'), and on success trigger a revalidation of the `useCacheStatus` SWR key so the stats refresh immediately.

These hooks should be ready to drop into any settings panel or admin UI that wants to show cache stats or let users manually flush their cache. Wire the `useCachedNetworkData` hook from the state management step into at least one existing data-fetching call in the app so the per-network SWR config is actually being used in practice.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure the cache API wiring is working end-to-end.

Confirm that:
- [ ] `cacheApi.getStatus()` attaches a valid Firebase ID token and returns the stats object from `GET /api/cache/status`
- [ ] `cacheApi.invalidate({ scope: 'all' })` calls `POST /api/cache/invalidate` with the correct body and auth header
- [ ] `useCacheStatus` polls every ~30 seconds and exposes hits, misses, keyCount, size, and keys
- [ ] `useCacheInvalidation` shows a loading state while the request is in flight
- [ ] After a successful invalidation, `useCacheStatus` refreshes automatically
- [ ] A 429 response from the invalidate endpoint surfaces a human-readable rate limit message
- [ ] The `x-internal-secret` header is used for `POST /api/cache/warm` and the Firebase token is NOT sent for that call
- [ ] At least one existing data-fetching hook in the app uses `useCachedNetworkData` with a real network ID

Test the actual flow: Sign in as a user → call `useCacheStatus` → see stats populate. Trigger `useCacheInvalidation` with scope `'network'` and a network key → see loading state → see success → confirm `useCacheStatus` data refreshes. Call invalidate 16 times in quick succession → confirm the 16th returns a rate limit error with a friendly message.

Then give me your honest advice on:
- Whether the Firebase token refresh is handled correctly (tokens expire after 1 hour)
- Performance concerns with polling `GET /api/cache/status` every 30 seconds across many open tabs
- Edge cases — what if the user signs out mid-request?
- Anything that would break in a production environment with real traffic
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: