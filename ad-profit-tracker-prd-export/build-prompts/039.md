# Step 39 of 437: Set up folder structure for Help Center

================================================================================

**Category:** Project Scaffolding
**Flow:** feature_help-documentation-center

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We're building a Help & Documentation Center for a Next.js + Firebase app. For this step, set up the folder structure so everything has a home before we start writing real code.

Create a help feature area under src/features/help/ with subfolders for: components (we'll build reusable pieces like ArticleRow, CategoryCard, TableOfContents, FeedbackWidget, and PopularArticlesSidebar), hooks (for data fetching logic like useHelpArticles, useHelpSearch, and useArticle), and types (for the HelpArticle TypeScript interface and related types).

For the Next.js pages, create the route files under src/app/(app)/help/ — we'll need a page.tsx for HelpCenterPage at /help, and a [articleId]/page.tsx for HelpArticlePage at /help/[articleId]. These live inside the (app) route group since they're protected routes.

On the backend side, create API route folders under src/app/api/help/ with subfolders for: articles/ (which will handle GET /api/help/articles), articles/[articleId]/ (for GET /api/help/articles/:articleId), articles/[articleId]/view/ (for PATCH /api/help/articles/:articleId/view), search/ (for GET /api/help/search), and feedback/ (for POST /api/help/feedback).

Also create a src/lib/helpSearch.ts utility file placeholder — we'll put the in-memory search scoring logic there so it stays out of the route handler.

This feature has two pages and five endpoints, so keeping things organized from the start will save headaches later.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure the folder structure is set up correctly.

Confirm that:
- [ ] src/features/help/ exists with components/, hooks/, and types/ subfolders
- [ ] src/app/(app)/help/page.tsx exists (HelpCenterPage route)
- [ ] src/app/(app)/help/[articleId]/page.tsx exists (HelpArticlePage route)
- [ ] All five API route files exist under src/app/api/help/
- [ ] src/lib/helpSearch.ts exists as a placeholder
- [ ] The project still compiles without errors after adding these files

Test the actual flow: Run the dev server and navigate to /help — it should resolve without a 404 (even if the page is empty). Navigate to /help/test-article — same thing.

Then give me your honest advice on:
- Whether this folder structure fits the existing project conventions
- If the (app) route group is the right place for protected routes in this project
- Anything you'd reorganize before we start building
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: