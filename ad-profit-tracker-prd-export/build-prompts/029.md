# Step 29 of 437: Set up folder structure for theming feature

================================================================================

**Category:** Project Scaffolding
**Flow:** feature_dark-mode-ui-theming

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We're building a dark mode and UI theming feature for a Next.js + Firebase app. For this step, I need you to set up the folder structure so everything has a home before we start building.

Create a theming feature area inside the existing project. You'll want a folder for theme-related components (we'll build a ThemeSettingsSection that lives inside the existing SettingsPage), a folder for any theme utility helpers (like color validation and preset swatch definitions), and a place for theme-related constants (the 8 preset accent colors: indigo, emerald, rose, amber, sky, violet, orange, and slate — store their hex values here so they're easy to reference everywhere).

Since ThemeSettingsSection integrates into the existing SettingsPage rather than being its own route, you don't need a new pages folder — just a components folder under something like src/features/theming/ or src/components/settings/theming/ depending on how the project is already organized.

Also make sure there's a place for the next-themes provider setup — we'll need to wrap the app with it in the next step, so identify where the root layout or _app file lives and note it for the SDK setup step.

The project should still build cleanly after this step with no new errors.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure the folder structure is set up correctly.

Confirm that:
- [ ] There's a theming feature folder with subfolders for components and utils
- [ ] There's a constants file that defines the 8 preset accent colors (indigo, emerald, rose, amber, sky, violet, orange, slate) with their hex values
- [ ] The project still builds without errors after adding these folders
- [ ] The root layout or _app file has been identified as the place where next-themes will be wired in

Test the actual flow: Run a build — this is just scaffolding, so it should pass cleanly with no new errors or warnings.

Then give me your honest advice on:
- Whether this folder structure fits naturally with how the rest of the project is organized
- If there's a better place to put theming constants given the existing project layout
- Anything you'd reorganize before we start building on top of this
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: