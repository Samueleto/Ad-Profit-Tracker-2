# Step 289 of 437: Integrate ManualRefreshPanel into the dashboard

================================================================================

**Category:** Feature Wiring & Integration
**Flow:** feature_manual-data-refresh-trigger

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

Now we need to wire the ManualRefreshPanel into the existing dashboard at /dashboard so it actually shows up and connects to everything.

Find the existing dashboard layout (likely a layout file or the main /dashboard page component) and import ManualRefreshPanel into it. Place it in the sidebar or top action bar area — wherever the design calls for it. It should be visible on every dashboard view without requiring any navigation.

Connect the panel to the `useManualRefresh` hook — the hook should be instantiated at the panel level (not at the page level) so its state is scoped to the panel and doesn't affect other dashboard sections.

Wire up the SyncHistoryDrawer so it renders at the page level (not inside the panel card) so it can slide over the full viewport. Pass the `isOpen`, `historyData`, `isLoading`, and `onClose` values from the hook into the drawer. The drawer should render as a fixed overlay on the right side of the screen.

Make sure the 'No networks configured' empty state links correctly to /settings — use the app's existing router (Next.js Link component) rather than a plain anchor tag.

Also wire up the 'Session expired' redirect: if a Firebase token refresh fails (covered in error_handling), the hook should redirect to / and trigger a toast notification saying 'Session expired'. Use whatever toast library the dashboard already uses — don't introduce a new one.

Finally, confirm that the panel doesn't break the existing dashboard layout on mobile or narrow viewports. The dense list layout should stack gracefully if the sidebar is narrow.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure the ManualRefreshPanel is properly wired into the dashboard.

Confirm that:
- [ ] The ManualRefreshPanel is visible on the /dashboard page without any extra navigation
- [ ] The panel sits in the sidebar or top action bar as designed (not as a full-width section)
- [ ] The SyncHistoryDrawer renders as a full-viewport slide-over overlay, not inside the panel card
- [ ] The 'No networks configured' link navigates to /settings using Next.js routing (no full page reload)
- [ ] The dashboard layout doesn't break on narrow viewports with the panel added
- [ ] The panel's state is scoped to itself — triggering a sync doesn't cause unrelated dashboard sections to re-render
- [ ] A session expiry redirects to / with a 'Session expired' toast using the existing toast system

Test the actual flow: Open /dashboard → see the ManualRefreshPanel in the sidebar/action bar → trigger a sync → see the drawer open when clicking 'View Sync History' → confirm the drawer overlays the full page → close the drawer → click the 'No networks configured' link (if applicable) → land on /settings without a full reload → resize the browser to a narrow viewport → confirm the panel layout doesn't overflow or break.

Then give me your honest advice on:
- Whether placing the panel in the sidebar/action bar is the right UX choice for this dashboard
- Any z-index or stacking context issues the drawer might cause with other dashboard overlays
- Whether scoping the hook to the panel level is the right call or if it should be lifted higher
- What a first-time user would find confusing about where the refresh controls are
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: