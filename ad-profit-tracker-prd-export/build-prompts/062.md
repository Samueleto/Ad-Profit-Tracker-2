# Step 62 of 437: Install and configure recharts for geo charts

================================================================================

**Category:** SDK Setup & Configuration
**Flow:** feature_geographic-performance-breakdown

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We need recharts set up so the GeoCountryDrilldownModal can render its daily trend mini-chart.

Install the recharts package if it isn't already in the project. This is the only new package this feature needs — everything else (Firebase Auth, fetch utilities) should already be wired up from the existing dashboard.

Once installed, do a quick sanity check: create a minimal recharts LineChart in a throwaway file to confirm it renders without SSR issues in Next.js. Recharts uses browser APIs and can cause 'window is not defined' errors if rendered server-side. If the project uses any server components that might accidentally import recharts, make sure the geo-breakdown components that use recharts are marked with 'use client' at the top.

Also confirm that the existing Firebase Auth setup (getIdToken, onAuthStateChanged) is accessible from the dashboard feature area — the geo breakdown components will need to attach a Bearer token to every API call they make to GET /api/stats/geo-breakdown, GET /api/roi/breakdown, and GET /api/stats/snapshot. If there's already a shared fetch utility that handles auth headers, note where it lives so we can use it in the hooks we build next.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure recharts is installed and won't cause SSR problems.

Confirm that:
- [ ] recharts is listed in package.json dependencies
- [ ] A simple LineChart renders in the browser without errors
- [ ] No 'window is not defined' or SSR errors appear in the Next.js build output
- [ ] Any component that imports recharts has 'use client' at the top
- [ ] The existing Firebase Auth token utility is accessible from the geo-breakdown feature folder

Test the actual flow: Install the package, drop a minimal LineChart into a client component, run the dev server, and confirm it renders. Then run a production build and check for SSR warnings.

Then give me your honest advice on:
- Whether recharts is the right choice here or if a lighter library would be better for a 200px mini-chart
- Any bundle size concerns from adding recharts
- SSR gotchas specific to this Next.js setup that I should watch out for
- Whether the existing auth token pattern will work cleanly for the three API calls this feature makes
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: