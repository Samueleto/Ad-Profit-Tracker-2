# Step 9 of 437: Set up folder structure for Zeydoo integration

================================================================================

**Category:** Project Scaffolding
**Flow:** feature_zeydoo-integration

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We're building a Zeydoo ad network integration that syncs CPA statistics into our platform. For this step, set up the folder structure so everything has a clean home before we start writing logic.

On the API side, create the route handlers under `src/app/api/networks/zeydoo/` (or `pages/api/networks/zeydoo/` if the project uses the Pages Router). Inside that folder we'll need files for: `sync` (the manual sync endpoint), `scheduled-sync` (the internal cron endpoint), `stats` (the main stats query), `stats/by-country` (the country breakdown), `stats/latest` (the most recent day summary), and `raw-response` (the API Explorer data).

On the server utilities side, create a shared folder — something like `src/lib/networks/zeydoo/` — where we'll put the Zeydoo API client, the response parser, and any helper functions that multiple endpoints share (like the batch upsert logic). This keeps the route handlers thin.

Also create a `src/lib/firebase/` folder if it doesn't already exist — we'll put the Firebase Admin SDK initialization there so it's shared across all endpoints.

The project should still build cleanly after this step with no new errors.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure the folder structure is set up correctly.

Confirm that:
- [ ] All six Zeydoo API route files exist under the correct path
- [ ] The shared Zeydoo library folder exists for helpers and the API client
- [ ] The Firebase lib folder exists for Admin SDK setup
- [ ] The project still builds without errors after adding these files

Test the actual flow: Run a build (`next build` or `npm run build`) and confirm it completes without errors. This is scaffolding only — no logic yet.

Then give me your honest advice on:
- Whether this folder structure fits well with the existing project layout
- If the App Router vs Pages Router choice is consistent with the rest of the project
- Anything you'd reorganize before we start adding real code
- Whether there are any naming conventions in the existing codebase we should follow
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: