# Step 40 of 437: Set up folder structure for deployment pipeline

================================================================================

**Category:** Project Scaffolding
**Flow:** feature_cloud-run-deployment-pipeline

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We're building a Cloud Run deployment pipeline for a Next.js + Firebase app. For this step, I need you to set up the folder structure for the three API endpoints we'll be building.

Create the following API route files under the Next.js `src/app/api/` (or `pages/api/` if using the Pages Router — match whatever convention the project already uses):
- `api/health/` — for the public liveness probe endpoint GET /api/health
- `api/ready/` — for the public readiness probe endpoint GET /api/ready
- `api/deployment/status/` — for the authenticated deployment metadata endpoint GET /api/deployment/status

Also create a shared utilities folder at `src/lib/deployment/` (or `lib/deployment/` depending on the project structure). We'll put helper functions here — things like reading environment variables safely, checking Firebase Admin initialization, and formatting the health/readiness response shapes.

Finally, make sure there's a place for environment variable documentation. Create or update a `.env.example` file at the project root that lists all the environment variables this feature depends on: `CLOUD_RUN_SERVICE_NAME`, `CLOUD_RUN_REGION`, `K_REVISION`, `K_SERVICE`, `K_CONFIGURATION`, `DEPLOYMENT_ENV`, `FIREBASE_PROJECT_ID`, `INTERNAL_SYNC_SECRET`, `EMAIL_PROVIDER_API_KEY`, `EMAIL_FROM_ADDRESS`. Don't put real values in this file — just the keys with placeholder comments explaining what each one is for.

This is scaffolding only — no logic yet, just the folder and file structure so everything has a home before we start building.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure the folder structure is set up correctly.

Confirm that:
- [ ] The `api/health/` route file exists
- [ ] The `api/ready/` route file exists
- [ ] The `api/deployment/status/` route file exists
- [ ] The `src/lib/deployment/` utilities folder exists
- [ ] `.env.example` lists all required environment variable keys: `CLOUD_RUN_SERVICE_NAME`, `CLOUD_RUN_REGION`, `K_REVISION`, `K_SERVICE`, `K_CONFIGURATION`, `DEPLOYMENT_ENV`, `FIREBASE_PROJECT_ID`, `INTERNAL_SYNC_SECRET`, `EMAIL_PROVIDER_API_KEY`, `EMAIL_FROM_ADDRESS`
- [ ] The project still builds without errors after adding these files

Test the actual flow: Run a build (`next build` or `npm run build`) and confirm it completes without errors. This is scaffolding only — no logic yet.

Then give me your honest advice on:
- Whether this folder structure fits the existing project conventions
- If there's a better place to put the shared deployment utilities given how the rest of the project is organized
- Any naming conventions you'd change to be consistent with the rest of the codebase
- Anything you'd set up now to make the next steps easier
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: