# Step 416 of 437: Test rate limiting endpoints and hooks

================================================================================

**Category:** Testing
**Flow:** feature_api-rate-limiting-throttling

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We're building an API rate limiting and throttling system. For this step, I need you to add tests that cover the four API endpoints and the four client-side hooks.

For the API endpoints, write unit/integration tests using Jest (or whatever test runner the project uses):

`GET /api/rate-limits/status`: Test that it returns 401 without a token. Test that it returns 400 for an invalid `networkId`. Test the happy path — mock `admin.auth().verifyIdToken()` to return a valid uid, mock the limiter registry to return fake limiter state, mock Firestore to return fake `networkConfigs` docs, and assert the response shape matches the spec (all fields present, `isThrottled` computed correctly). Test that `isThrottled` is true when `reservoir == 0`. Test that `isThrottled` is true when `circuitBreakerOpen == true`.

`POST /api/rate-limits/reset`: Test 401 with no auth. Test 400 for invalid `networkId` and invalid `scope`. Test that a valid user call with scope `'outbound'` calls `limiter.updateSettings()` and writes an audit log. Test that a valid user call with scope `'user-quota'` clears the sliding window counter. Test that scope `'both'` does both. Test that the 10/hour rate limit returns 429 on the 11th call. Test that a valid `x-internal-secret` call succeeds without rate limiting and without writing an audit log. Test that the endpoint never resets circuit breaker state.

`GET /api/rate-limits/config`: Test 401 without token. Test the happy path — mock the config files and assert all four networks and all quota configs are returned with correct fields including `description`.

`GET /api/rate-limits/violations`: Test 401 without token. Test 400 for invalid `networkId` and out-of-range `limit`. Test the happy path with mocked Firestore — assert pagination works (pass a cursor, get the next page). Test that the `endpoint` filter works. Test that the query always scopes to the authenticated user's uid.

For the client-side hooks, write React Testing Library tests:
- `useRateLimitStatus`: mock fetch, assert loading state then populated state. Assert polling fires again after the interval. Assert it stops polling on unmount.
- `useRateLimitReset`: mock fetch, assert the `reset` function sends the right body. Assert it calls `onSuccess` after a 200. Assert it surfaces `retryAfterSeconds` on a 429.
- `useRateLimitViolations`: assert `loadMore` appends results. Assert `hasMore` is false when the last page is returned.

Put API tests in `__tests__/api/rate-limits/` and hook tests in `__tests__/features/rate-limits/hooks/`.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure all the tests pass and cover the important paths.

Confirm that:
- [ ] All `GET /api/rate-limits/status` tests pass (401, 400, happy path, isThrottled logic)
- [ ] All `POST /api/rate-limits/reset` tests pass (401, 400, 429, user flow, internal-secret flow, no circuit breaker reset)
- [ ] All `GET /api/rate-limits/config` tests pass (401, happy path with descriptions)
- [ ] All `GET /api/rate-limits/violations` tests pass (401, 400, pagination, endpoint filter, user scoping)
- [ ] `useRateLimitStatus` hook tests pass (loading state, populated state, polling, unmount cleanup)
- [ ] `useRateLimitReset` hook tests pass (correct request body, onSuccess callback, 429 handling)
- [ ] `useRateLimitViolations` hook tests pass (loadMore appends, hasMore false on last page)
- [ ] No tests are flaky (no timing-dependent assertions without proper async handling)

Test the actual flow: Run the full test suite with `jest --coverage`. All tests should pass. Check coverage — the four API handlers and four hooks should have meaningful coverage on the happy path and the main error paths. Then manually test the most critical path: call `POST /api/rate-limits/reset` as a user 11 times — the 11th should 429. Call it once with the internal secret — should succeed without counting against the limit.

Then give me your honest advice on:
- What's not covered by these tests that could break in production (especially the dual-auth logic and the audit log failure path)
- Whether the hook tests are testing real behavior or just mocking everything away
- Any flaky test risks (timing, shared in-memory state between tests, Firebase mock leaks)
- Tests you'd add before feeling confident about a release
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: