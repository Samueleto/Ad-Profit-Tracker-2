# Step 101 of 437: Set up Firestore benchmarks collection and security rules

================================================================================

**Category:** Database Setup
**Flow:** feature_performance-benchmarking

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We need to set up the `benchmarks` Firestore collection with the right security rules and indexes before we build anything that reads or writes to it.

**Firestore Security Rules** — Add rules for the `benchmarks` collection:
- Any authenticated user can create a document, but only if the `userId` field in the document matches their own Firebase Auth UID (`request.auth.uid`)
- A user can only read their own document — `resource.data.userId == request.auth.uid`
- A user can only update their own document — same check
- Deletes should be soft-delete only: the rule should allow updates (to set a `deleted` flag) but block hard deletes
- The `createdAt` and `userId` fields must be immutable — on update, verify `request.resource.data.createdAt == resource.data.createdAt` and `request.resource.data.userId == resource.data.userId`

**Firestore Indexes** — The benchmark API routes query the `adStats` collection (which already exists) in two ways that need composite indexes:
- `adStats`: composite index on `userId ASC` + `date ASC` (for the current-period query in GET /api/benchmarks/performance)
- `adStats`: composite index on `userId ASC` + `networkId ASC` + `date ASC` (for the network-filtered query)
- `benchmarks`: single-field index on `userId` is sufficient since we do a simple equality filter

Add these to your `firestore.indexes.json` file and deploy them. Also add the security rules to `firestore.rules`.

**System Defaults Config** — Create `lib/benchmarks/defaults.ts` that exports the server-side system default benchmark values: `{ roi: 50, ctr: 2.5, cpm: 1.5, revenue: 1000, cost: 500, impressions: 100000, clicks: 2500 }`. Also export the metric units map (e.g., roi → '%', cpm → '$', revenue → '$', impressions → 'count'). This file will be imported by all three API routes so the defaults are defined in one place.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure the database is properly configured for the benchmarking feature.

Confirm that:
- [ ] Firestore security rules allow a user to create/read/update only their own `benchmarks` document
- [ ] The rules block hard deletes on `benchmarks`
- [ ] The rules prevent `createdAt` and `userId` from being changed on update
- [ ] Composite indexes on `adStats` for `userId+date` and `userId+networkId+date` are deployed
- [ ] `lib/benchmarks/defaults.ts` exports the system defaults object with all 7 metrics
- [ ] The metric units map is exported from the same file
- [ ] Rules deploy without errors (`firebase deploy --only firestore`)

Test the actual flow: Use the Firestore security rules simulator — try reading another user's benchmarks document (should be denied). Try updating `userId` on an existing document (should be denied). Try creating a document with a mismatched `userId` (should be denied). Try a valid read and write as the correct user (should succeed).

Then give me your honest advice on:
- Any gaps in the security rules that could let a user access another user's data
- Whether the indexes are sufficient for the queries we'll run in the API routes
- Edge cases in the immutability rules (what if `createdAt` is missing on an old document?)
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: