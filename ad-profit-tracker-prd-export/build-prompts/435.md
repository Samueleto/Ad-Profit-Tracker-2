# Step 435 of 437: Test email alerts preferences end-to-end

================================================================================

**Category:** Testing
**Flow:** feature_email-alerts-notifications

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

Let's add tests to make sure the email alerts feature works correctly and doesn't break.

**Unit tests for useEmailAlertPreferences hook:**
- Fetches GET /api/notifications/preferences on mount and populates all 9 toggle states, alertDeliveryEmail, and lastTestEmailSentAt
- toggleEmailEnabled optimistically updates state, fires PATCH, and rolls back on failure
- setAllEmailEnabled(false) updates all 9 rows and fires the correct PATCH payload
- Master toggle is true when all 9 are enabled, false when all disabled, indeterminate when mixed
- sendTestAlert sets isSendingTest=true during the call and updates lastTestEmailSentAt on success
- saveDeliveryEmail calls PATCH with alertDeliveryEmail and tracks saving state
- 401 response triggers token refresh logic
- 429 response on sendTestAlert sets the correct testError message

**Unit tests for error handling:**
- Each error code (401, 403, 404, 500) on GET /api/notifications/preferences maps to the correct UI state
- PATCH failure on a toggle rolls back the optimistic update
- 400 on POST /api/email/send-test maps to the 'Add a delivery email' message
- 502 on POST /api/email/send-test maps to the provider unavailable message

**Component tests for EmailAlertPreferencesSection:**
- Shows 9 shimmer rows while loading
- Renders all 9 toggle rows with correct labels, descriptions, and severity badges after load
- 'Critical Alerts' group header appears before Sync Failure, Reconciliation Anomaly, Circuit Breaker Opened
- 'Informational' group header appears before the remaining 6 types
- Flipping a toggle calls toggleEmailEnabled with the correct type and value
- Master toggle off disables all 9 rows visually
- 'Send Test Alert' button is disabled while isSendingTest is true
- 'Last test sent' timestamp renders correctly from lastTestEmailSentAt
- Inline error banner appears on 500 fetch error with a working Retry button
- 'Access Denied' message appears on 403 (no redirect)

**Integration test — full preferences flow:**
Open SettingsPage → navigate to General Preferences → Email Alerts section loads → toggle Sync Failure email on → PATCH fires with correct payload → 'Saved ✓' flashes → toggle master off → all 9 go off → change delivery email → save → confirmation appears → send test email → toast shows recipient address → expand Email Delivery History → test log entry appears.

**API endpoint tests (using Firebase emulator or mocked admin SDK):**
- POST /api/email/send-test: valid token → 200 with sent=true; no token → 401; 4th call in an hour → 429; no email configured → 400
- POST /api/email/send-alert: valid token → 200; x-internal-secret → 200; emailEnabled=false → 200 with sent=false; invalid type → 400
- POST /api/email/send-summary: valid secret → 200; Firebase token only → 401
- GET /api/email/log: valid token → 200 with user's logs only; no token → 401

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure all tests pass and cover the important paths.

Confirm that:
- [ ] All useEmailAlertPreferences unit tests pass (fetch, toggle, master toggle, test send, rollback)
- [ ] All error handling unit tests pass (each error code maps to the right state)
- [ ] All EmailAlertPreferencesSection component tests pass (loading, all 9 rows, group headers, master toggle, test button, error states)
- [ ] The integration test completes the full preferences flow end-to-end
- [ ] POST /api/email/send-test endpoint tests pass (valid, no auth, rate limit, no email)
- [ ] POST /api/email/send-alert endpoint tests pass (user auth, internal secret, disabled preference, invalid type)
- [ ] POST /api/email/send-summary endpoint tests pass (valid secret, user token rejected)
- [ ] GET /api/email/log endpoint tests pass (scoped to user, no auth rejected)
- [ ] No tests are flaky (no timing-dependent assertions without proper async handling)

Test the actual
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: