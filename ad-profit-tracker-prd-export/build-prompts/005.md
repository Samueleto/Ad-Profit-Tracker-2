# Step 5 of 437: Set up folder structure for network configuration

================================================================================

**Category:** Project Scaffolding
**Flow:** feature_network-configuration-management

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We're building a network configuration management feature. For this step, create a new feature area under src/features/networkConfig/ with folders for: components (for UI elements like NetworkCard, AdvancedSettings), hooks (for custom React hooks like useNetworkConfig), types (for TypeScript interfaces), and api (for server-side route handlers).

On the backend side, ensure there's a structure for Firebase Cloud Functions that will handle the API endpoints related to network configuration. This includes setting up directories for functions like config list, update, reorder, test connection, sync all, and reset.

This setup will help us keep the code organized as we build out the feature.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Confirm that:
- [ ] The networkConfig feature folder exists with components/, hooks/, types/, and api/ subfolders
- [ ] The Firebase functions directory for networkConfig is set up
- [ ] The project still builds without errors

Test: Run a build to ensure nothing is broken.
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: