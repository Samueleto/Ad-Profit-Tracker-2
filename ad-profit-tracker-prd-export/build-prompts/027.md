# Step 27 of 437: Set up folder structure for filtering feature

================================================================================

**Category:** Project Scaffolding
**Flow:** feature_data-filtering-search

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We're building a data filtering and search system that integrates into an existing financial metrics dashboard. For this step, set up the folder structure so everything has a home before we start writing real code.

On the frontend, create a feature folder for filters — something like `src/features/filters/` or `src/components/filters/` depending on how the rest of the project is organized. Inside it, create subfolders for: `components` (we'll build the FilterToolbar and its sub-controls here), `hooks` (custom hooks for filter state and API calls), and `store` (the Zustand slice extensions for filter state).

On the API side, create a folder at `src/app/api/filters/` (Next.js App Router convention). Inside it, create subfolders for each route segment we'll need: `options/`, `stats/`, `search/`, and `presets/`. The presets folder will also need a `[presetId]/` subfolder for the DELETE route.

Also create a `src/lib/filters/` folder for shared utilities — things like the ISO 3166-1 country name lookup map, metric config constants, and the node-cache instance that all filter endpoints will share.

The goal here is just clean scaffolding. No logic yet — just the folders and placeholder files so the project still builds cleanly.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure the folder structure is in place and nothing is broken.

Confirm that:
- [ ] The `src/features/filters/` folder exists with `components/`, `hooks/`, and `store/` subfolders
- [ ] The `src/app/api/filters/` folder exists with `options/`, `stats/`, `search/`, and `presets/` subfolders
- [ ] The `presets/[presetId]/` subfolder exists for the DELETE route
- [ ] The `src/lib/filters/` shared utilities folder exists
- [ ] The project still builds without errors after adding these folders

Test the actual flow: Run `npm run build` or `npm run dev` — the app should start cleanly with no import errors or missing module complaints.

Then give me your honest advice on:
- Whether this folder structure fits naturally with how the rest of the project is organized
- If there's a better place to put the Zustand store slice given the existing store setup
- Anything you'd reorganize before we start building on top of this
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: