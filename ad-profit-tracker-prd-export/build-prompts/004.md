# Step 4 of 437: Set up folder structure for user preferences

================================================================================

**Category:** Project Scaffolding
**Flow:** feature_user-settings-preferences

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We're building a User Settings & Preferences feature on top of an existing Next.js + Firebase app. For this step, I need you to set up the folder structure for everything we're about to build.

On the frontend, create a preferences feature area inside the existing settings section. We'll need a folder for the PreferencesCard Client Component (which lives under something like `src/features/settings/preferences/` or `src/components/settings/`). This component will be imported into the existing SettingsPage at `/settings`.

On the API side, create a folder for the two preferences endpoints under `src/app/api/settings/preferences/`. We'll need a single route file here that handles both GET and PATCH requests.

Also create a shared utilities folder (if one doesn't already exist) for things like timezone validation helpers and currency code lists — these will be used by the PATCH endpoint to validate incoming values.

Don't build any logic yet — just get the folders and placeholder files in place so the project still compiles cleanly.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure the folder structure is set up correctly.

Confirm that:
- [ ] There's a folder for the PreferencesCard component under the settings feature area
- [ ] There's an API route file at `src/app/api/settings/preferences/route.ts` (or equivalent path)
- [ ] There's a shared utilities/validation folder for timezone and currency helpers
- [ ] The project still builds without errors after adding these folders and placeholder files

Test the actual flow: Just run a build (`npm run build` or `next build`) — this is scaffolding only, so nothing should break.

Then give me your honest advice on:
- Whether this folder structure fits naturally with how the existing SettingsPage is organized
- If there's a better place to put the PreferencesCard given the project's current conventions
- Anything you'd reorganize before we start building real logic
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: