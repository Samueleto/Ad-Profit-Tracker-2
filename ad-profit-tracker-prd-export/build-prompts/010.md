# Step 10 of 437: Set up folder structure for Propush integration

================================================================================

**Category:** Project Scaffolding
**Flow:** feature_propush-integration

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We're building a Propush ad network integration that syncs publisher statistics into our app. For this step, set up the folder structure so everything has a clear home before we start writing logic.

On the API side, create a folder at `src/app/api/networks/propush/` (or `pages/api/networks/propush/` if using the Pages Router). Inside that, we'll need route files for: `sync`, `scheduled-sync`, `stats`, `stats/by-country`, `stats/latest`, and `raw-response`. Create those subfolders and placeholder route files now.

Create a shared server-side utilities folder at `src/lib/propush/` (or `lib/propush/`) for things like the Propush API client, crypto helpers, and rate limiting logic — we'll fill these in during later steps.

Also create a `src/lib/firebase/` folder if one doesn't already exist — this is where the Firebase Admin SDK initialization will live.

The goal here is just to get the skeleton in place so the project still builds cleanly and we have obvious places to put each piece as we build it out.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure the folder structure is set up correctly.

Confirm that:
- [ ] All six Propush API route files exist under `api/networks/propush/`
- [ ] The `lib/propush/` utility folder exists
- [ ] The `lib/firebase/` folder exists
- [ ] The project still builds without errors after adding these files

Test the actual flow: Run `npm run build` (or `next build`) and confirm there are no compilation errors. This is scaffolding only — nothing should be wired up yet.

Then give me your honest advice on:
- Whether this folder structure fits well with the existing project layout
- If there's a better place to put shared utilities given how the rest of the codebase is organized
- Anything you'd rename or reorganize before we start building
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: