# Step 105 of 437: Verify Firestore collections and indexes for PDF export

================================================================================

**Category:** Database Setup
**Flow:** feature_pdf-export-functionality

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

The PDF export endpoint reads from two Firestore collections — `adStats` and `auditLogs` — and also writes an audit log entry on successful export. Both collections should already exist from earlier features, but we need to make sure the right indexes are in place for the queries this endpoint runs.

Here's what the PDF endpoint queries:

**adStats collection:**
- Filter by `userId == uid` AND `date >= dateFrom` AND `date <= dateTo`
- This is a compound query on `userId` + `date` (range). If this index doesn't already exist from the Excel export feature, add it to `firestore.indexes.json` now.

**auditLogs collection:**
- Filter by `userId == uid` AND `createdAt >= dateFrom` AND `createdAt <= dateTo`
- Same pattern — compound index on `userId` + `createdAt` (range). Again, likely already exists, but verify.

**Writing audit logs:**
- The endpoint writes a new document to `auditLogs` after a successful PDF export with fields: `action`, `resourceType`, `resourceId`, `metadata` (object with exportType, sectionsIncluded, dateFrom, dateTo, orientation, paperSize, filename), `status`, and a timestamp. Make sure the auditLogs collection structure supports this shape.

Check `firestore.indexes.json` and confirm both compound indexes are declared. If they're missing, add them and deploy with `firebase deploy --only firestore:indexes`. Don't add duplicate indexes if they're already there from the Excel export setup.

Also double-check Firestore security rules: the PDF endpoint uses the Firebase Admin SDK server-side, so it bypasses client-side rules — but make sure no client-side rules accidentally expose these collections directly to unauthenticated users.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure the database is ready for the PDF export queries.

Confirm that:
- [ ] `firestore.indexes.json` has a compound index on `adStats` for `userId` + `date`
- [ ] `firestore.indexes.json` has a compound index on `auditLogs` for `userId` + `createdAt`
- [ ] Both indexes are deployed and show as 'Enabled' in the Firebase console
- [ ] The `auditLogs` collection can accept documents with the fields: action, resourceType, resourceId, metadata (object), status, createdAt
- [ ] Firestore security rules don't expose `adStats` or `auditLogs` to unauthenticated clients directly

Test the actual flow: In the Firebase console, manually run a query on `adStats` filtered by `userId` and a date range — it should complete without an 'index required' error. Do the same for `auditLogs` filtered by `userId` and `createdAt` range.

Then give me your honest advice on:
- Whether these indexes are sufficient for the query patterns in the PDF endpoint
- Any missing indexes that could cause slow queries at scale
- Whether the audit log write could cause any issues (missing fields, wrong types)
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: