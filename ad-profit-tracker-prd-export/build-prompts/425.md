# Step 425 of 437: Test the Zeydoo integration end-to-end

================================================================================

**Category:** Testing
**Flow:** feature_zeydoo-integration

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We're building a Zeydoo ad network integration. For this step, I need you to add tests covering the critical paths across all six endpoints and the key UI components.

**API endpoint unit tests:**

For POST /api/networks/zeydoo/sync — test that it rejects requests without a valid token (401), rejects inactive network configs (400), enforces the 90-day date range limit (400), calls the Zeydoo API with the correct parameters, batch-upserts records into adStats, updates networkConfigs with lastSyncedAt and lastSyncStatus='success', writes an audit log entry, and returns 502 when the Zeydoo API fails.

For POST /api/networks/zeydoo/scheduled-sync — test that it rejects requests without the x-internal-secret header (401), processes multiple users, continues when one user fails, and returns a summary with success/failure counts.

For GET /api/networks/zeydoo/stats — test that it returns only the authenticated user's data, correctly aggregates by 'day' vs 'total', and rejects date ranges over 90 days.

For GET /api/networks/zeydoo/stats/by-country — test that it groups by country correctly, sorts by revenue descending, calculates percentage shares, and respects the limit parameter.

For GET /api/networks/zeydoo/stats/latest — test that it returns 404 with code 'NO_DATA' when no records exist, and returns the correct aggregated summary when data exists.

For GET /api/networks/zeydoo/raw-response — test that it returns the rawResponse field from adStats documents and includes the schema definition.

**Hook unit tests:**

Test useZeydooSync: isSyncing is true during the request, false after. On success, syncVersion increments. On 429, error contains the rate limit reset time.

Test useZeydooLatest: 404 response sets data to null without setting error. Other errors set the error field.

**Integration test (full flow):**

Simulate a logged-in user: trigger a manual sync via ZeydooSyncControl → verify the POST /api/networks/zeydoo/sync call is made with the correct token → mock a successful Zeydoo API response → verify adStats documents are upserted → verify networkConfigs is updated → verify the stats panels refetch → verify the latest card shows updated data.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure all the Zeydoo tests pass and cover the important paths.

Confirm that:
- [ ] All six API endpoint tests pass
- [ ] POST /api/networks/zeydoo/sync test verifies the Zeydoo API is called with correct params
- [ ] POST /api/networks/zeydoo/sync test verifies adStats upsert and networkConfigs update
- [ ] POST /api/networks/zeydoo/scheduled-sync test verifies one user's failure doesn't stop others
- [ ] GET /api/networks/zeydoo/stats/latest test covers both the 404 and data-found cases
- [ ] useZeydooSync hook test verifies isSyncing state transitions
- [ ] useZeydooLatest hook test verifies 404 is treated as empty state, not error
- [ ] The integration test covers the full sync → refetch flow
- [ ] All tests mock Firebase Admin and the Zeydoo API (no real network calls)
- [ ] The full test suite passes with no failures

Test the actual flow: Run the full test suite. Then manually test the integration: log in → open the Zeydoo dashboard → trigger a sync → watch the stats panels refresh → change the date range → see the overview and top countries both update → open the raw explorer → fetch data for a specific date → see JSON output. Everything should work end-to-end with no console errors.

Then give me your honest advice on:
- What critical paths aren't covered by these tests that could break in production
- Whether the mocking strategy for Firebase Admin is reliable across all test cases
- Any flaky test risks (timing issues, shared state between tests, async race conditions)
- Tests you'd add before feeling confident about a production release
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: