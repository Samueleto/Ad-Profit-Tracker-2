# Step 36 of 437: Set up folder structure for email alerts

================================================================================

**Category:** Project Scaffolding
**Flow:** feature_email-alerts-notifications

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We're building an email alerts and notifications feature for an ad profit tracking app. For this step, set up the folder structure so everything has a home before we start writing real code.

On the backend (Firebase Cloud Functions or Next.js API routes), create a folder for the email-related endpoints. We'll have three new email endpoints here: `send-alert`, `send-summary`, `send-test`, `schedule-summary`, and `log`. We're also extending two existing endpoints — `GET /api/notifications/preferences` and `PATCH /api/notifications/preferences` — so note where those live and plan to modify them in place rather than creating new files.

On the frontend, we're adding a section to the existing SettingsPage. Create a folder under your settings or preferences feature area for the `EmailAlertPreferencesSection` component and any sub-components it needs (toggle rows, severity badges, the master toggle, the delivery email field).

Also create a shared folder for email HTML templates — we'll need at least three: one for alert emails, one for summary emails, and one for the test email. Keeping templates separate from route logic makes them much easier to maintain.

Finally, create a shared utilities file for email-related helpers: rate limit key generation, email address validation, and the audit log writer. Several endpoints will reuse these, so centralizing them now saves duplication later.

The project should still build without errors after this step — we're just adding empty folders and placeholder files.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure the folder structure is set up correctly.

Confirm that:
- [ ] There's an `api/email/` folder with placeholder files for `send-alert`, `send-summary`, `send-test`, `schedule-summary`, and `log`
- [ ] The existing `api/notifications/preferences` files are identified and ready to be extended
- [ ] There's a shared email templates folder with placeholder files for alert, summary, and test templates
- [ ] There's a shared email utilities file
- [ ] There's a folder for the `EmailAlertPreferencesSection` component
- [ ] The project still builds without errors after adding these folders

Test the actual flow: Run a build — this is scaffolding only, so nothing should break.

Then give me your honest advice on:
- Whether this folder structure fits naturally with the existing project layout
- If there's a better place to put the email templates given the project's conventions
- Any naming conflicts with existing files
- Anything you'd reorganize before we start building
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: