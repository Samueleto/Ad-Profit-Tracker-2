# Step 7 of 437: Set up folder structure for ExoClick integration

================================================================================

**Category:** Project Scaffolding
**Flow:** feature_exoclick-integration

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We're building an ExoClick ad network integration that syncs ad statistics into Firestore and exposes them via a set of API routes. For this step, set up the folder structure so everything has a home before we start writing real code.

On the backend (Next.js API routes), create a folder at `src/app/api/networks/exoclick/` with subfolders or files for each of the six endpoints we'll build: `sync`, `stats`, `stats/by-country`, `stats/latest`, `raw-response`, and `scheduled-sync`. Each of these will become a `route.ts` file.

Also create a shared utilities folder at `src/lib/exoclick/` — we'll put the ExoClick API client wrapper, the decryption helper, and the Firestore upsert logic here so the sync and scheduled-sync endpoints can share code without duplicating it.

Create a `src/lib/firestore/` folder if it doesn't already exist — we'll put Firestore collection helpers here.

On the frontend side, create a `src/features/exoclick/` folder with subfolders for `components` and `hooks`. We'll build the stats UI components and data-fetching hooks here.

Finally, make sure there's a `.env.local` file (or equivalent) with placeholder entries for `EXOCLICK_ENCRYPTION_SECRET`, `INTERNAL_SYNC_SECRET`, and `NEXT_PUBLIC_FIREBASE_*` config values. Don't fill them in yet — just document what's needed.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure the folder structure is set up correctly.

Confirm that:
- [ ] All six API route files exist under `src/app/api/networks/exoclick/`
- [ ] The `src/lib/exoclick/` shared utilities folder exists
- [ ] The `src/lib/firestore/` folder exists
- [ ] The `src/features/exoclick/components/` and `hooks/` folders exist
- [ ] `.env.local` has placeholder entries for `EXOCLICK_ENCRYPTION_SECRET`, `INTERNAL_SYNC_SECRET`, and Firebase config vars
- [ ] The project still builds without errors after adding these files

Test the actual flow: Run `npm run build` or `npm run dev` — the project should start without any import or compilation errors. This is scaffolding only, so no real logic yet.

Then give me your honest advice on:
- Whether this folder structure fits the existing project conventions
- If there's a better way to organize the shared ExoClick utilities
- Any naming conventions you'd change before we start building on top of this
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: