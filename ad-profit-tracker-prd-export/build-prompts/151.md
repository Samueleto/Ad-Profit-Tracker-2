# Step 151 of 437: Build Complete Navigation System

================================================================================

**Category:** Navigation & Routing
**Flow:** Integration Architecture (Phase 5D)

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

Build the complete navigation system for the Ad Profit Tracker web application using Next.js App Router. Start by creating the core routing infrastructure and layout components before implementing any page content.

First, establish the route structure in the app directory. Create route groups: app/(auth) for public routes and app/(app) for protected routes. The public layout at app/(auth)/layout.tsx should render a simple header with the app logo and login button. The protected layout at app/(app)/layout.tsx must implement the AppShell component with sidebar navigation, header with user menu, and main content area.

Implement the AppShell component in components/layout/AppShell.tsx with a responsive sidebar containing five primary navigation items: Dashboard (/dashboard), Settings (/settings), Reports (/reports), Team (/settings/team), and Help (/help). Each nav item should use Lucide React icons (home, settings, file-text, users, help-circle). The sidebar should collapse on mobile devices and include a user menu dropdown in the top-right with Sign Out functionality.

Create the authentication system using Firebase Auth. Build components/auth/AuthProvider.tsx to wrap the entire app with Firebase authentication context. Implement components/auth/LoginModal.tsx as a modal dialog with Google OAuth and email/password login options. The modal should appear when unauthorized users access protected routes or when the login button is clicked. Configure Firebase in lib/firebase.ts with proper environment variables for API keys.

Establish route protection using middleware.ts to check authentication status and redirect unauthorized users. Protected routes should trigger the login modal instead of hard redirects to maintain user experience. Implement components/auth/AuthGuard.tsx to wrap protected layouts and handle authentication state changes.

Create placeholder page components for all routes. Each page should be a simple React component that renders only a heading with the page name and a brief description. Key pages include: app/(app)/dashboard/page.tsx (ManualRefreshPanel), app/(app)/settings/page.tsx (SettingsPage), app/(app)/reports/page.tsx (CustomReportBuilderPage), app/(app)/settings/team/page.tsx (TeamManagementPage), app/(app)/help/page.tsx (HelpCenterPage), app/(app)/help/[articleId]/page.tsx (HelpArticlePage), app/(app)/onboarding/page.tsx (OnboardingWizardPage), and app/(app)/invite/accept/page.tsx (AcceptInvitationPage).

Implement modal routing for components that appear as overlays. Create components/modals/ModalProvider.tsx to manage modal state globally. Modal components should be rendered conditionally based on URL parameters or application state. Key modals include ExportModal, ScheduleReportModal, InviteMemberModal, and various settings modals.

Build the responsive navigation system. The sidebar should be fixed on desktop (min-width: 1024px) and transform into a mobile drawer on smaller screens. Implement components/navigation/MobileDrawer.tsx for mobile navigation with a hamburger menu trigger. Ensure all navigation items are accessible via keyboard and screen readers.

Configure global state management using React Context. Create contexts/AuthContext.tsx for user authentication state, contexts/ApiKeyContext.tsx for API key management, contexts/DataSyncContext.tsx for synchronization status, and contexts/ThemeContext.tsx for dark/light mode theming. Each context should provide both state and actions to consuming components.

Implement the theme system with CSS variables for light and dark modes. Create styles/globals.css with comprehensive color schemes and component styling. The theme should be toggleable via ThemeContext and persist user preferences in localStorage.

Ensure proper TypeScript typing throughout the navigation system. Define interfaces for User, ApiKey, SyncStatus, and Theme objects. Create type definitions in types/navigation.ts for route configurations and navigation items.

The navigation system must be fully functional with working routes, authentication guards, modal management, and responsive behavior before any page content is implemented. All components should render placeholder content that clearly identifies their purpose and location in the application hierarchy.

================================================================================

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: