# Step 306 of 437: Wire Zeydoo components into the network dashboard

================================================================================

**Category:** Feature Wiring & Integration
**Flow:** feature_zeydoo-integration

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We're building a Zeydoo ad network integration. For this step, I need you to connect all the Zeydoo components and hooks together and integrate them into wherever the app displays network integrations.

First, create a ZeydooDashboard container component that assembles all five pieces — ZeydooLatestCard, ZeydooStatsOverview, ZeydooTopCountries, ZeydooSyncControl, and ZeydooRawExplorer — into a single cohesive view. This container owns the shared date range state so that ZeydooStatsOverview and ZeydooTopCountries always show data for the same period. When the user changes the date range in the overview, the top countries table should update to match.

Wire up the sync flow end-to-end: ZeydooSyncControl uses useZeydooSync, and when a sync completes successfully, the ZeydooLatestCard and ZeydooStatsOverview should both refresh without the user having to do anything. Make sure the syncVersion coordination from useZeydooSync actually triggers refetches in useZeydooStats and useZeydooLatest.

For the scheduled sync endpoint (POST /api/networks/zeydoo/scheduled-sync), this is an internal backend endpoint and doesn't need any UI. However, make sure it's wired up as a Next.js API route that can be called by a cron job or scheduler. Document in a comment at the top of the route file how to configure the scheduler (e.g., Vercel Cron, Cloud Scheduler) to hit this endpoint daily with the correct x-internal-secret header.

Finally, integrate ZeydooDashboard into the app's existing network settings or integrations area. If there's a networks list page or a per-network detail page, add Zeydoo as an entry point there. The integration should only be visible/accessible to authenticated users — wrap the dashboard in an auth check and redirect to login if the user isn't signed in.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure everything is wired together correctly.

Confirm that:
- [ ] ZeydooDashboard renders all five sub-components in one view
- [ ] Changing the date range in ZeydooStatsOverview also updates ZeydooTopCountries
- [ ] A successful sync in ZeydooSyncControl refreshes both ZeydooLatestCard and ZeydooStatsOverview
- [ ] The ZeydooDashboard is accessible from the app's networks/integrations area
- [ ] Unauthenticated users are redirected to login before seeing the dashboard
- [ ] POST /api/networks/zeydoo/scheduled-sync exists as a Next.js API route
- [ ] The scheduled-sync route has documentation comments explaining how to configure the cron job
- [ ] The scheduled-sync route rejects requests without the correct x-internal-secret header

Test the actual flow: Log in → navigate to the networks/integrations area → find Zeydoo → open the dashboard → see all panels load → change the date range → confirm both the stats overview and top countries table update → click 'Sync Now' → wait for completion → confirm the latest card and stats overview both refresh with new data → open the raw explorer → pick a date → fetch raw data → see JSON output. Then test the scheduled-sync route directly with curl — with the correct secret it should run, without it should return 401.

Then give me your honest advice on:
- Whether the shared date range state between components could cause any sync/timing issues
- How the scheduled sync should handle partial failures (some users succeed, some fail)
- Whether the cron job setup is documented clearly enough for a non-developer to configure
- Any navigation dead-ends or confusing UX in the wiring
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: