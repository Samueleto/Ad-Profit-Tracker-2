# Step 32 of 437: Set up folder structure for PDF export

================================================================================

**Category:** Project Scaffolding
**Flow:** feature_pdf-export-functionality

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We're building a PDF export feature that plugs into the existing ExportModal as a second tab alongside the Excel export. For this step, set up the folder structure so everything has a home before we start writing code.

On the frontend side, create a folder for the PDF export feature inside whatever directory the existing Excel export lives in — something like `features/export/pdf/` or `components/export/pdf/`. We'll need a place for the PdfExportTab component and any helper utilities (like formatting currency values or computing KPIs from raw data).

On the backend side, create a new route file for `POST /api/export/pdf`. The Excel export endpoint likely already has a folder at something like `app/api/export/` — add a `pdf` subfolder there with a `route.ts` file. We'll also want a shared utilities folder for things both the Excel and PDF endpoints can reuse (like the rate limiter logic, the country name lookup, and the Firestore query helpers) if that doesn't already exist.

Don't write any logic yet — just make sure the files and folders are in place and the project still compiles cleanly.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure the folder structure is set up correctly.

Confirm that:
- [ ] There's a new `app/api/export/pdf/` folder with a `route.ts` file
- [ ] There's a frontend folder for the PdfExportTab component
- [ ] Any shared export utilities (rate limiter, country lookup) have a home that both Excel and PDF can import from
- [ ] The project still builds without errors after adding these empty files

Test the actual flow: Run `npm run build` (or `next build`) and confirm no new errors were introduced — this is just scaffolding.

Then give me your honest advice on:
- Whether this folder structure fits naturally with the existing Excel export structure
- If there's a better way to co-locate shared export logic
- Anything you'd reorganize before we start building the actual feature
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: