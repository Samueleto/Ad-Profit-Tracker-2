# Step 436 of 437: Test RBAC permissions, UI gates, and API endpoints

================================================================================

**Category:** Testing
**Flow:** feature_role-based-access-control

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

Let's add tests to make sure the RBAC system works correctly and doesn't regress.

**Unit tests — permission logic:**
- `checkPermission` utility: test that it returns true for a granted permission, false for a denied one, and false gracefully when the permissions map is null or undefined.
- Role hierarchy logic: test that Owner always gets all permissions true, that Admin cannot edit Admin permissions, that Member cannot edit any role.
- System defaults: test that the default permission maps for admin/member/viewer/analyst match the spec (e.g., viewer only gets canViewDashboard and canViewReports).
- Permission key validation: test that unknown keys are rejected and all 9 valid keys are accepted.

**Unit tests — hooks:**
- `useRolePermissions`: mock GET /api/rbac/permissions — test loading state, success state with full matrix, error state, and that `refresh()` triggers a re-fetch.
- `useMyPermissions`: mock GET /api/rbac/my-permissions — test loading state, success state, and that isOwner is true when workspaceRole is 'owner'.

**Component tests — PermissionsTab:**
- Renders shimmer rows while loading, then the full matrix on success.
- Shows the 'Customize Role Permissions' button for Owner and Admin, hides it for Member and Viewer.
- Shows the 'Copy Role' dropdown for Owner only.
- Renders the 'Access Denied' message on a 403 response.
- Renders the error banner with Retry on a 500 response.
- The Role Descriptions accordion is collapsed by default and expands on click.

**Component tests — RolePermissionsEditModal:**
- Pre-populates toggles from the fetched permission data.
- Switching the role selector resets toggles to the correct values for that role.
- Disables all toggles and shows a spinner on Save during the saving state.
- Shows inline validation when all permissions are removed from a role.
- Shows the 403 error message inside the modal without closing it.
- Calls PATCH /api/rbac/permissions with the correct payload on Save.
- Calls `refresh()` on the parent after a successful save.

**API endpoint tests:**
- GET /api/rbac/permissions: returns 401 without token, returns 200 with full matrix for valid token, returns system defaults when no workspacePermissions document exists, merges custom permissions with defaults correctly.
- PATCH /api/rbac/permissions: returns 403 when Member tries to edit, returns 403 when Admin tries to edit Admin role, returns 400 for

================================================================================

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: