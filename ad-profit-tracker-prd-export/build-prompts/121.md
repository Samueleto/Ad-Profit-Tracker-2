# Step 121 of 437: Create AuditLog TypeScript types and validation

================================================================================

**Category:** Schema & Model Definitions
**Flow:** feature_audit-logging-activity-tracking

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We need a shared TypeScript type for the `auditLogs` collection and some validation helpers that both the API endpoints and the frontend components can use.

Create an `AuditLog` interface in `src/features/audit/types/auditLog.ts`. It should have all the fields from the `auditLogs` schema: `id` (string), `userId` (string), `action` (typed as a union of the allowed enum values), `resourceType` (typed as a union of the allowed enum values), `resourceId` (string or null), `metadata` (a flexible `Record<string, unknown>`), `ipAddress` (string), `userAgent` (string), `status` ('success' | 'failure'), `errorMessage` (string or null), and `createdAt` (a Firestore `Timestamp` on the server side, but a serialized ISO string or number when returned from the API).

Define the action enum values as a const array so they can be used for both TypeScript typing and runtime validation: `api_key_saved`, `api_key_deleted`, `network_config_updated`, `network_config_reordered`, `network_config_reset`, `network_connection_tested`, `manual_sync_triggered`, `preferences_updated`, `profile_updated`, `account_deleted`. Export this as `AUDIT_ACTIONS`.

Do the same for resource types: `api_key`, `network_config`, `preferences`, `profile`, `sync`. Export as `AUDIT_RESOURCE_TYPES`.

Create a `LogFilters` interface for the query params used by `GET /api/audit/logs` and `GET /api/audit/logs/export`: `action` (array of action enum values, optional), `resourceType` (resource type enum, optional), `startDate` (string, optional), `endDate` (string, optional), `status` ('success' | 'failure', optional), `search` (string, optional), `limit` (number, optional), `cursor` (string, optional).

Also create a `PaginatedLogsResponse` interface matching what `GET /api/audit/logs` returns: `{ logs: AuditLog[], total: number, hasMore: boolean, nextCursor: string | null }`.

These types should be importable by both the API route handlers and the frontend components — put them somewhere both can reach (the `src/features/audit/types/` folder works, or `src/types/` if the project has a shared types directory).

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure the types are correct and complete.

Confirm that:
- [ ] `AuditLog` interface has all 11 fields from the schema (id, userId, action, resourceType, resourceId, metadata, ipAddress, userAgent, status, errorMessage, createdAt)
- [ ] `AUDIT_ACTIONS` const array contains all 10 action values
- [ ] `AUDIT_RESOURCE_TYPES` const array contains all 5 resource type values
- [ ] `LogFilters` interface covers all the query params for the GET endpoints
- [ ] `PaginatedLogsResponse` interface matches the expected API response shape
- [ ] TypeScript compiles without errors when these types are imported in a test file
- [ ] The `action` field on `AuditLog` is typed as a union (not just `string`), so TypeScript catches invalid values

Test the actual flow: Create a mock `AuditLog` object in a scratch file and assign it to the `AuditLog` type — TypeScript should catch it if any field is missing or has the wrong type. Try assigning an invalid action string like `'invalid_action'` — TypeScript should flag it as an error.

Then give me your honest advice on:
- Whether the type definitions are strict enough to catch mistakes at compile time
- Any fields that might need different types on the server vs. client (like `createdAt` as Firestore Timestamp vs. serialized string)
- Whether the `metadata` field being `Record<string, unknown>` is flexible enough without being too loose
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: