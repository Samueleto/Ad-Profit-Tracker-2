# Step 302 of 437: Wire Networks tab into existing SettingsPage

================================================================================

**Category:** Feature Wiring & Integration
**Flow:** feature_network-configuration-management

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

Now we need to connect the NetworkConfigTab into the existing SettingsPage at /settings without breaking any existing tabs.

Open the existing SettingsPage and add a 'Networks' tab to its tab navigation. The tab label should say 'Networks' and it should render the NetworkConfigTab component when active. Make sure the tab integrates with however the SettingsPage currently manages its active tab — whether that's local state, a URL query param like ?tab=networks, or a router-level tab system. If the SettingsPage uses URL-based tab switching, make sure navigating directly to /settings?tab=networks (or equivalent) opens the Networks tab immediately.

Wire the useNetworkConfigs hook into NetworkConfigTab so all the data and action functions flow down to the NetworkCard components. The NetworkCard components should receive their config data and callbacks (onUpdate, onTestConnection, onReset) as props — they shouldn't call the hook themselves.

Connect the authExpired flag from useNetworkConfigs to a redirect: when authExpired becomes true, use Next.js router to push to '/' and show a 'Session expired' toast notification.

Connect the syncAllResult from useNetworkConfigs to a toast notification that fires after POST /api/networks/sync-all completes. The toast should summarize: how many networks were triggered, which ones were skipped (and why — e.g. 'no API key'), and any that failed.

Make sure the drag-and-drop context from dnd-kit wraps the network card grid inside NetworkConfigTab. The DndContext and SortableContext should live in NetworkConfigTab (not in SettingsPage), so the drag-and-drop is scoped to just the networks grid. Wire the onDragEnd event to call the reorderNetworks function from useNetworkConfigs.

Finally, make sure the Networks tab is only visible to authenticated users. If the SettingsPage already has an auth guard, the Networks tab inherits it. If not, add a check inside NetworkConfigTab that redirects to '/' if there's no authenticated Firebase user.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure the Networks tab is properly wired into the SettingsPage and everything connects end-to-end.

Confirm that:
- [ ] A 'Networks' tab appears in the SettingsPage tab bar without breaking existing tabs
- [ ] Clicking the Networks tab renders the NetworkConfigTab
- [ ] If the SettingsPage uses URL-based tabs, navigating to /settings?tab=networks (or equivalent) opens the Networks tab directly
- [ ] The useNetworkConfigs hook is wired into NetworkConfigTab and data flows down to NetworkCard as props
- [ ] When authExpired becomes true, the app redirects to '/' with a 'Session expired' toast
- [ ] After syncAll completes, a toast shows the triggered/skipped/failed summary
- [ ] The dnd-kit DndContext wraps only the network card grid (not the whole SettingsPage)
- [ ] Dragging a card and dropping it calls reorderNetworks and the order updates
- [ ] An unauthenticated user cannot see or access the Networks tab

Test the actual flow: Open /settings as a logged-in user → click the Networks tab → all 4 cards load. Drag RollerAds above ExoClick → confirm the order changes and the reorder API fires. Click 'Sync All Now' → see the summary toast. Open a new browser tab and navigate directly to /settings?tab=networks → Networks tab opens immediately. Sign out and try to access /settings → redirected away. Simulate token expiry (or manually trigger authExpired) → redirected to / with the session expired toast.

Then give me your honest advice on:
- Whether the tab integration could break any existing SettingsPage functionality
- Whether the dnd-kit scope is correct and won't interfere with other draggable elements in the app
- Edge cases in the auth redirect flow (what if the redirect fires while a save is in progress?)
- Anything a user would find confusing about how the tab fits into the Settings experience
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: