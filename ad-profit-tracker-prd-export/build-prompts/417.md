# Step 417 of 437: Test cache hooks, endpoints, and config logic

================================================================================

**Category:** Testing
**Flow:** feature_data-caching-optimization

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

Let's add tests to make sure the caching layer works correctly and doesn't regress.

Unit tests for utility functions:
- `formatTTL`: test that 60 → '1 minute', 3600 → '1 hour', 90 → '1 minute 30 seconds' (or whatever format you chose), 0 → '0 seconds', and large values like 86400 → '1 day'.
- Cache key namespacing logic: given a UID and a network ID, confirm the generated key matches the expected format and that two different UIDs produce different keys.

Unit tests for hooks (use `renderHook` with mocked Firestore and fetch):
- `useNetworkCacheConfig`: returns defaults when the document doesn't exist; returns Firestore values when it does; applies defaults for any missing fields.
- `useCacheStatus`: returns loading state initially; populates stats on success; returns error state on 500.
- `useCacheInvalidation`: sets loading to true while in flight; clears loading and returns success on 200; returns the right error message for 400, 401, and 429 responses.

API endpoint tests (use a test runner that can spin up Next.js API routes, or test the handler functions directly):
- `POST /api/cache/invalidate`: returns 401 with no token; returns 400 with an invalid scope; returns 200 with `invalidatedCount` on success; returns 429 after exceeding 15 requests in an hour.
- `GET /api/cache/status`: returns 401 with no token; returns 200 with the stats object; only returns keys belonging to the authenticated user's UID.
- `POST /api/cache/warm`: returns 401 without the correct `x-internal-secret`; processes tasks and returns `tasksProcessed` and `tasksFailed` counts; continues processing remaining tasks when one fails.

Integration test (end-to-end flow):
- Sign in → call `GET /api/cache/status` → see initial stats → call `POST /api/cache/invalidate` with scope `'all'` → call `GET /api/cache/status` again → confirm key count dropped.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure all the cache tests pass and cover the important paths.

Confirm that:
- [ ] All `formatTTL` unit tests pass with correct human-readable output
- [ ] Cache key namespacing tests confirm UID isolation
- [ ] `useNetworkCacheConfig` tests cover missing document, full document, and partial fields
- [ ] `useCacheStatus` tests cover loading, success, and error states
- [ ] `useCacheInvalidation` tests cover loading state, success, and 400/401/429 error responses
- [ ] `POST /api/cache/invalidate` tests cover 401, 400, 200, and 429 responses
- [ ] `GET /api/cache/status` tests confirm only the authenticated user's keys are returned
- [ ] `POST /api/cache/warm` tests confirm it continues on partial failure and reports counts correctly
- [ ] The integration test completes the full invalidate-then-check-status flow
- [ ] All tests pass with `npm test` (or equivalent)

Test the actual flow: Run the full test suite. Then manually run the integration test: sign in → check status → invalidate all → check status again → confirm the key count changed. All steps should complete without errors.

Then give me your honest advice on:
- What important scenarios aren't covered by these tests that could break in production
- Whether the mocking strategy (MSW vs. direct mocks) is appropriate for this codebase
- Any flaky test risks (timing-sensitive SWR behavior, polling intervals in tests)
- Tests you'd want before feeling confident shipping this to real users
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: