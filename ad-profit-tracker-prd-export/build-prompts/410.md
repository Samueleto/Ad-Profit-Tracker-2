# Step 410 of 437: Test the geographic breakdown feature

================================================================================

**Category:** Testing
**Flow:** feature_geographic-performance-breakdown

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We need tests to make sure the geographic performance breakdown feature works correctly and doesn't break when things go wrong.

**Unit tests for hooks:**

- useGeoBreakdown: test that it fires both GET /api/stats/geo-breakdown and GET /api/roi/breakdown in parallel when called. Test that it merges colorCode from the ROI response into the geo rows. Test that it caps the date range at 90 days. Test that it re-fetches when the date range changes. Test that it handles a partial failure (one endpoint fails, one succeeds).

- useGeoCountryDrilldown: test that it fetches GET /api/stats/snapshot with the correct country code and date range. Test that it aggregates the response into { date, netProfit } objects sorted by date. Test that it re-fetches when the country code changes.

**Component tests for GeoBreakdownSection:**

- Renders 10 shimmer rows during loading state.
- Renders the correct number of rows based on the Top N selector (10/20/50).
- Country search input filters rows client-side without triggering a re-fetch.
- Metric toggle changes the sort order of rows without triggering a re-fetch.
- Null revenue/cost values render as '—' not '$0'.
- Rows with positive ROI have the green Tailwind classes; negative ROI has red; zero/null has amber.
- The metric share progress bar renders in the share column.
- GEO Insights strip is collapsed by default; clicking the chevron expands it.
- Clicking a country row calls the onCountrySelect callback with the correct country code and name.
- Network Breakdown sub-row expands inline when the row toggle is clicked.
- Empty state renders with 'Sync Now' CTA when the API returns an empty array.
- Error state renders with a Retry button on 500; clicking Retry re-calls both endpoints.

**Component tests for GeoCountryDrilldownModal:**

- Renders the country name and flag in the modal title.
- Shows a 200px shimmer during loading.
- Renders a recharts LineChart with the correct number of data points.
- Per-network breakdown table renders the correct rows.
- Empty state shows the country name in the message.
- Close button calls the onClose callback.
- Error state shows a Retry button that re-calls GET /api/stats/snapshot.

**Integration test (full user flow):**

Load the dashboard with a mocked date range → GeoBreakdownSection fetches both endpoints → table renders with color-coded rows → change the metric toggle → rows re-sort → click a country row → GeoCountryDrilldownModal opens → chart renders with daily data → click 'View Full Trend' → modal closes → click a different country → modal opens with new data → close the modal → expand GEO Insights strip → verify the stats are correct.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure all tests pass and cover the important paths.

Confirm that:
- [ ] All useGeoBreakdown unit tests pass (parallel fetch, colorCode merge, 90-day cap, re-fetch on date change, partial failure)
- [ ] All useGeoCountryDrill
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: