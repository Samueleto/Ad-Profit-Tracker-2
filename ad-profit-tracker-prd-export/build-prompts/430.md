# Step 430 of 437: Test the Financial Metrics Dashboard end-to-end

================================================================================

**Category:** Testing
**Flow:** feature_financial-metrics-dashboard

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

Let's add tests to make sure the Financial Metrics Dashboard works correctly and doesn't break silently.

**Unit tests for useDashboardMetrics hook:**
- Initializes with Last 7 days date range and status 'idle'
- Fetches GET /api/dashboard/metrics on mount with correct dateFrom/dateTo params
- Status transitions: 'loading' during fetch, 'success' on valid response, 'empty' when arrays are empty, 'error' on failure
- setDateRange with a preset correctly computes dateFrom/dateTo using date-fns
- setDateRange with a custom range over 90 days sets dateRangeValidationError and does not fetch
- refresh() calls POST /api/cache/invalidate then re-fetches GET /api/dashboard/metrics
- 401 response triggers getIdToken(true) retry; if retry also 401s, sets status to redirect
- Freshness derivation: null cachedAt → { type: 'live' }; timestamp cachedAt → { type: 'cached', minutesAgo: N }
- AbortController cancels stale requests when date range changes rapidly

**Unit tests for KpiCard component:**
- Renders value, percentage change, and directional arrow correctly
- Shows green styling for positive values, red for negative, amber for zero
- Shows '—' and no arrow when change value is null
- Shows '—' when the main value is null (ROI with zero cost)

**Unit tests for TrendChart component:**
- Renders a recharts LineChart when dailySeries has data
- Shows 'No trend data available' message when dailySeries is empty

**Unit tests for GeoBreakdownTable component:**
- Renders correct number of rows from topCountries array
- Shows 'No geographic data available' when topCountries is empty
- Rows are sorted by net profit descending

**Integration tests for FinancialMetricsDashboardPage:**
- Loading state: shows four shimmer KPI card skeletons, chart placeholder, and 5 shimmer GEO rows
- Success state: KPI cards, chart, and GEO table all populated from mock GET /api/dashboard/metrics response
- Empty state: shows 'No data for this date range' message and 'Sync Now' CTA
- Error 500: shows error banner with Retry button; previously loaded data stays visible
- Error 404: shows inline 'No data found' message with 'Reset Date Range' button
- Date range change: new fetch fires with updated params
- 'Refresh Data' button: calls POST /api/cache/invalidate then re-fetches
- ManualRefreshPanel is present and collapsed by default

**Auth/security tests:**
- Unauthenticated request to GET /api/dashboard/metrics returns 401
- Request with invalid token returns 401

================================================================================

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: