# Step 367 of 437: Handle auth errors with clear user feedback

================================================================================

**Category:** Error Handling
**Flow:** feature_user-authentication-accounts

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

Firebase Auth returns error codes that mean nothing to users. We need to map them to helpful messages and make sure every failure mode in the auth flow is handled gracefully.

**Firebase Auth error mapping:**
Create a utility function that takes a Firebase Auth error code and returns a user-friendly string. Map at minimum:
- `auth/wrong-password` or `auth/invalid-credential` → 'Incorrect password. Try again or reset your password.'
- `auth/user-not-found` → 'No account found with this email.'
- `auth/email-already-in-use` → 'An account with this email already exists. Try signing in instead.'
- `auth/weak-password` → 'Password must be at least 6 characters.'
- `auth/invalid-email` → 'Please enter a valid email address.'
- `auth/too-many-requests` → 'Too many attempts. Please wait a few minutes and try again.'
- `auth/popup-closed-by-user` → null (don't show an error — the user intentionally closed the popup)
- `auth/popup-blocked` → 'Your browser blocked the sign-in popup. Please allow popups for this site.'
- `auth/network-request-failed` → 'Network error. Check your connection and try again.'
- `auth/user-disabled` → 'This account has been disabled. Contact support.'
- Any unknown code → 'Something went wrong. Please try again.'

**Where errors appear in the LoginModal:**
- Field-specific errors (wrong password, invalid email, email in use, weak password) → show inline below the relevant field, not in a banner
- Non-field errors (network, popup blocked, too many requests) → show as a toast notification with a retry button where applicable
- `popup-closed-by-user` → silently ignore, reset the Google button loading state

**API call failures:**
- If POST `/api/auth/sync-user` fails after a successful Firebase Auth sign-in, don't block the user — log the error and let them through to `/dashboard`. The sync can be retried on next load via GET `/api/auth/get-user`.
- If GET `/api/auth/get-user` returns 500, show a non-blocking error toast and fall back to the Firebase Auth user object for basic display (name, email) while the full profile is unavailable.
- If PATCH `/api/auth/update-profile` fails, show an error toast and revert the optimistic UI update in AuthContext.
- If DELETE `/api/auth/delete-account` fails, show a clear error message and do NOT clear the auth state — the user is still signed in and their account still exists.

**Form validation (before hitting Firebase):**
Validate client-side before calling any Firebase method: email must be a valid format, password must be at least 6 characters, and in Sign Up mode the two passwords must match. Show these as inline field errors immediately on blur or submit — don't wait for Firebase to reject them.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure every failure mode is handled gracefully and users see helpful messages.

Confirm that:
- [ ] Wrong password shows 'Incorrect password' below the password field (not a generic banner)
- [ ] Non-existent email shows 'No account found with this email' below the email field
- [ ] Registering with an existing email shows 'An account with this email already exists'
- [ ] Weak password (under 6 chars) shows the requirement inline
- [ ] Too many failed attempts shows the rate limit message
- [ ] Closing the Google popup silently resets the button — no error shown
- [ ] Popup blocked shows a helpful message about allowing popups
- [ ] Network error shows a toast with a retry option
- [ ] Client-side validation fires on blur/submit before any Firebase call
- [ ] Mismatched passwords in Sign Up are caught client-side
- [ ] If POST `/api/auth/sync-user` fails, the user still gets to `/dashboard`
- [ ] If PATCH `/api/auth/update-profile` fails, the UI reverts to the previous value
- [ ] If DELETE `/api/auth/delete-account` fails, the user remains signed in with an error message

Test the actual flow: On the Sign In tab, enter a valid email with the wrong password → see inline error below password field. Enter a non-existent email → see inline error below email field. On Sign Up tab, enter mismatched passwords → see error before any network call. Enter a 3-character password → see inline error. Try signing in rapidly many times → see rate limit message. Close the Google popup mid-flow → no error, button resets. Turn off WiFi and try to sign in → see network error toast with retry.

Then give me your honest advice on:
- Whether the error messages are clear enough for non-technical users
- Any Firebase error codes we're not handling that could surface raw error strings
- Edge cases in the error recovery flow (what if the user retries and gets a different error?)
- Whether the sync-user failure fallback could leave the app in an inconsistent state
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: