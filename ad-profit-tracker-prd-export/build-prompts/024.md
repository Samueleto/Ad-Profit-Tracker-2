# Step 24 of 437: Set up folder structure for comparative analysis

================================================================================

**Category:** Project Scaffolding
**Flow:** feature_comparative-network-analysis

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We're building a Comparative Network Analysis feature that lives as a tab inside the existing FinancialMetricsDashboardPage. For this step, I need you to set up the folder structure so everything has a home before we start building.

Create a comparative-analysis feature area under something like src/features/comparative-analysis/ (or wherever the project keeps feature modules). Inside it, create subfolders for: components (we'll build several reusable pieces here — comparison cards, a grouped bar chart, an efficiency table, and a network ranking strip), hooks (for data fetching and state), and types (TypeScript interfaces for the comparison payload).

On the API side, create a route file placeholder for GET /api/networks/comparison — this will be a single endpoint that returns all four networks' data in one consolidated payload.

The ComparativeNetworkAnalysisTab itself is not a standalone page — it renders as a tab integrated into the existing FinancialMetricsDashboardPage. So there's no new route to register, just a new tab panel component that gets added to the existing dashboard tab bar.

Keep this step to just folder creation and empty placeholder files. The goal is that the project still builds cleanly after this step with no new errors.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure the folder structure is set up correctly.

Confirm that:
- [ ] The comparative-analysis feature folder exists with components/, hooks/, and types/ subfolders
- [ ] There's a placeholder route file at src/app/api/networks/comparison/route.ts (even if it just returns a 501 for now)
- [ ] The project still builds without errors after adding these folders and files
- [ ] No existing dashboard tabs or routes have been changed

Test the actual flow: Run a build (npm run build or equivalent) and confirm it completes without errors. Open the dashboard in the browser and confirm the existing tabs still work exactly as before.

Then give me your honest advice on:
- Whether this folder structure fits naturally with how the rest of the project is organized
- If there's a better place to put the comparison feature given the existing codebase layout
- Anything you'd reorganize before we start building the actual components
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: