# Step 63 of 437: Verify recharts and firebase-admin are ready

================================================================================

**Category:** SDK Setup & Configuration
**Flow:** feature_per-network-analytics-tabs

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We're building a feature that uses recharts for the daily trend AreaChart and firebase-admin for server-side token verification. For this step, make sure both are properly installed and configured — we don't want to discover missing setup halfway through building the components.

On the frontend side, check that `recharts` is installed. If it's not already in the project (it may already be there from GeoBreakdownSection), add it. We'll be using the `AreaChart`, `Area`, `XAxis`, `YAxis`, `CartesianGrid`, `Tooltip`, and `ResponsiveContainer` components from recharts. No special initialization needed — just confirm the import works.

Also confirm `firebase/auth` is available on the client side for attaching Bearer tokens to API calls. Specifically, we'll be calling `getIdToken()` and `getIdToken(true)` (forced refresh) from the Firebase Auth SDK. If there's already a utility function in the project that gets the current user's ID token and attaches it as an Authorization header, note where it is — we'll reuse it rather than write it again.

On the backend side, confirm `firebase-admin` is initialized. There should already be an admin initialization file somewhere in the project (since FinancialMetricsDashboardPage already uses Firebase Auth middleware). Find it and note the import path — the new `GET /api/networks/stats` route handler will import `admin.auth().verifyIdToken()` from there.

Also install `node-cache` if it's not already present. This is used for the 5-minute server-side cache on network stats responses. Create or locate a shared cache instance — we want one `NodeCache` instance reused across requests, not a new one created per request. A good place for this is a singleton in `src/lib/cache.ts` (or wherever the project keeps server utilities).

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure all the SDKs and packages are ready to use.

Confirm that:
- [ ] `recharts` is installed and a test import of `AreaChart` from 'recharts' compiles without errors
- [ ] `firebase/auth` is available and `getIdToken` can be imported from the existing auth setup
- [ ] `firebase-admin` is initialized and `admin.auth().verifyIdToken()` is callable from a server route
- [ ] `node-cache` is installed
- [ ] There's a shared `NodeCache` singleton (e.g., in `src/lib/cache.ts`) that can be imported by the API route
- [ ] The project builds without errors after these additions

Test the actual flow: Add a temporary import of `AreaChart` from 'recharts' in a component file and confirm it compiles. Check that the firebase-admin initialization doesn't throw on server startup. Verify the NodeCache singleton can be imported and a test `set`/`get` works.

Then give me your honest advice on:
- Whether the firebase-admin initialization is set up correctly for Next.js (common gotcha: initializing multiple times in dev due to hot reload)
- Any version conflicts between recharts and the existing React version
- Whether the NodeCache singleton pattern will work correctly in Next.js serverless functions or if a different caching approach is needed
- Any missing environment variables needed for firebase-admin to work
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: