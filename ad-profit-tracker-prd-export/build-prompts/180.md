# Step 180 of 437: Build all five schedule API endpoints

================================================================================

**Category:** API Endpoints
**Flow:** feature_scheduled-report-delivery

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We're building five API endpoints that power the ScheduleReportModal. All of them live under /api/schedules/ and all require a valid Firebase ID token in the Authorization header. Here's what each one does:

POST /api/schedules/create (in src/app/api/schedules/route.ts) — Creates a new schedule. Verify the ID token first, then validate all fields using validateScheduleFields(). Check that the user's users document exists and that the reportId exists in their savedReports array (return 404 if not). Check that scheduledReports doesn't already have 10 items (return 400) and doesn't already have a schedule for this reportId (return 409). Generate a UUID with generateScheduleId(), denormalize the reportName from the matching savedReport, compute nextRunAt with computeNextRunAt(), then use Firestore Admin arrayUnion to append the new schedule object to users.scheduledReports. Also set scheduledReportsUpdatedAt to serverTimestamp(). Write an audit log entry to the auditLogs collection with action='scheduled_report_created'. Return 201 with the created schedule.

GET /api/schedules (also in src/app/api/schedules/route.ts) — Lists the user's schedules. Verify the token, read the users document, extract the scheduledReports array. If a reportId query param is provided, filter to just that report's schedule. Sort by createdAt descending. For each schedule, compute a human-readable nextRunLabel using date-fns-tz (e.g., 'Tomorrow at 8:00 AM EST'). Return 200 with the enriched array and a total count. Return an empty array (not 404) if the user has no schedules yet.

PATCH /api/schedules/:id (in src/app/api/schedules/[id]/route.ts) — Updates an existing schedule. Verify the token, validate the :id param and any provided fields. Read the users document, find the matching schedule by id — return 404 if not found. Merge the updates into the existing schedule object, recompute nextRunAt, set updatedAt. Use a Firestore transaction: arrayRemove the old object, arrayUnion the updated object, set scheduledReportsUpdatedAt. Write an audit log with action='scheduled_report_updated' and the list of changed fields. Return 200 with the full updated schedule.

DELETE /api/schedules/:id (also in src/app/api/schedules/[id]/route.ts) — Deletes a schedule. Verify the token, validate :id, read the users document, find the schedule (404 if missing). Use arrayRemove to remove it and update scheduledReportsUpdatedAt. Write an audit log with action='scheduled_report_deleted'. Return 200 with { deleted: true, scheduleId }.

POST /api/schedules/:id/send-now (in src/app/api/schedules/[id]/send-now/route.ts) — Triggers immediate delivery. Verify the token, validate :id, enforce a rate limit of 5 manual sends per hour per user (return 429 if exceeded). Find the schedule in the users document. Resolve the dateRangePreset to actual dates using resolveDateRangePreset() in the schedule's timezone. Call the existing report generation logic (POST /api/export/excel or POST /api/export/pdf internally) to generate a file buffer in memory. Use the sendReportEmail() function from src/lib/email.ts to send the file to schedule.deliveryEmail — never use an email from the request body. Subject line: 'Ad Profit Tracker Report — [reportName] — [dateFrom] to [dateTo]'. Write an audit log with action='scheduled_report_delivered' including fileSizeBytes. Return 200 with { sent: true, deliveryEmail, format, dateFrom, dateTo }. Return 502 if the email provider fails, 500 if report generation fails.

For rate limiting on all endpoints, use a simple in-memory counter keyed by uid + endpoint, or use a Firestore-based counter if the project already has a rate limiting pattern. Apply the limits from the spec: 10 creates/hour, 20 updates/hour, 20 deletes/hour, 5 send-now/hour.

Also fully implement sendReportEmail() in src/lib/email.ts now — it should accept the recipient address, subject, a Buffer containing the file, and the filename (e.g., 'MyReport-2024-01-01-2024-01-07.xlsx'). Attach the buffer as a file attachment and send via the configured Nodemailer transporter.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure all five endpoints work correctly.

Confirm that:
- [ ] POST /api/schedules/create with a valid token and valid body returns 201 with the created schedule object including id, nextRunAt, and all submitted fields
- [ ] POST /api/schedules/create with a non-existent reportId returns 404
- [ ] POST /api/schedules/create when 10 schedules already exist returns 400 with 'Maximum 10 scheduled reports allowed'
- [ ] POST /api/schedules/create for a reportId that already has a schedule returns 409
- [ ] GET /api/schedules returns the user's schedules with a nextRunLabel field on each
- [ ] GET /api/schedules?reportId=xxx filters to just that report's schedule
- [ ] GET /api/schedules for a user with no schedules returns { schedules: [], total: 0 } (not 404)
- [ ] PATCH /api/schedules/:id updates the schedule and recomputes nextRunAt
- [ ] PATCH /api/schedules/:id with an id that doesn't belong to the user returns 404
- [ ] DELETE /api/schedules/:id removes the schedule and returns { deleted: true }
- [ ] POST /api/schedules/:id/send-now generates and emails the report, returns { sent: true, deliveryEmail, format, dateFrom, dateTo }
- [ ] POST /api/schedules/:id/send-now after 5 calls in an hour returns 429
- [ ] All five endpoints return 401 when called without a token or with an invalid token
- [ ] Audit log entries are written to Firestore for create, update, delete, and send-now actions

Test the actual flow: Using a REST client (curl or Postman) with a valid Firebase ID token — create a schedule → GET to see it in the list with nextRunLabel → PATCH to change the frequency → GET again to confirm nextRunAt changed → POST send-now → check the delivery email inbox for the report attachment → DELETE the schedule → GET to confirm it's gone. Try each endpoint without auth and confirm 401s.

Then give me your honest advice on:
- Whether the Firestore arrayRemove + arrayUnion pattern in PATCH is safe under concurrent updates
- Whether the in-memory rate limiting will work correctly in a serverless/edge environment
- Security gaps — can a user trigger report generation for another user's data?
- What happens if the email provider is down — does the endpoint fail gracefully?
- Performance concerns with reading the full users document on every request
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: