# Step 295 of 437: Connect ExportModal to dashboard Export button

================================================================================

**Category:** Feature Wiring & Integration
**Flow:** feature_excel-export-functionality

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

Now we need to wire the ExportModal into the existing FinancialMetricsDashboardPage so the Export button actually opens it.

In FinancialMetricsDashboardPage, find the existing 'Export' button. Hook it up to call openExportModal() from the Zustand store. Render the ExportModal component somewhere in the page's JSX — it should be conditionally shown based on isExportModalOpen from the store. Since it's a modal with a backdrop, it can sit at the bottom of the page's return statement so it renders on top of everything else via z-index.

For the GET /api/export/preview call: this should fire automatically when the modal opens (when isExportModalOpen becomes true). Pass the current dateFrom and dateTo from the Zustand store as query parameters. Include the Firebase Auth Bearer token in the Authorization header — get this by calling firebaseAuth.currentUser?.getIdToken() before making the request.

For the POST /api/export/excel call: build the request body from the modal's current state — the selected sheet keys (mapping the UI checkboxes to the correct API keys: 'summary', 'daily_trend', 'geo_breakdown', per-network checkbox expands to ['exoclick', 'rollerads', 'zeydoo', 'propush'], and 'activity_log'), plus dateFrom, dateTo, filename (if provided), and includeHeaders. Send this as JSON with Content-Type: application/json and the Firebase Auth Bearer token in the Authorization header.

Handle the binary response correctly: the POST /api/export/excel endpoint returns a raw .xlsx binary stream. Receive it as a Blob (use response.blob() in fetch, or responseType: 'blob' in axios). Then create an object URL with URL.createObjectURL(blob), create a temporary anchor element, set its href to the object URL and its download attribute to the filename (e.g., 'ad-profit-tracker-export-2024-01-01-2024-01-31.xlsx'), programmatically click it, then revoke the object URL with URL.revokeObjectURL() to clean up memory.

After a successful download, call setExportStatus('success'), show the 'Export complete — file downloaded' toast using the project's existing toast system, and set a 1.5-second timeout that calls closeExportModal().

Make sure the modal is also accessible from the keyboard — the backdrop click and Escape key should both call closeExportModal() (but only when exportStatus is not 'exporting', since we don't want users accidentally cancelling a running export).

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure everything is properly connected end-to-end.

Confirm that:
- [ ] Clicking the 'Export' button on FinancialMetricsDashboardPage opens the ExportModal
- [ ] The modal immediately calls GET /api/export/preview with the correct dateFrom/dateTo and Bearer token
- [ ] Row count badges populate from the preview response
- [ ] Clicking Export Now sends POST /api/export/excel with the correct JSON body and Bearer token
- [ ] The response is received as a Blob and triggers a file download in the browser
- [ ] The downloaded file is named correctly (default or custom filename with .xlsx extension)
- [ ] The object URL is cleaned up after the download
- [ ] The success toast appears and the modal closes after 1.5 seconds
- [ ] Pressing Escape closes the modal when not exporting
- [ ] Clicking the backdrop closes the modal when not exporting
- [ ] Pressing Escape or clicking the backdrop does NOT close the modal while an export is in progress

Test the actual flow: Open the dashboard → click Export → modal opens → row counts load → select a subset of sheets → expand Advanced → enter a custom filename → click Export Now → spinner appears → file downloads with the correct filename → toast shows → modal closes. Reopen the modal → press Escape → modal closes. Start an export → try pressing Escape → modal stays open.

Then give me your honest advice on:
- Whether the Blob/object URL approach for file downloads works reliably across all major browsers
- What happens if the user navigates away from the dashboard mid-export
- Edge cases (what if the filename contains characters the OS doesn't allow, what if the Blob is empty)
- Anything that would feel broken or confusing to a real user
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: