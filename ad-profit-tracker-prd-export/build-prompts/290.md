# Step 290 of 437: Wire SyncStatusPanel into the dashboard

================================================================================

**Category:** Feature Wiring & Integration
**Flow:** feature_real-time-sync-status-monitoring

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

Now we need to connect the SyncStatusPanel into the existing /dashboard page correctly, making sure it sits in the right place and integrates with other dashboard sections.

**Placement** — Add SyncStatusPanel to the existing dashboard page component, specifically in the ManualRefreshPanel area. If ManualRefreshPanel is a section or region of the dashboard, place SyncStatusPanel adjacent to or below it — the exact position should feel natural given the existing layout. Don't create a new route or page.

**Critical Anomalies Link** — The 'View Details' link in the CriticalAnomaliesStrip should scroll to or navigate to the reconciliation section of the dashboard. If the reconciliation section is on the same page, use a smooth scroll to its anchor. If it's a separate tab or section, wire up the link to navigate there correctly. Check how the existing dashboard handles internal navigation and follow the same pattern.

**PATCH /api/sync/progress Integration** — The PATCH /api/sync/progress endpoint is called internally by sync helper functions (not directly by the UI), but the SyncStatusPanel needs to reflect the phase updates it writes. Since the panel polls GET /api/sync/live-state which reads `currentSyncPhase` from the networkConfigs documents that PATCH /api/sync/progress updates, this should work automatically through polling. No direct wiring needed — just confirm the polling picks up phase changes within the next poll cycle.

**GET /api/errors/circuit-breaker/status** — The SyncStatusPanel references this endpoint for circuit breaker status. Check whether GET /api/sync/live-state already returns `circuitBreakerOpen` and `circuitBreakerOpenedAt` per network (it does, per the endpoint spec). If so, don't make a separate call to /api/errors/circuit-breaker/status — use the data already in the live-state response. If the circuit breaker badge needs richer data than what live-state provides, add a call to /api/errors/circuit-breaker/status in the `useSyncStatus` hook's fetch orchestration.

**GET /api/reconciliation/status** — This endpoint provides the `overallHealth` field for the health banner. Since GET /api/sync/live-state also computes and returns `overallHealth`, use the live-state response as the primary source. Only call /api/reconciliation/status separately if the live-state `overallHealth` field is missing or if the reconciliation status provides additional context the banner needs.

**Session Expiry Redirect** — When `useSyncStatus` returns `sessionExpired: true`, the dashboard page (or a wrapper component) should show a 'Session expired' toast notification and redirect the user to / (the root/login page). Wire this up in the dashboard page component that renders SyncStatusPanel — watch the `sessionExpired` value and trigger the redirect + toast when it becomes true.

**403 Access Denied** — When the hook returns an error with type '403', the SyncStatusPanel should show an inline 'Access Denied' banner with a link back to /dashboard (or just a message if the user is already on the dashboard). Don't redirect — keep the user on the page.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure the SyncStatusPanel is properly wired into the dashboard and connected to all the right pieces.

Confirm that:
- [ ] SyncStatusPanel renders inside the existing /dashboard page in the ManualRefreshPanel area — the dashboard page still works as before with the new section added
- [ ] The 'View Details' link in the CriticalAnomaliesStrip navigates to or scrolls to the reconciliation section correctly
- [ ] Circuit breaker status (Open/Closed badge) is driven by the `circuitBreakerOpen` field from GET /api/sync/live-state — no redundant call to /api/errors/circuit-breaker/status unless necessary
- [ ] The health banner uses `overallHealth` from GET /api/sync/live-state — no redundant call to GET /api/reconciliation/status unless the field is missing
- [ ] When `sessionExpired` becomes true, a 'Session expired' toast appears and the user is redirected to /
- [ ] When a 403 error occurs, an inline 'Access Denied' banner appears inside the panel — no redirect
- [ ] Phase updates written by PATCH /api/sync/progress are reflected in the panel within the next poll cycle (5 or 30 seconds depending on state)
- [ ] The rest of the dashboard (other sections, ManualRefreshPanel) still works normally with SyncStatusPanel added

Test the actual flow: Open /dashboard as an authenticated user → SyncStatusPanel loads in the correct position → health banner shows → all four network cards render → trigger a manual sync from ManualRefreshPanel → watch the corresponding network card switch to amber pulsing within 5 seconds (next poll) → sync completes → card turns green → click 'View Details' in the anomalies strip (if anomalies exist) → lands on the reconciliation section → sign out or let the session expire → 'Session expired' toast appears → redirected to /.

Then give me your honest advice on:
- Whether placing SyncStatusPanel in the ManualRefreshPanel area creates any layout or z-index conflicts
- Whether the session expiry redirect could fire prematurely (e.g., during a brief token refresh delay)
- Any circular dependencies between the dashboard sections (e.g., ManualRefreshPanel triggering syncs that SyncStatusPanel polls)
- What a new user would find confusing about how this section relates to the rest of the dashboard
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: