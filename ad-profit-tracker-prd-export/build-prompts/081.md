# Step 81 of 437: Add onboarding fields to Firestore security rules

================================================================================

**Category:** Database Setup
**Flow:** feature_user-onboarding-flow

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We're extending the existing users collection in Firestore with four new onboarding-related fields. We don't need a new collection — these fields live inside the existing users/{uid} document. For this step, I need you to update the Firestore security rules and make sure the data structure is ready.

The four new fields we're adding to users documents are: onboardingCompletedAt (a Firestore server timestamp, null until onboarding is done), onboardingSkipped (a boolean, true if the user clicked 'Skip for now' on the API keys step), preferences.timezone (an IANA timezone string like 'America/New_York'), and preferences.defaultDateRange (one of 'last7days', 'last30days', or 'thisMonth').

Update firestore.rules to make sure the following is enforced:
- Only authenticated users can read or write their own document (request.auth.uid == userId)
- The createdAt field is immutable — once set, it cannot be changed
- Deletes should be soft-deleted (mark a deleted flag) rather than hard-deleting the document
- Any authenticated user can create their own document (for the sync-user flow)

Also add a Firestore composite index if one doesn't already exist: we'll want to be able to query users by uid efficiently, but since uid is the document ID this is already indexed. No additional indexes are needed for the onboarding fields themselves.

If the project uses firestore.indexes.json, make sure it's up to date. Deploy the updated rules using the Firebase CLI.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure the Firestore rules are correct and the data structure is ready.

Confirm that:
- [ ] Security rules allow a user to read their own document (uid matches)
- [ ] Security rules allow a user to update their own document
- [ ] Security rules block a user from reading or writing another user's document
- [ ] The createdAt field cannot be changed once set
- [ ] An authenticated user can create their own document
- [ ] The rules deploy without errors via Firebase CLI

Test the actual flow: Use the Firebase Rules Playground or Emulator to simulate: (1) User A reads their own document — should succeed. (2) User A reads User B's document — should be denied. (3) User A updates their own document including onboardingCompletedAt — should succeed. (4) User A tries to change createdAt — should be denied. (5) Unauthenticated request reads any document — should be denied.

Then give me your honest advice on:
- Any gaps in the security rules that could expose user data
- Whether the soft-delete pattern is implemented consistently with the rest of the project
- Edge cases in the rules (what if preferences is null, what if the document doesn't exist yet)
- Anything you'd tighten before going to production
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: