# Step 299 of 437: Wire DailyProfitTrendSection into the dashboard

================================================================================

**Category:** Feature Wiring & Integration
**Flow:** feature_daily-profit-trend-visualization

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We need to connect the DailyProfitTrendSection into the existing FinancialMetricsDashboardPage so it behaves as a first-class part of the dashboard rather than an isolated widget.

**Date range sync:** The FinancialMetricsDashboardPage likely already has a date range selector at the top level. The DailyProfitTrendSection should read from that same date range state — don't create a second independent date range. If the dashboard manages its date range in local state or a context, pass it down as a prop to DailyProfitTrendSection. The section's own date range selector (Last 7 / 14 / 30 / This Month / Custom) should update the dashboard-level date range, not just its own local state. This way, all sections of the dashboard stay in sync.

**Placement:** Insert the DailyProfitTrendSection as a full-width card directly below the KPI row on FinancialMetricsDashboardPage. Don't add a new route — this is purely a layout addition to the existing page.

**Snapshot drill-down:** When a user clicks a data point on the chart, the DailyProfitTrendSection calls GET /api/stats/snapshot for that day. Show the result in a modal or slide-over panel that appears on top of the dashboard. This panel should be dismissible (click outside or press Escape) and should not navigate away from /dashboard.

**ManualRefreshPanel link:** The empty state CTA 'Sync Now' should link to wherever the ManualRefreshPanel lives in the dashboard. If it's a section on the same page, scroll to it. If it's a separate panel or modal, open it.

**Export button wiring:** The 'Export Chart' button uses recharts' built-in PNG export. Make sure the recharts ref is properly attached to the AreaChart component so the export captures the full chart including the breakeven line, moving average overlay, and data point markers.

**Toast notifications:** Wire up the 'Session expired' toast that fires when a 401 token refresh fails and the user is redirected to /. Use whatever toast system the rest of the FinancialMetricsDashboardPage already uses — don't introduce a new one.

**Loading coordination:** If the parent FinancialMetricsDashboardPage has a global loading state (e.g., for the KPI row), make sure the DailyProfitTrendSection's loading state is independent — it shouldn't block or be blocked by other sections loading.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure the DailyProfitTrendSection is properly wired into the FinancialMetricsDashboardPage.

Confirm that:
- [ ] DailyProfitTrendSection appears below the KPI row on /dashboard — no new route exists
- [ ] Changing the date range in DailyProfitTrendSection updates the dashboard-level date range (other sections respond too)
- [ ] Changing the date range from the dashboard's top-level selector also updates the chart
- [ ] Clicking a data point opens the day snapshot panel without navigating away from /dashboard
- [ ] The snapshot panel is dismissible by clicking outside or pressing Escape
- [ ] The 'Sync Now' CTA in the empty state correctly links to or opens the ManualRefreshPanel
- [ ] The 'Export Chart' button downloads a PNG that includes all chart overlays
- [ ] The 'Session expired' toast appears when a 401 refresh fails and the user is redirected to /
- [ ] The DailyProfitTrendSection's loading state is independent from other dashboard sections

Test the actual flow: Open /dashboard → see DailyProfitTrendSection below the KPI row → change the date range using the section's selector → confirm other dashboard sections also update → change the date range from the top-level dashboard selector → confirm the chart updates → click a data point → snapshot panel opens over the dashboard → press Escape → panel closes → trigger the empty state (select a range with no data) → click 'Sync Now' → ManualRefreshPanel opens or scrolls into view → click 'Export Chart' → PNG downloads with all overlays visible.

Then give me your honest advice on:
- Whether the date range sync approach could cause infinite re-render loops
- Any layout shift or visual jank when the section loads independently from the rest of the dashboard
- Edge cases in the snapshot panel (what if GET /api/stats/snapshot returns 404 for that day?)
- What a real user would find confusing or broken about the integration
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: