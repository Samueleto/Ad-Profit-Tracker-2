# Step 291 of 437: Integrate geo section into the dashboard

================================================================================

**Category:** Feature Wiring & Integration
**Flow:** feature_geographic-performance-breakdown

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We're wiring the geographic performance breakdown into the existing FinancialMetricsDashboardPage. This step is about connecting all the pieces together so the feature works end-to-end within the dashboard.

**Placement in FinancialMetricsDashboardPage** — Import GeoBreakdownSection and add it directly below the DailyProfitTrendSection card in the dashboard layout. No new route is needed — this is a section within the existing /dashboard page.

**Date range prop/context** — Pass the dashboard's active date range down to GeoBreakdownSection. If the dashboard uses a React context for shared state, consume it inside GeoBreakdownSection. If it uses props, thread the date range and any date-change handlers through. Make sure GeoBreakdownSection re-renders (and re-fetches) whenever the parent date range changes.

**Modal wiring** — GeoCountryDrilldownModal should be rendered inside GeoBreakdownSection (not at the page level), controlled by the selected country state. When a country row is clicked, set the selected country and open the modal. When the modal's close button is clicked or the backdrop is clicked, clear the selected country and close the modal. The modal needs access to the active date range to pass to GET /api/stats/snapshot — make sure this flows through.

**'View Full Trend' link** — Inside GeoCountryDrilldownModal, the 'View Full Trend' link should navigate to the DailyProfitTrendSection on the same dashboard page, ideally by scrolling to it and/or applying a country pre-filter if the DailyProfitTrendSection supports filtering. If it doesn't support country filtering yet, the link should at minimum scroll to the DailyProfitTrendSection section. Close the modal before or during this navigation.

**'Sync Now' CTA** — In the empty state of GeoBreakdownSection, the 'Sync Now' button should link to or open the ManualRefreshPanel. Wire this up however the ManualRefreshPanel is currently accessed in the dashboard (link, modal trigger, or navigation).

**Session expiry handling** — If any API call returns a 401, the component should attempt to refresh the Firebase ID token using getIdToken(true). If the refresh succeeds, retry the failed request once. If the refresh fails, close any open modal, redirect the user to / (the root/login page), and show a 'Session expired' toast notification.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure everything is properly wired together in the dashboard.

Confirm that:
- [ ] GeoBreakdownSection appears in FinancialMetricsDashboardPage below DailyProfitTrendSection
- [ ] Changing the dashboard date range causes GeoBreakdownSection to re-fetch its data
- [ ] Clicking a country row opens GeoCountryDrilldownModal with the correct country name and flag
- [ ] The modal receives the active date range and uses it for GET /api/stats/snapshot
- [ ] Clicking the modal backdrop or close button dismisses the modal and clears the selected country
- [ ] The 'View Full Trend' link closes the modal and scrolls to / navigates to DailyProfitTrendSection
- [ ] The 'Sync Now' CTA in the empty state correctly links to or opens the ManualRefreshPanel
- [ ] A 401 response triggers a token refresh attempt before redirecting
- [ ] After a successful token refresh, the failed request is retried automatically
- [ ] A failed token refresh redirects to / with a 'Session expired' toast

Test the actual flow: Open /dashboard → scroll down to confirm GeoBreakdownSection is below DailyProfitTrendSection → change the date range in the dashboard header → GeoBreakdownSection re-fetches → click a country row → modal opens with correct country → check the chart uses the same date range as the dashboard → click 'View Full Trend' → modal closes and DailyProfitTrendSection is in view → go back to GeoBreakdownSection → click a different country → modal opens with the new country's data → click the backdrop → modal closes. Simulate a 401 by expiring the token → verify redirect to / with toast.

Then give me your honest advice on:
- Whether the modal rendering location (inside GeoBreakdownSection vs. portal to document body) could cause z-index or overflow issues
- How the 'View Full Trend' navigation behaves if DailyProfitTrendSection doesn't support country filtering
- Any prop-drilling pain points that suggest a context refactor
- What would break if the dashboard's date range state management changes in the future
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: