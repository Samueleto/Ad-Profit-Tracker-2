# Step 392 of 437: Handle failures gracefully across all error panels

================================================================================

**Category:** Error Handling
**Flow:** feature_error-handling-retry-logic

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We're building an ad network sync dashboard. For this step, I need you to make sure the error monitoring feature itself fails gracefully — it would be ironic if the error monitoring UI crashed without a helpful message.

**API-level error handling** — Each of the seven endpoints should catch Firestore errors and return a clean 500 response with a generic message (never expose raw Firestore error details or stack traces to the client). Specifically: if `admin.auth().verifyIdToken()` throws (not just returns invalid), catch it and return 401. If a `getDocs` or `updateDoc` call throws, catch it, log the error server-side, and return `{ error: 'Failed to read data. Please try again.' }` with status 500.

**Panel-level error states** — Each of the five UI panels (`CircuitBreakerStatusPanel`, `ErrorLogPanel`, `RetryStatePanel`, `ErrorSummaryWidget`, `RetryConfigPanel`) should handle three states distinctly:
- *Loading*: show skeleton placeholders that match the panel's layout (not a generic spinner)
- *Error*: show an inline error message with a 'Retry' button that re-triggers the fetch. Don't show a full-page error — these are panels inside a dashboard, so keep the error contained
- *Empty*: show a friendly message specific to the panel (e.g., 'No errors in the last 7 days' for the summary, 'All circuits are closed' for the circuit breaker panel)

**Mutation error handling** — For `POST /api/errors/circuit-breaker/reset` and `PATCH /api/errors/retry-config`:
- 400 (validation error): show the specific validation message inline next to the relevant field or button
- 404 (network not found): show 'This network is not configured yet'
- 429 (rate limited): show 'Too many attempts — you can try again in X minutes' (parse the `Retry-After` header to compute X)
- 500: show 'Something went wrong. The change may not have been saved. Please try again.'
- Network failure (fetch threw): show 'Connection error — check your internet and try again'

**Circuit breaker reset confirmation** — Before calling `POST /api/errors/circuit-breaker/reset`, show a confirmation dialog: 'Are you sure you want to reset the circuit breaker for [network]? This will allow sync attempts to resume immediately.' This prevents accidental resets. After a successful reset, show a success toast: 'Circuit breaker reset. Sync will resume on the next trigger.'

**Stale data warnings** — If the circuit breaker status auto-refresh fails (network error during background poll), don't show an error banner — just show a subtle 'Last updated X minutes ago' timestamp on the panel so users know the data might be stale. Only show an error if the initial load fails.

**Token expiry** — If any fetch returns 401, it means the Firebase ID token has expired. Trigger a token refresh silently using the Firebase Auth SDK. If the refresh succeeds, retry the failed request once. If the refresh fails (user session ended), redirect to the login page with a message: 'Your session has expired. Please sign in again.'

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure the error monitoring feature handles its own failures gracefully.

Confirm that:
- [ ] Each panel shows a skeleton loader (not a spinner) while fetching
- [ ] Each panel shows an inline error with a 'Retry' button if the fetch fails — not a full-page crash
- [ ] `CircuitBreakerStatusPanel` shows 'All circuits are closed' when no circuits are open
- [ ] `ErrorSummaryWidget` shows 'No errors in the last 7 days' when there are no failures
- [ ] Clicking 'Reset Circuit' shows a confirmation dialog before calling the API
- [ ] A successful reset shows a success toast
- [ ] A 429 on reset shows 'Too many attempts — try again in X minutes' with the correct time from `Retry-After`
- [ ] A 404 on reset shows 'This network is not configured yet'
- [ ] A 500 from any endpoint shows a generic 'Something went wrong' message (no stack traces)
- [ ] If the background auto-refresh fails, the panel shows 'Last updated X minutes ago' instead of an error banner
- [ ] A 401 response triggers a silent token refresh and retries the request once
- [ ] If the token refresh fails, the user is redirected to login with a session expiry message
- [ ] Raw Firestore errors never appear in API responses

Test the actual flow: Open the dashboard with network throttling set to 'Offline' → all panels show skeleton then inline error with Retry button. Turn network back on → click Retry → panels load. Click 'Reset Circuit' → confirmation dialog appears → cancel → nothing happens → confirm → success toast appears. Simulate a 429 by hitting the reset limit → see the rate limit message with correct minutes. Expire the Firebase token manually → make a request → token refreshes silently → request succeeds. Fully invalidate the session → make a request → redirected to login with expiry message.

Then give me your honest advice on:
- Whether the silent token refresh retry loop could cause infinite retries if the backend keeps returning 401 for a reason other than token expiry
- Whether the 'Last updated X minutes ago' stale data indicator is visible enough — users might make decisions based on stale circuit breaker state
- Edge cases in the confirmation dialog — what if the user double-clicks 'Confirm' and fires two reset requests simultaneously?
- What error scenarios we haven't covered that could leave the UI in a broken or misleading state
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: