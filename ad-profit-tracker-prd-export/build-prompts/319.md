# Step 319 of 437: Wire PDF tab into the existing ExportModal

================================================================================

**Category:** Feature Wiring & Integration
**Flow:** feature_pdf-export-functionality

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We're wiring the PdfExportTab into the existing ExportModal that was already built for the Excel export feature. The goal is to make the modal feel like one cohesive two-tab experience, not two separate things bolted together.

Here's what needs to happen:

Open the existing ExportModal component and add a second tab called 'PDF' alongside the existing 'Excel' tab. The tab switcher should already exist — just register the new tab and render PdfExportTab when it's active.

The ExportModal already fetches GET /api/export/preview when it opens. Make sure the preview data (row counts per section) is accessible to PdfExportTab without triggering a second fetch. If the modal stores this in local state, pass it down as a prop to PdfExportTab. If it uses a context, make sure PdfExportTab reads from the same context. The shimmer loading state in PdfExportTab should be driven by the same loading flag the Excel tab uses.

The date range (dateFrom and dateTo) is managed at the ExportModal level. Pass these values into PdfExportTab (or make them available via context) so the export handler can include them in the POST /api/export/pdf request.

The modal's close function also needs to be accessible to PdfExportTab — the Cancel button and the post-success auto-close both need to call it. Pass it as a prop or wire it through context.

Make sure the toast notification system used by the Excel tab is also used by PdfExportTab for the 'PDF export complete — file downloaded' message. Don't introduce a second toast library — reuse whatever's already there.

Finally, verify that switching between the Excel and PDF tabs doesn't reset the date range or lose the preview data. The shared state should persist across tab switches within the same modal session.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure the PDF tab is properly wired into the ExportModal and everything works together.

Confirm that:
- [ ] The ExportModal shows two tabs: 'Excel' and 'PDF'
- [ ] Clicking the 'PDF' tab renders PdfExportTab
- [ ] Switching between tabs doesn't re-fetch GET /api/export/preview
- [ ] The row count badges in PdfExportTab show the same numbers as the Excel tab
- [ ] The shimmer loading state in PdfExportTab is driven by the same loading flag as the Excel tab
- [ ] The date range selected in the modal is correctly passed to PdfExportTab and included in the POST /api/export/pdf request
- [ ] The Cancel button in PdfExportTab closes the ExportModal
- [ ] The post-success auto-close (1.5 seconds) closes the ExportModal
- [ ] The success toast uses the same toast system as the Excel tab
- [ ] Switching from PDF tab to Excel tab and back doesn't reset the PDF tab's checkbox selections

Test the actual flow: Open the ExportModal on /dashboard → it defaults to the Excel tab → click 'PDF' tab → see the same row count badges (no loading flash, data already there) → configure some PDF options → switch back to Excel tab → switch back to PDF tab → options are still set → click Export Now → file downloads → toast appears → modal closes. Reopen the modal → preview data loads fresh.

Then give me your honest advice on:
- Whether the shared state approach between tabs is clean or creates hidden dependencies
- What happens if the Excel tab and PDF tab are both mid-operation somehow
- Whether the tab switch preserving state is the right UX (vs. resetting on switch)
- Anything that would break if the ExportModal is refactored in the future
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: