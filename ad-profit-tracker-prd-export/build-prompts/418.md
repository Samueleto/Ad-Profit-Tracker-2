# Step 418 of 437: Test the dark mode and theming feature

================================================================================

**Category:** Testing
**Flow:** feature_dark-mode-ui-theming

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

Let's add tests to make sure the theming feature works correctly and doesn't break when things go wrong.

**Unit tests for useThemePreferences hook:**
- On mount, it calls GET /api/settings/preferences with a Bearer token
- After a successful fetch, the working state matches the returned `themePreference` and `accentColor`
- Calling `savePreferences()` sends PATCH /api/settings/preferences with the correct body
- Calling `resetToDefaults()` sets themePreference to 'light' and accentColor to '#6366f1'
- Hex validation: '#ff6600' passes, '#gggggg' fails, 'javascript:alert(1)' fails, '#fff' passes (3-char hex), '#ffffff' passes
- A 401 response triggers a token refresh and retry
- A 404 response results in default values being used

**Component tests for ThemeSettingsSection:**
- Renders shimmer placeholders while loading
- After loading, the segmented control reflects the saved themePreference
- The active accent swatch shows a checkmark
- Clicking a different theme option calls setTheme() from next-themes
- Clicking 'Custom' reveals the hex input field
- The live preview strip updates when theme or accent changes
- The 'Save Appearance' button is disabled while the hex input is invalid
- A successful save shows a 'Saved' toast
- A failed save shows 'Failed to save. Please try again.' inline
- A 403 response renders the 'Access Denied' message and hides the controls
- A 500 response renders the error banner with a Retry button
- Clicking Retry re-fetches preferences

**Integration test (end-to-end):**
- Navigate to /settings → click the Appearance tab → see controls load → change theme to Dark → entire page goes dark → change accent to rose → live preview updates → click Save → 'Saved' toast appears → refresh → Dark theme and rose accent are still applied → click 'Reset to Defaults' → Light + indigo restored → Save fires automatically.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure all the tests pass and cover the important paths.

Confirm that:
- [ ] All unit tests for useThemePreferences pass (fetch, save, reset, hex validation, 401 retry, 404 defaults)
- [ ] All component tests for ThemeSettingsSection pass (loading, populated, interactions, error states)
- [ ] The integration test passes end-to-end
- [ ] Hex validation tests cover valid 3-char, valid 6-char, invalid chars, and injection attempts
- [ ] The 403 and 500 error state tests confirm the right UI is shown
- [ ] The 'Saved' toast test confirms it appears after a successful PATCH

Test the actual flow: Run the full test suite with no failures. Then manually run the integration test: open /settings → Appearance tab → change to Dark → page goes dark → change accent → preview updates → save → toast → refresh → still dark with new accent → reset → Light + indigo → auto-save fires.

Then give me your honest advice on:
- What's not covered by these tests that could break in production (e.g., next-themes SSR behavior, CSS property application in jsdom)
- Whether the component tests are testing behavior or implementation details
- Any flaky test risks (timing, async state updates, toast visibility)
- Tests you'd add before feeling confident about a release
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: