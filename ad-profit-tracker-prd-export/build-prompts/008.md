# Step 8 of 437: Set up folder structure for RollerAds integration

================================================================================

**Category:** Project Scaffolding
**Flow:** feature_rollerads-integration

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We're building a RollerAds ad network integration into an existing Next.js + Firebase app. For this step, set up the folder structure so everything has a home before we start writing logic.

Create the following API route folders under `src/app/api/networks/rollerads/` (or `pages/api/networks/rollerads/` if the project uses the Pages Router — match whatever convention the project already uses):
- `sync/` — for the manual sync endpoint (POST /api/networks/rollerads/sync)
- `stats/` — for the main stats endpoint (GET /api/networks/rollerads/stats)
- `stats/by-country/` — for the country breakdown endpoint (GET /api/networks/rollerads/stats/by-country)
- `stats/latest/` — for the dashboard summary endpoint (GET /api/networks/rollerads/stats/latest)
- `raw-response/` — for the API Explorer endpoint (GET /api/networks/rollerads/raw-response)
- `scheduled-sync/` — for the Cloud Scheduler endpoint (POST /api/networks/rollerads/scheduled-sync)

Also create a shared utilities folder at `src/lib/rollerads/` (or wherever the project keeps shared server-side helpers). We'll put the RollerAds axios client factory and any RollerAds-specific helpers here.

Finally, create a `src/lib/rollerads/fieldSchema.ts` file as a placeholder — we'll populate it with the RollerAds /sites/stat field definitions when we build the raw-response endpoint.

Don't write any logic yet — just make sure the folder structure is in place and the project still builds.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure the folder structure is set up correctly.

Confirm that:
- [ ] All six API route files exist under `src/app/api/networks/rollerads/` (or the equivalent Pages Router paths)
- [ ] The `src/lib/rollerads/` shared utilities folder exists
- [ ] The project still builds without errors after adding these files
- [ ] No existing routes or imports are broken

Test the actual flow: Run `npm run build` (or `next build`) and confirm it completes without errors. This is just scaffolding — no logic yet.

Then give me your honest advice on:
- Whether this folder structure fits the project's existing API conventions
- If there's a better place for the shared RollerAds utilities given the project's current layout
- Anything you'd reorganize before we start writing real code
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: