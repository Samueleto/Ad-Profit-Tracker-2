# Step 137 of 437: Create TypeScript types for comparison payload

================================================================================

**Category:** Schema & Model Definitions
**Flow:** feature_comparative-network-analysis

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

The GET /api/networks/comparison endpoint returns a fairly rich consolidated payload. For this step, create the TypeScript types that both the API route and the frontend components will share.

Create a types file (something like src/features/comparative-analysis/types/comparison.ts) and define the following interfaces that mirror the endpoint's response shape:

A NetworkComparisonItem type covering: networkId (string), dataRole ('cost' | 'revenue'), primaryMetric (number), impressions (number), clicks (number), averageCtr (number), averageCpm (number), daysWithData (number), metricShare (number), rank (number), and a nested networkStatus object with lastSyncedAt (string | null), lastSyncStatus (string), and circuitBreakerOpen (boolean).

A CrossNetworkMetrics type covering: totalRevenue (number), totalCost (number), netProfit (number), overallRoi (number | null), revenuePerImpression (number | null), costPerClick (number | null).

A NetworkRankingItem type covering: networkId (string), rank (number), metricValue (number), metricLabel (string).

A ComparisonResponse type that wraps everything: dateFrom (string), dateTo (string), metric (string), networks (NetworkComparisonItem[]), crossNetwork (CrossNetworkMetrics), rankings (NetworkRankingItem[]), cachedAt (string | null).

Also define a ComparisonMetric union type: 'revenue' | 'cost' | 'roi' | 'impressions' | 'clicks' | 'ctr' | 'cpm' — this gets used both in the API validation and in the MetricToggle component on the frontend.

Export all of these. The API route will import them for type-safety on the response, and the frontend components will import them for prop types.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure the types are correct and usable.

Confirm that:
- [ ] ComparisonResponse, NetworkComparisonItem, CrossNetworkMetrics, NetworkRankingItem, and ComparisonMetric are all exported from the types file
- [ ] NetworkComparisonItem includes all fields: networkId, dataRole, primaryMetric, impressions, clicks, averageCtr, averageCpm, daysWithData, metricShare, rank, and the nested networkStatus object
- [ ] CrossNetworkMetrics has overallRoi, revenuePerImpression, and costPerClick typed as number | null (not just number)
- [ ] ComparisonMetric union covers all 7 allowed values
- [ ] The project compiles without TypeScript errors after adding these types

Test the actual flow: Import ComparisonResponse in a test file and create a mock object that satisfies the type — TypeScript should accept it without errors. Try creating an object with a missing field — TypeScript should flag it. Try assigning an invalid string to ComparisonMetric — TypeScript should reject it.

Then give me your honest advice on:
- Whether these types fully match what the endpoint will actually return
- Any fields that should be optional (?) vs. required given real-world data gaps
- Whether it makes sense to share these types with the existing adStats/networkConfigs type definitions already in the project
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: