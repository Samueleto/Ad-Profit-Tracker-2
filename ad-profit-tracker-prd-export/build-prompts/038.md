# Step 38 of 437: Set up RBAC folder structure

================================================================================

**Category:** Project Scaffolding
**Flow:** feature_role-based-access-control

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

We're building a Role-Based Access Control system on top of an existing Next.js + Firebase app. For this step, set up the folder structure so everything has a home before we start writing real code.

On the frontend, create an rbac feature area under src/features/rbac/ with subfolders for: components (we'll have PermissionsTab and RolePermissionsEditModal here), hooks (for fetching permissions data), and types (TypeScript interfaces for roles and permissions).

On the API side, create the route handlers folder at src/app/api/rbac/. Inside it, create three subfolders: permissions/ (for the GET and PATCH handlers on /api/rbac/permissions), my-permissions/ (for GET /api/rbac/my-permissions), and check/ (for POST /api/rbac/check).

Also create a shared server utility folder at src/lib/rbac/ — we'll put the system defaults config and permission-checking helpers here. These will be imported by multiple API routes, so keeping them in a shared lib folder avoids duplication.

The goal is just clean scaffolding — no logic yet, just the folders and maybe empty index files so the project still compiles.

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure the folder structure is set up correctly.

Confirm that:
- [ ] src/features/rbac/ exists with components/, hooks/, and types/ subfolders
- [ ] src/app/api/rbac/ exists with permissions/, my-permissions/, and check/ subfolders
- [ ] src/lib/rbac/ exists for shared server utilities
- [ ] The project still builds without errors after adding these folders

Test the actual flow: Run the dev server or a build — this is scaffolding only, so nothing should break.

Then give me your honest advice on:
- Whether this folder structure fits well with the existing project layout
- If there's a better place for the shared rbac lib utilities given how the project is already organized
- Anything you'd reorganize before we start building the actual feature
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: