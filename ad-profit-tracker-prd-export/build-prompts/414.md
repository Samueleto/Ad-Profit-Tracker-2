# Step 414 of 437: Test the Excel export feature end-to-end

================================================================================

**Category:** Testing
**Flow:** feature_excel-export-functionality

================================================================================

## 📋 PROMPT - Copy Everything Below and Paste into Your AI Assistant

================================================================================

Let's add tests to make sure the export feature works correctly and doesn't break.

**Unit tests:**

- Sheet key mapping: test that the UI checkboxes map to the correct API sheet keys — especially that the 'Per-Network Stats' checkbox expands to ['exoclick', 'rollerads', 'zeydoo', 'propush']
- Filename sanitization: test the server-side filename validator with valid inputs ('my-export_2024'), invalid inputs ('../etc/passwd', 'file name with spaces', a string over 100 chars), and empty input (should fall back to default)
- Date range validation: test the server-side validator with valid ranges, dateFrom > dateTo (should fail), ranges over 90 days (should fail), and invalid date formats
- Rate limit logic: test that the 11th request within an hour returns 429
- Preview row count computation: given a mock set of adStats documents, test that daily_trend count equals unique dates, geo_breakdown equals unique countries, and each network count is correct
- Blob download helper: test that the function creates an anchor element, sets the correct href and download attribute, clicks it, and revokes the object URL

**Component tests (React Testing Library):**

- ExportModal renders in loading state (5 shimmer rows visible, Export Now disabled)
- ExportModal renders with preview data (row count badges visible, Export Now enabled)
- ExportModal renders in empty state (hasData: false → Export Now disabled with tooltip)
- ExportModal renders in exporting state (spinner on button, checkboxes disabled, progress message visible)
- ExportModal renders error states: 403 shows 'Access Denied', 500 shows error banner with Retry button, 429 shows rate limit message
- Unchecking all checkboxes disables Export Now
- Advanced section expands and collapses on chevron click
- Cancel button calls closeExportModal
- Escape key calls closeExportModal when not exporting
- Escape key does NOT call closeExportModal when exportStatus is 'exporting'

**Integration tests:**

- Full happy path: open modal → preview loads → select sheets → click Export Now → file downloads → toast appears → modal closes
- Error recovery: Export Now returns 500 → error banner appears → click Retry → succeeds
- Token expiry: mock a 401 response → token refresh is attempted → on refresh failure, redirect to /

**API endpoint tests (using a test Firebase project or mocked admin SDK):**

- GET /api/export/preview: returns 401 without token, returns 400 for invalid dates, returns correct row counts for valid request, returns cached response on second call (X-Cache: HIT header)
- POST /api/export/excel: returns 401 without token, returns 400 for invalid sheets, returns 400 for >90 day range, returns 400 for invalid filename, returns 429 after rate limit, returns a valid .xlsx binary for a successful request, writes an audit log entry after success

================================================================================

---

## 🤖 Verification Prompt

**After implementing this step**, copy the prompt below and paste it into your AI assistant
to verify your implementation. You can paste this multiple times to iteratively improve
your code until all criteria are met.

```
Let's make sure all tests pass and the important paths are covered.

Confirm that:
- [ ] All unit tests pass (sheet key mapping, filename sanitization, date validation, rate limit, row count computation, blob download)
- [ ] All component tests pass (loading, ready, empty, exporting, error states, keyboard interactions)
- [ ] All API endpoint tests pass (auth, validation, rate limiting, successful export, audit log)
- [ ] Integration tests pass (happy path, error recovery, token expiry)
- [ ] The test suite runs without flaky failures

Test the actual flow: Run the full test suite with no failures. Then do a manual end-to-end test: open the dashboard → click Export → wait for row counts → select 3 sheets → enter a custom filename → click Export Now → file downloads with the correct name → toast appears → modal closes. Then reopen and test the error path: disconnect from the network → click Export Now → error banner appears → reconnect → click Retry → file downloads.

Then give me your honest advice on:
- What important scenarios aren't covered by these tests that could break in production
- Whether the component tests are testing behavior (what the user sees) vs. implementation details (internals)
- Any flaky test risks (timing issues with the 1.5s modal close timeout, async token refresh, blob URL creation)
- What you'd want covered before feeling confident deploying this to real users
```

---

*Generated by VisualPRD - Self-contained prompt with embedded entity context*

Need more details? Check the individual entity files: