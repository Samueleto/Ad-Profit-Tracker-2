# workspaceInvitations

## Description

PURPOSE: Stores pending, accepted, declined, and revoked workspace invitations

FIELDS:
id: string
├── Purpose: Auto-generated Firestore document ID
│   ├── Constraints: required
│   └── Mutable: no

workspaceId: string
├── Purpose: The workspace (owner's uid) this invitation is for
│   ├── Constraints: required
│   └── Mutable: no

workspaceName: string
├── Purpose: Denormalized workspace name for display in invitation email and accept page without extra reads
│   ├── Constraints: required
│   └── Mutable: yes

invitedByUid: string
├── Purpose: UID of the user who sent the invitation — always from verified token
│   ├── Constraints: required
│   └── Mutable: no

invitedByName: string
├── Purpose: Denormalized display name of inviting user for invitation email rendering
│   ├── Constraints: required
│   └── Mutable: yes

invitedEmail: string
├── Purpose: Email address the invitation was sent to — used to verify accepting user's identity
│   ├── Constraints: required
│   └── Mutable: yes

role: string
├── Purpose: Role being offered: 'admin' or 'member' — never 'owner'
│   ├── Constraints: required
│   └── Mutable: yes

token: string
├── Purpose: Hashed UUID v4 token embedded in the invitation link — single-use
│   ├── Constraints: required
│   └── Mutable: yes

tokenHash: string
├── Purpose: SHA-256 hash of the token for secure server-side lookup without storing plaintext
│   ├── Constraints: required
│   └── Mutable: yes

personalMessage: string
├── Purpose: Optional personal message from the inviting user, max 200 chars
│   └── Mutable: yes

status: string
├── Purpose: Enum: pending | accepted | declined | revoked | expired
│   ├── Constraints: required
│   └── Mutable: yes

expiresAt: timestamp
├── Purpose: Invitation expires 7 days after creation — enforced server-side on accept
│   ├── Constraints: required
│   └── Mutable: yes

acceptedAt: timestamp
├── Purpose: Server timestamp when invitation was accepted — null until accepted
│   └── Mutable: yes

acceptedByUid: string
├── Purpose: UID of the user who accepted — set on acceptance, null until then
│   └── Mutable: no

createdAt: timestamp
├── Purpose: Server timestamp at creation — immutable
│   ├── Constraints: required
│   └── Mutable: no

updatedAt: timestamp
└── Purpose: Server timestamp updated on status changes
    ├── Constraints: required
    └── Mutable: yes

FIRESTORE SECURITY RULES:
- Create: [AUTH_USER_ID] != null
- Read: [AUTH_USER_ID] != null
- Update: [user_id] == [AUTH_USER_ID]
- Delete: [user_id] == [AUTH_USER_ID]
- Immutable: createdAt

LIFECYCLE:
- Created: set [SERVER_TIMESTAMP] for createdAt
- Updated: update [UPDATED_AT]

## Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| id | string | Yes | Auto-generated Firestore document ID |
| workspaceId | string | Yes | The workspace (owner's uid) this invitation is for |
| workspaceName | string | Yes | Denormalized workspace name for display in invitation email and accept page without extra reads |
| invitedByUid | string | Yes | UID of the user who sent the invitation — always from verified token |
| invitedByName | string | Yes | Denormalized display name of inviting user for invitation email rendering |
| invitedEmail | string | Yes | Email address the invitation was sent to — used to verify accepting user's identity |
| role | string | Yes | Role being offered: 'admin' or 'member' — never 'owner' |
| token | string | Yes | Hashed UUID v4 token embedded in the invitation link — single-use |
| tokenHash | string | Yes | SHA-256 hash of the token for secure server-side lookup without storing plaintext |
| personalMessage | string | No | Optional personal message from the inviting user, max 200 chars |
| status | string | Yes | Enum: pending \| accepted \| declined \| revoked \| expired |
| expiresAt | timestamp | Yes | Invitation expires 7 days after creation — enforced server-side on accept |
| acceptedAt | timestamp | No | Server timestamp when invitation was accepted — null until accepted |
| acceptedByUid | string | No | UID of the user who accepted — set on acceptance, null until then |
| createdAt | timestamp | Yes | Server timestamp at creation — immutable |
| updatedAt | timestamp | Yes | Server timestamp updated on status changes |

---
Generated by VisualPRD