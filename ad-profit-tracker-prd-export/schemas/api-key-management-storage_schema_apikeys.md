# apiKeys

## Description

PURPOSE: Stores users' encrypted API keys for various ad networks

FIELDS:
encryptedKey: string
├── Purpose: The ad network API key, encrypted on the server before being stored.
│   ├── Constraints: required
│   └── Mutable: yes

networkId: string
├── Purpose: Identifier for the ad network (e.g., 'exoclick', 'rollerads').
│   ├── Constraints: required
│   └── Mutable: no

userId: string
├── Purpose: The UID of the user who owns this key. Used for security rules.
│   ├── Constraints: required
│   └── Mutable: no

createdAt: timestamp
├── Purpose: Server timestamp of when the key was first added.
│   ├── Constraints: required
│   └── Mutable: no

updatedAt: timestamp
└── Purpose: Server timestamp of when the key was last updated.
    ├── Constraints: required
    └── Mutable: yes

FIRESTORE SECURITY RULES:
- Create: [AUTH_USER_ID] != null
- Read: [user_id] == [AUTH_USER_ID]
- Update: [user_id] == [AUTH_USER_ID]
- Delete: Soft delete (mark deleted, retain data)
- Immutable: createdAt, userId

LIFECYCLE:
- Created: set [SERVER_TIMESTAMP] for createdAt
- Updated: update [UPDATED_AT]

## Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| encryptedKey | string | Yes | The ad network API key, encrypted on the server before being stored. |
| networkId | string | Yes | Identifier for the ad network (e.g., 'exoclick', 'rollerads'). |
| userId | string | Yes | The UID of the user who owns this key. Used for security rules. |
| createdAt | timestamp | Yes | Server timestamp of when the key was first added. |
| updatedAt | timestamp | Yes | Server timestamp of when the key was last updated. |

---
Generated by VisualPRD