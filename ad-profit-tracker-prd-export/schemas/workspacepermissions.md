# workspacePermissions

## Description

PURPOSE: Stores per-workspace customizable role permission configurations

FIELDS:
id: string
├── Purpose: Auto-generated Firestore document ID
│   ├── Constraints: required
│   └── Mutable: no

workspaceId: string
├── Purpose: The workspace owner's uid — primary partition key
│   ├── Constraints: required
│   └── Mutable: no

rolePermissions: map
├── Purpose: Nested map: { admin: { canViewDashboard: true, canExportData: true, canManageApiKeys: true, canSyncNetworks: true, canViewReports: true, canCreateReports: true, canManageTeam: true, canViewAuditLogs: true, canManageBenchmarks: true }, member: { ... }, viewer: { canViewDashboard: true, canViewReports: true, ... }, analyst: { canViewDashboard: true, canViewReports: true, canCreateReports: true, canManageBenchmarks: true, ... } }. Owner permissions are always all-true and not stored here.
│   ├── Constraints: required
│   └── Mutable: yes

customizedRoles: array
├── Purpose: Array of role strings that have been customized from system defaults. Used to show 'customized' badge in UI.
│   └── Mutable: yes

createdAt: timestamp
├── Purpose: Server timestamp at document creation. Immutable.
│   ├── Constraints: required
│   └── Mutable: no

updatedAt: timestamp
├── Purpose: Server timestamp updated on every PATCH /api/rbac/permissions call.
│   ├── Constraints: required
│   └── Mutable: yes

updatedByUid: string
└── Purpose: UID of the user who last modified the permissions config. For audit context.
    └── Mutable: no

FIRESTORE SECURITY RULES:
- Create: [AUTH_USER_ID] != null
- Read: [AUTH_USER_ID] != null
- Update: [user_id] == [AUTH_USER_ID]
- Delete: [user_id] == [AUTH_USER_ID]
- Immutable: createdAt

LIFECYCLE:
- Created: set [SERVER_TIMESTAMP] for createdAt
- Updated: update [UPDATED_AT]

## Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| id | string | Yes | Auto-generated Firestore document ID |
| workspaceId | string | Yes | The workspace owner's uid — primary partition key |
| rolePermissions | map | Yes | Nested map: { admin: { canViewDashboard: true, canExportData: true, canManageApiKeys: true, canSyncNetworks: true, canViewReports: true, canCreateReports: true, canManageTeam: true, canViewAuditLogs: true, canManageBenchmarks: true }, member: { ... }, viewer: { canViewDashboard: true, canViewReports: true, ... }, analyst: { canViewDashboard: true, canViewReports: true, canCreateReports: true, canManageBenchmarks: true, ... } }. Owner permissions are always all-true and not stored here. |
| customizedRoles | array | No | Array of role strings that have been customized from system defaults. Used to show 'customized' badge in UI. |
| createdAt | timestamp | Yes | Server timestamp at document creation. Immutable. |
| updatedAt | timestamp | Yes | Server timestamp updated on every PATCH /api/rbac/permissions call. |
| updatedByUid | string | No | UID of the user who last modified the permissions config. For audit context. |

## Relationships


## Indexes


---
Generated by VisualPRD