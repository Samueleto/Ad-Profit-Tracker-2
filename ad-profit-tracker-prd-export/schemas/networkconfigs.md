# networkConfigs

## Description

PURPOSE: Stores per-user, per-network configuration for each of the four supported ad networks

FIELDS:
userId: string
├── Purpose: UID of the owning user. Duplicated on document for Firestore Security Rules enforcement.
│   ├── Constraints: required
│   └── Mutable: no

networkId: string
├── Purpose: One of: exoclick, rollerads, zeydoo, propush. Immutable after creation.
│   ├── Constraints: required
│   └── Mutable: no

isActive: boolean
├── Purpose: Whether this network participates in scheduled and manual data pulls. Default: true.
│   ├── Constraints: required
│   └── Mutable: yes

syncSchedule: string
├── Purpose: Cron-style schedule identifier: 'hourly', 'daily', 'weekly'. Controls Cloud Scheduler trigger frequency. Default: 'daily'.
│   ├── Constraints: required
│   └── Mutable: yes

dataRole: string
├── Purpose: Immutable role label: 'cost' (ExoClick) or 'revenue' (RollerAds, Zeydoo, Propush). Set at document creation, never changed.
│   ├── Constraints: required
│   └── Mutable: yes

endpointOverride: string
├── Purpose: Optional custom API base URL override for this network. Null means use the system default. Validated as a URL on write.
│   └── Mutable: yes

timeoutSeconds: number
├── Purpose: HTTP request timeout in seconds for API calls to this network. Range: 5–60. Default: 30.
│   ├── Constraints: required
│   └── Mutable: yes

retryAttempts: number
├── Purpose: Number of retry attempts on transient API failures. Range: 1–5. Default: 3.
│   ├── Constraints: required
│   └── Mutable: yes

lastSyncedAt: timestamp
├── Purpose: Server timestamp of the most recent successful data pull for this network. Written by Cloud Function after successful sync.
│   └── Mutable: yes

lastSyncStatus: string
├── Purpose: Status of the last sync attempt: 'success', 'partial', 'failed'. Written by Cloud Function.
│   └── Mutable: yes

lastSyncError: string
├── Purpose: Error message from the last failed sync attempt. Null on success. Written by Cloud Function.
│   └── Mutable: yes

displayOrder: number
├── Purpose: User-defined sort order for drag-and-drop reordering. Default: 0=exoclick, 1=rollerads, 2=zeydoo, 3=propush.
│   ├── Constraints: required
│   └── Mutable: yes

createdAt: timestamp
├── Purpose: Server timestamp when config document was first created.
│   ├── Constraints: required
│   └── Mutable: no

updatedAt: timestamp
└── Purpose: Server timestamp of last config update.
    ├── Constraints: required
    └── Mutable: yes

FIRESTORE SECURITY RULES:
- Create: [AUTH_USER_ID] != null
- Read: [user_id] == [AUTH_USER_ID]
- Update: [user_id] == [AUTH_USER_ID]
- Delete: Soft delete (mark deleted, retain data)
- Immutable: createdAt, userId

LIFECYCLE:
- Created: set [SERVER_TIMESTAMP] for createdAt
- Updated: update [UPDATED_AT]

## Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| userId | string | Yes | UID of the owning user. Duplicated on document for Firestore Security Rules enforcement. |
| networkId | string | Yes | One of: exoclick, rollerads, zeydoo, propush. Immutable after creation. |
| isActive | boolean | Yes | Whether this network participates in scheduled and manual data pulls. Default: true. |
| syncSchedule | string | Yes | Cron-style schedule identifier: 'hourly', 'daily', 'weekly'. Controls Cloud Scheduler trigger frequency. Default: 'daily'. |
| dataRole | string | Yes | Immutable role label: 'cost' (ExoClick) or 'revenue' (RollerAds, Zeydoo, Propush). Set at document creation, never changed. |
| endpointOverride | string | No | Optional custom API base URL override for this network. Null means use the system default. Validated as a URL on write. |
| timeoutSeconds | number | Yes | HTTP request timeout in seconds for API calls to this network. Range: 5–60. Default: 30. |
| retryAttempts | number | Yes | Number of retry attempts on transient API failures. Range: 1–5. Default: 3. |
| lastSyncedAt | timestamp | No | Server timestamp of the most recent successful data pull for this network. Written by Cloud Function after successful sync. |
| lastSyncStatus | string | No | Status of the last sync attempt: 'success', 'partial', 'failed'. Written by Cloud Function. |
| lastSyncError | string | No | Error message from the last failed sync attempt. Null on success. Written by Cloud Function. |
| displayOrder | number | Yes | User-defined sort order for drag-and-drop reordering. Default: 0=exoclick, 1=rollerads, 2=zeydoo, 3=propush. |
| createdAt | timestamp | Yes | Server timestamp when config document was first created. |
| updatedAt | timestamp | Yes | Server timestamp of last config update. |

## Relationships


## Indexes


---
Generated by VisualPRD