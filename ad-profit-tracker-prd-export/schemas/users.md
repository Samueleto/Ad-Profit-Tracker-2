# users

## Description

PURPOSE: Central user profile document for every authenticated user

FIELDS:
uid: string
├── Purpose: Firebase Auth UID — document ID matches this value
│   ├── Constraints: required
│   └── Mutable: no

email: string
├── Purpose: User email from Firebase Auth
│   ├── Constraints: required
│   └── Mutable: yes

displayName: string
├── Purpose: Display name from Google OAuth or manually set
│   └── Mutable: yes

photoURL: string
├── Purpose: Avatar URL from Google OAuth or null
│   └── Mutable: yes

authProvider: string
├── Purpose: google | email — tracks how user authenticated
│   ├── Constraints: required
│   └── Mutable: yes

createdAt: timestamp
├── Purpose: Firestore server timestamp on first sign-up
│   ├── Constraints: required
│   └── Mutable: no

lastLoginAt: timestamp
├── Purpose: Updated on every successful sign-in via sync-user endpoint
│   ├── Constraints: required
│   └── Mutable: yes

role: string
├── Purpose: Default: user. Future: admin. Used by Firestore Security Rules for access control
│   ├── Constraints: required
│   └── Mutable: yes

preferences: map
└── Purpose: Embedded user preferences object — avoids a separate collection. Contains: { defaultDateRange: string, timezone: string }
    └── Mutable: yes

FIRESTORE SECURITY RULES:
- Create: [AUTH_USER_ID] != null
- Read: [user_id] == [AUTH_USER_ID]
- Update: [user_id] == [AUTH_USER_ID]
- Delete: Soft delete (mark deleted, retain data)
- Immutable: createdAt, userId

LIFECYCLE:
- Created: set [SERVER_TIMESTAMP] for createdAt

## Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| uid | string | Yes | Firebase Auth UID — document ID matches this value |
| email | string | Yes | User email from Firebase Auth |
| displayName | string | No | Display name from Google OAuth or manually set |
| photoURL | string | No | Avatar URL from Google OAuth or null |
| authProvider | string | Yes | google \| email — tracks how user authenticated |
| createdAt | timestamp | Yes | Firestore server timestamp on first sign-up |
| lastLoginAt | timestamp | Yes | Updated on every successful sign-in via sync-user endpoint |
| role | string | Yes | Default: user. Future: admin. Used by Firestore Security Rules for access control |
| preferences | map | No | Embedded user preferences object — avoids a separate collection. Contains: { defaultDateRange: string, timezone: string } |

## Relationships


## Indexes


---
Generated by VisualPRD