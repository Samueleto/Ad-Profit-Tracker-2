# networkConfigs

## Description

PURPOSE: Adds retry state and circuit breaker fields to the existing networkConfigs collection

FIELDS:
retryCount: number
├── Purpose: Current consecutive failure count for this network. Incremented on each failed sync attempt, reset to 0 on success. Used to determine if circuit breaker threshold is reached.
│   └── Mutable: yes

nextRetryAt: timestamp
├── Purpose: Timestamp of the next scheduled automatic retry, computed using exponential backoff formula: baseDelay * 2^(retryCount-1) with jitter. Null when lastSyncStatus is 'success' or no retry is pending.
│   └── Mutable: yes

circuitBreakerOpen: boolean
├── Purpose: When true, all sync attempts for this network are skipped until manually reset or a cooldown period elapses. Set to true after retryAttempts consecutive failures. Default: false.
│   └── Mutable: yes

circuitBreakerOpenedAt: timestamp
├── Purpose: Server timestamp when the circuit breaker was last opened. Used to compute auto-reset after cooldown period (default 1 hour). Null when circuit is closed.
│   └── Mutable: yes

lastErrorCode: string
├── Purpose: Semantic error code from the last failure: 'NETWORK_TIMEOUT', 'AUTH_FAILURE', 'RATE_LIMITED', 'PARSE_ERROR', 'FIRESTORE_WRITE_FAILURE', 'DECRYPTION_FAILURE'. Null on success. Used to classify retryable vs non-retryable errors.
│   └── Mutable: yes

totalFailureCount: number
└── Purpose: Lifetime count of all failed sync attempts for this network. Never reset. Used for monitoring and alerting dashboards.
    └── Mutable: yes

FIRESTORE SECURITY RULES:
- Create: [AUTH_USER_ID] != null
- Read: [AUTH_USER_ID] != null
- Update: [user_id] == [AUTH_USER_ID]
- Delete: [user_id] == [AUTH_USER_ID]
- Immutable: createdAt

LIFECYCLE:
- Created: set [CREATED_AT]

## Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| retryCount | number | No | Current consecutive failure count for this network. Incremented on each failed sync attempt, reset to 0 on success. Used to determine if circuit breaker threshold is reached. |
| nextRetryAt | timestamp | No | Timestamp of the next scheduled automatic retry, computed using exponential backoff formula: baseDelay * 2^(retryCount-1) with jitter. Null when lastSyncStatus is 'success' or no retry is pending. |
| circuitBreakerOpen | boolean | No | When true, all sync attempts for this network are skipped until manually reset or a cooldown period elapses. Set to true after retryAttempts consecutive failures. Default: false. |
| circuitBreakerOpenedAt | timestamp | No | Server timestamp when the circuit breaker was last opened. Used to compute auto-reset after cooldown period (default 1 hour). Null when circuit is closed. |
| lastErrorCode | string | No | Semantic error code from the last failure: 'NETWORK_TIMEOUT', 'AUTH_FAILURE', 'RATE_LIMITED', 'PARSE_ERROR', 'FIRESTORE_WRITE_FAILURE', 'DECRYPTION_FAILURE'. Null on success. Used to classify retryable vs non-retryable errors. |
| totalFailureCount | number | No | Lifetime count of all failed sync attempts for this network. Never reset. Used for monitoring and alerting dashboards. |

---
Generated by VisualPRD