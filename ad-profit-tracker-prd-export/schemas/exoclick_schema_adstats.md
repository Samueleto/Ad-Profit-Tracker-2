# adStats

## Description

PURPOSE: Stores per-day, per-country ad network statistics fetched from ExoClick (and later other networks)

FIELDS:
id: string
├── Purpose: Auto-generated Firestore document ID
│   ├── Constraints: required
│   └── Mutable: no

userId: string
├── Purpose: UID of the owning user. Duplicated for Firestore Security Rules enforcement.
│   ├── Constraints: required
│   └── Mutable: no

networkId: string
├── Purpose: Ad network identifier — 'exoclick' for this feature. Indexed for per-network queries.
│   ├── Constraints: required
│   └── Mutable: no

date: string
├── Purpose: Date of the stat record in YYYY-MM-DD format. Indexed for date-range queries.
│   ├── Constraints: required
│   └── Mutable: yes

country: string
├── Purpose: ISO 3166-1 alpha-2 country code (e.g., 'US', 'DE'). Null for aggregate rows. Indexed for GEO breakdown queries.
│   └── Mutable: yes

cost: number
├── Purpose: Total ad spend cost in USD for this network/date/country combination. Set for ExoClick (cost-only network). Null for revenue-only networks.
│   └── Mutable: yes

revenue: number
├── Purpose: Total revenue in USD. Null for ExoClick (cost-only). Set for revenue networks.
│   └── Mutable: yes

impressions: number
├── Purpose: Total ad impressions for this record. Optional enrichment field from ExoClick response.
│   └── Mutable: yes

clicks: number
├── Purpose: Total ad clicks for this record. Optional enrichment field from ExoClick response.
│   └── Mutable: yes

ctr: number
├── Purpose: Click-through rate as a decimal (e.g., 0.025 = 2.5%). Computed from ExoClick response.
│   └── Mutable: yes

cpm: number
├── Purpose: Cost per mille (1000 impressions) in USD. Computed from ExoClick response.
│   └── Mutable: yes

rawResponse: map
├── Purpose: Sanitized snapshot of the raw API response row for debugging. Stripped of any credential fields before storage.
│   └── Mutable: yes

syncedAt: timestamp
├── Purpose: Server timestamp of when this record was written or last updated by a sync operation.
│   ├── Constraints: required
│   └── Mutable: yes

createdAt: timestamp
└── Purpose: Server timestamp of initial document creation. Immutable.
    ├── Constraints: required
    └── Mutable: no

FIRESTORE SECURITY RULES:
- Create: [AUTH_USER_ID] != null
- Read: [user_id] == [AUTH_USER_ID]
- Update: [user_id] == [AUTH_USER_ID]
- Delete: Soft delete (mark deleted, retain data)
- Immutable: createdAt, userId

LIFECYCLE:
- Created: set [SERVER_TIMESTAMP] for createdAt

## Fields

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| id | string | Yes | Auto-generated Firestore document ID |
| userId | string | Yes | UID of the owning user. Duplicated for Firestore Security Rules enforcement. |
| networkId | string | Yes | Ad network identifier — 'exoclick' for this feature. Indexed for per-network queries. |
| date | string | Yes | Date of the stat record in YYYY-MM-DD format. Indexed for date-range queries. |
| country | string | No | ISO 3166-1 alpha-2 country code (e.g., 'US', 'DE'). Null for aggregate rows. Indexed for GEO breakdown queries. |
| cost | number | No | Total ad spend cost in USD for this network/date/country combination. Set for ExoClick (cost-only network). Null for revenue-only networks. |
| revenue | number | No | Total revenue in USD. Null for ExoClick (cost-only). Set for revenue networks. |
| impressions | number | No | Total ad impressions for this record. Optional enrichment field from ExoClick response. |
| clicks | number | No | Total ad clicks for this record. Optional enrichment field from ExoClick response. |
| ctr | number | No | Click-through rate as a decimal (e.g., 0.025 = 2.5%). Computed from ExoClick response. |
| cpm | number | No | Cost per mille (1000 impressions) in USD. Computed from ExoClick response. |
| rawResponse | map | No | Sanitized snapshot of the raw API response row for debugging. Stripped of any credential fields before storage. |
| syncedAt | timestamp | Yes | Server timestamp of when this record was written or last updated by a sync operation. |
| createdAt | timestamp | Yes | Server timestamp of initial document creation. Immutable. |

---
Generated by VisualPRD